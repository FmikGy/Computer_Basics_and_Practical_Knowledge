# 国网计算机基础与实用知识晨练｜2026-09-15

> **今日主题：** PCI/PCI-X/PCIe/AGP 扩展总线、Windows 网络诊断命令、Linux 管道/重定向/通配符、字符编码、图像数字化与容量、扫描仪与显示器、备份基础、桌面快捷键与剪贴板  
> **今日目标：** 最近几天已重点覆盖 CPU 指标、存储接口与介质、FAT32/NTFS、Linux 权限/磁盘/进程、IEEE 802、主板芯片组、传统 I/O、BIOS/UEFI、音视频与文件格式等。今天避开连续热点，补齐扩展总线演进、实用网络命令、Shell 基础、字符编码、图像容量、外设、备份和桌面操作等“见过即可拿分”的杂项。

## 一、今日知识点

### 1. PCI、PCI-X、PCIe、AGP：扩展总线先看时代与定位

| 名称 | 基础定位 | 典型关键词 |
|---|---|---|
| PCI | 经典 PC 局部总线 | 并行、共享总线、历史常见 |
| PCI-X | PCI 的扩展，曾较多用于服务器 | 并行、服务器、向后兼容思想 |
| PCIe | 现代主流高速扩展互连 | 串行、点对点、Lane（通道） |
| AGP | 曾专门面向图形加速卡 | 显卡、历史接口，后被 PCIe 取代 |
| ISA/EISA | 更早期的扩展总线 | 老教材、历史 PC |

PCIe 与传统 PCI 的核心辨析不是“名字多了一个 e”，而是体系发生了明显变化：PCIe 采用高速串行、点对点连接，并可通过多个 Lane 组合形成 x1、x4、x8、x16 等链路宽度。现代独立显卡常使用 PCIe x16 形态的插槽。

**考试口径：** AGP 是早期显卡专用高速接口；PCI 是通用扩展总线；现代显卡主流转向 PCIe。

⚠️ **易错提醒：** PCIe 是串行互连，不要因为 x16 就误认为“16 位并行总线”。x16 表示由 16 条 Lane 组成的链路。

🧠 **速记：** `ISA 更老，PCI 通用并行，AGP 专显卡，PCIe 串行点对点。`

### 2. Windows 网络诊断命令：ipconfig、ping、tracert、netstat

| 命令 | 主要用途 | 高频关键词 |
|---|---|---|
| `ipconfig` | 查看/管理本机 TCP/IP 配置 | IP、掩码、网关、DNS 等 |
| `ping` | 测试 IP 层连通性和往返响应 | ICMP Echo |
| `tracert` | 跟踪数据包到目标的大致路径 | 逐跳、路由路径 |
| `netstat` | 查看网络连接、监听端口等统计信息 | 连接、端口、状态 |
| `hostname` | 显示主机名 | 主机名称 |

常见示例：

```bat
ipconfig /all
ping 8.8.8.8
tracert example.com
netstat -ano
hostname
```

📌 **国网考点：** “查看本机 IP 配置”优先想到 `ipconfig`；“测试目标是否可达”想到 `ping`；“经过哪些路由节点”想到 `tracert`；“查看连接和端口”想到 `netstat`。

⚠️ **易错提醒：** `ping` 失败不一定证明目标主机宕机，因为中间设备或目标主机可能禁用/过滤 ICMP。

### 3. Linux 管道、重定向与通配符：符号用途别串台

Shell 中几个符号是高性价比记忆点：

| 符号 | 基础含义 |
|---|---|
| `>` | 将标准输出重定向到文件，通常覆盖 |
| `>>` | 将标准输出追加到文件 |
| `<` | 从文件重定向标准输入 |
| `|` | 把前一命令的输出作为后一命令的输入 |
| `*` | 通配任意长度字符序列 |
| `?` | 通配单个字符 |

示例：

```bash
ls -l > files.txt
printf 'new line\n' >> files.txt
ps aux | grep nginx
ls *.log
ls file?.txt
```

`ps aux | grep nginx` 中，管道 `|` 负责把 `ps` 的输出交给 `grep` 筛选。它与 `>` 的“写入文件”作用完全不同。

📌 **国网考点：** `>` 与 `>>` 最爱考“覆盖还是追加”；`|` 最爱考“连接两个命令的数据流”。

🧠 **速记：** `单尖括号覆盖，双尖括号追加，竖线前出后进。`

### 4. ASCII、Unicode、UTF-8、UTF-16、GB2312、GBK：字符集和编码要分层

| 名称 | 基础定位 | 考试识别点 |
|---|---|---|
| ASCII | 早期字符编码标准，基础 ASCII 使用 7 位 | 英文字母、数字、控制字符 |
| Unicode | 为全球字符建立统一编码空间/字符编号体系 | 统一字符集合思想 |
| UTF-8 | Unicode 的一种编码形式 | 变长、ASCII 兼容性好 |
| UTF-16 | Unicode 的一种编码形式 | 以 16 位代码单元为基础，可使用代理对 |
| GB2312 | 中国较早的汉字编码标准 | 简体中文基础字符集 |
| GBK | 对 GB2312 的扩展 | 覆盖字符更多，兼容 GB2312 常用编码范围 |

UTF-8 不是“另一套与 Unicode 无关的字符集”。考试中可理解为：Unicode 解决“字符对应什么码点”，UTF-8/UTF-16 解决这些 Unicode 码点如何编码成字节序列。

⚠️ **易错提醒：** 一个 Unicode 字符在 UTF-8 中不固定占 1 字节；中文字符通常也不能按“一个汉字恒等于 2 字节”机械判断，具体取决于编码。

🧠 **速记：** `Unicode 定码点，UTF-8/16 定编码；GBK 是 GB2312 的扩展。`

### 5. 像素、分辨率、色深与未压缩图像容量

数字图像题通常只需掌握四个关键词：

- **像素（pixel）**：数字图像的基本图像元素。
- **分辨率**：常以水平像素 × 垂直像素表示，如 1920×1080。
- **色深**：描述每个像素用于表示颜色的信息位数。
- **颜色模型**：常见 RGB 用红、绿、蓝三个分量描述颜色；印刷领域常见 CMYK。

对于**未压缩位图**，基础估算可用：

`图像数据量（bit）≈ 水平像素 × 垂直像素 × 每像素位数`

例如 100×100 像素、24 bit 色深的未压缩图像，原始像素数据约为：

`100 × 100 × 24 = 240000 bit = 30000 Byte`

实际文件还可能包含文件头、调色板、元数据等，因此文件大小未必严格等于这个值。

📌 **国网考点：** 24 位真彩色通常可理解为 RGB 三个通道各 8 位，共可表达约 2^24 种颜色组合。

⚠️ **易错提醒：** 分辨率提高、色深提高通常都会增加原始数据量；JPEG/PNG 等压缩后文件大小不能直接套未压缩公式。

### 6. 扫描仪、显示器与输入/输出设备分类

| 设备 | 基础分类 | 作用 |
|---|---|---|
| 键盘 | 输入设备 | 输入字符/命令 |
| 鼠标 | 输入设备 | 指点输入 |
| 扫描仪 | 输入设备 | 将纸质图文数字化 |
| 显示器 | 输出设备 | 视觉输出 |
| 打印机 | 输出设备 | 产生纸质输出 |
| 触摸屏 | 可同时涉及输入与输出 | 显示 + 触控输入 |

扫描仪常见指标包括光学分辨率等；显示器常见指标包括分辨率、刷新率、尺寸等。基础考试重在识别设备类别，而不是背具体产品参数。

**历史口径：** CRT 显示器使用阴极射线管，体积和重量较大；LCD 液晶显示器后来成为主流平板显示技术。现代显示技术还包括 OLED 等，但老题问 CRT/LCD 时应按教材时代回答。

⚠️ **易错提醒：** “触摸屏一定只是输出设备”错误，因为触控本身承担输入功能。

### 7. 完全备份、增量备份、差异备份：恢复链条是关键

| 方式 | 每次备份什么 | 恢复时通常需要 |
|---|---|---|
| 完全备份 | 选定范围内全部数据 | 最近一次完整备份 |
| 增量备份 | 自上一次备份（完整或增量）以来的变化 | 完整备份 + 之后各次增量 |
| 差异备份 | 自上一次完整备份以来的变化 | 完整备份 + 最新一次差异 |

例：周日完全备份，周一到周三每天增量备份。若周三故障，恢复通常需要周日完整备份，再依次应用周一、周二、周三增量。

若采用差异备份，则周三差异备份已经累计包含自周日完整备份后的变化，恢复通常只需“周日完整 + 周三差异”。

📌 **国网考点：** 增量通常单次备份量较小，但恢复链较长；差异备份会随距离上次完整备份时间增长而变大，但恢复链较短。

⚠️ **易错提醒：** RAID、备份、容灾不是同义词。RAID 可提高可用性或性能，但不能替代独立备份。

### 8. 剪贴板、快捷键、回收站与临时文件：桌面系统小常识

Windows 等桌面系统中常见快捷操作：

| 快捷键 | 常见作用 |
|---|---|
| `Ctrl+C` | 复制 |
| `Ctrl+X` | 剪切 |
| `Ctrl+V` | 粘贴 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+A` | 全选 |
| `Alt+Tab` | 在打开的应用/窗口间切换 |
| `Win+E` | 打开文件资源管理器 |

**剪贴板**用于暂存复制/剪切的数据，是一种逻辑上的临时交换区域；复制操作一般不会删除原对象，剪切后完成粘贴则表现为移动。

**回收站**用于暂存部分通过图形界面删除的文件，使其有机会恢复。但并非所有删除操作都会进入回收站，例如某些命令行删除、网络位置删除或永久删除操作可能直接移除。

**临时文件**是程序或系统在运行过程中产生的临时数据，不能简单等同于“垃圾文件”；程序正常运行期间可能仍需要它们。

📌 **国网考点：** `Ctrl+C/X/V`、剪贴板和回收站的基本作用属于典型应用常识题。

---

## 二、国网风格练习题

### 1. 下列关于 PCIe 的说法，正确的是（ ）。

A. PCIe 是典型的并行共享总线  
B. PCIe 采用高速串行、点对点连接  
C. PCIe 仅能连接显卡  
D. PCIe 已被 AGP 全面取代

**答案：B**

**解析：** PCIe 采用串行、点对点体系，并以 Lane 组织链路。A 描述更接近传统 PCI 的特征；C 错在 PCIe 可连接显卡、网卡、存储控制器等多种设备；D 方向相反，现代 PC 中是 PCIe 取代了 AGP 的显卡扩展地位。

### 2. 在 Windows 中，要查看本机较完整的 TCP/IP 配置信息，最合适的命令是（ ）。

A. `ipconfig /all`  
B. `tracert`  
C. `tree`  
D. `type`

**答案：A**

**解析：** `ipconfig /all` 可显示较详细的网络配置信息。`tracert` 用于路径跟踪；`tree` 显示目录树；`type` 显示文本文件内容。

### 3. 在 Windows 中，主要用于查看当前网络连接、监听端口等信息的命令是（ ）。

A. `netstat`  
B. `format`  
C. `ren`  
D. `chkdsk`

**答案：A**

**解析：** `netstat` 用于显示网络连接、监听端口和相关统计信息。其余三项分别与格式化、重命名和磁盘检查有关。

### 4. Linux 命令 `ps aux | grep nginx` 中符号 `|` 的作用是（ ）。

A. 将输出覆盖写入文件  
B. 将输出追加到文件  
C. 将前一命令的输出作为后一命令的输入  
D. 同时启动两个互不相关的命令

**答案：C**

**解析：** `|` 是管道符。A 对应 `>`，B 对应 `>>`。管道的核心是连接两个命令的数据流。

### 5. 下列关于 UTF-8 的说法，正确的是（ ）。

A. UTF-8 与 Unicode 完全无关  
B. UTF-8 中每个字符固定占 8 bit  
C. UTF-8 是 Unicode 的一种编码形式  
D. UTF-8 只能表示英文字符

**答案：C**

**解析：** UTF-8 是 Unicode 的一种变长编码方式。名称中的“8”并不表示每个字符固定只占 8 bit；它也远不只支持英文字符。

### 6. 一幅 100×100 像素、24 bit 色深的未压缩图像，仅计算原始像素数据，其容量约为（ ）。

A. 10000 Byte  
B. 24000 Byte  
C. 30000 Byte  
D. 240000 Byte

**答案：C**

**解析：** 数据量为 `100×100×24=240000 bit`，再除以 8 得 `30000 Byte`。D 把 bit 数误当成 Byte 数，是常见干扰项。

### 7. 下列设备中，典型地属于输入设备的是（ ）。

A. 扫描仪  
B. 打印机  
C. 普通显示器  
D. 投影仪

**答案：A**

**解析：** 扫描仪将纸质图文转换为数字信息并输入计算机。打印机、普通显示器和投影仪主要属于输出设备。

### 8. 周日进行一次完全备份，周一、周二、周三分别进行增量备份。周三备份完成后发生故障，通常恢复到周三状态需要（ ）。

A. 只使用周三增量备份  
B. 周日完全备份和周三增量备份  
C. 周日完全备份以及周一、周二、周三的增量备份  
D. 只使用周日完全备份

**答案：C**

**解析：** 增量备份只保存自上一次备份以来的变化，因此恢复时通常要从完整备份开始，按顺序应用后续每次增量。若是差异备份，则恢复链通常更短。

### 9. 下列快捷键与功能对应正确的是（ ）。

A. `Ctrl+V`——复制  
B. `Ctrl+X`——剪切  
C. `Ctrl+Z`——全选  
D. `Alt+Tab`——删除文件

**答案：B**

**解析：** `Ctrl+X` 是剪切；`Ctrl+V` 是粘贴；`Ctrl+Z` 是撤销；`Alt+Tab` 常用于窗口/应用切换。

### 10. 【间隔复习】关于 NVMe 与 SATA 的说法，正确的是（ ）。

A. 所有 SSD 都必须使用 NVMe  
B. NVMe 常运行在 PCIe 之上，面向高速非易失存储  
C. SATA 只能连接机械硬盘  
D. M.2 外形的 SSD 必然是 NVMe SSD

**答案：B**

**解析：** NVMe 常与 PCIe 高速链路结合。SSD 也可以使用 SATA；SATA 不仅用于 HDD，也可用于 SSD；M.2 是形态/连接规格的一部分，存在 SATA M.2 与 PCIe/NVMe M.2，不能看到 M.2 就直接等同 NVMe。

### 11. 【间隔复习】Linux 中用于查看某个目录自身占用空间的常用命令是（ ）。

A. `df`  
B. `du`  
C. `mount`  
D. `pwd`

**答案：B**

**解析：** `du` 用于统计文件或目录占用空间；`df` 更侧重文件系统整体空间使用情况；`mount` 用于挂载；`pwd` 显示当前工作目录。

### 12. 【间隔复习】下列关于 SSD 的说法，错误的是（ ）。

A. 常见 SSD 使用 NAND Flash  
B. SSD 没有 HDD 那样的磁头寻道机械动作  
C. SSD 必然属于磁存储介质  
D. SSD 可采用 SATA 接口，也可采用 PCIe/NVMe 方案

**答案：C**

**解析：** 常见 SSD 属于半导体存储，不是磁存储。HDD 才是典型磁存储。A、B、D 均符合基础考试口径。

### 13. 【间隔复习】传统教材中，主要负责高速部件连接、常与 CPU、内存和显卡联系在一起的芯片组部分是（ ）。

A. 南桥  
B. 北桥  
C. CMOS 电池  
D. BIOS

**答案：B**

**解析：** 传统“北桥+南桥”架构中，北桥通常负责 CPU、内存及高速图形接口等高速连接；南桥偏向 I/O。现代处理器已集成许多传统北桥功能，因此要区分考试历史口径与现代实现。

### 14. 【间隔复习】在 FAT32 与 NTFS 的基础比较中，下列说法较符合常见考试口径的是（ ）。

A. FAT32 通常比 NTFS 提供更丰富的权限与日志机制  
B. NTFS 支持权限、日志等较丰富的文件系统特性  
C. NTFS 只能用于软盘  
D. FAT32 是 Linux 独有文件系统

**答案：B**

**解析：** NTFS 是 Windows NT 系列常用文件系统，支持访问控制、日志等丰富特性。FAT32 结构相对简单、兼容性广，但不具备 NTFS 同等级的权限和日志能力。

---

## 三、今日速记

### 1. 扩展总线演进

- **ISA/EISA**：老式扩展总线，认历史定位。
- **PCI**：经典通用并行共享总线。
- **AGP**：历史显卡专用接口。
- **PCIe**：现代主流，**串行 + 点对点 + Lane**，常见 x1/x4/x8/x16。
- 口诀：**“ISA 老，PCI 广，AGP 显卡，PCIe 串行闯天下。”**

### 2. Windows 网络四件套

| 想做什么 | 命令 |
|---|---|
| 看本机 IP 配置 | `ipconfig` |
| 测连通 | `ping` |
| 看路径 | `tracert` |
| 看连接/端口 | `netstat` |

### 3. Linux Shell 符号

- `>`：覆盖输出。
- `>>`：追加输出。
- `<`：输入重定向。
- `|`：前一命令输出 → 后一命令输入。
- `*`：任意长度字符序列。
- `?`：单个字符。

### 4. 编码关系

- ASCII：基础 ASCII **7 位**。
- Unicode：统一字符码点体系。
- UTF-8：Unicode 变长编码，ASCII 兼容性好。
- UTF-16：以 16 位代码单元为基础，可用代理对。
- GB2312：较早简体中文字符编码标准。
- GBK：对 GB2312 的扩展。
- 口诀：**“Unicode 定字符，UTF 负责怎么存。”**

### 5. 图像容量

`未压缩像素数据量(bit) ≈ 宽 × 高 × 色深`

- 8 bit = 1 Byte。
- 24 bit RGB：常理解为 R/G/B 各 8 bit。
- 压缩后的 JPEG/PNG 文件大小不能直接用原始公式判断。

### 6. 输入输出设备

- 输入：键盘、鼠标、扫描仪。
- 输出：显示器、打印机、投影仪。
- 触摸屏：兼具显示输出与触控输入。

### 7. 三种备份

- **完全**：全备；恢复最直接。
- **增量**：自上一次备份后的变化；恢复 = 完整 + 后续所有增量。
- **差异**：自上一次完整备份后的变化；恢复 = 完整 + 最新差异。
- **RAID ≠ 备份**。

### 8. 桌面快捷键

- `Ctrl+C` 复制；`Ctrl+X` 剪切；`Ctrl+V` 粘贴。
- `Ctrl+Z` 撤销；`Ctrl+A` 全选。
- `Alt+Tab` 切换应用/窗口；`Win+E` 文件资源管理器。
- 回收站不是所有删除操作的必经之地。
