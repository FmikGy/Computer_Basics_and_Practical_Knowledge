# 国网计算机基础与实用知识晨练｜2026-09-19

> **今日主题：** Cache/显存/虚拟内存、FAT/FAT32/NTFS、Linux 进程与作业控制、USB/PS2 接口、图像格式、浏览器缓存/临时文件/回收站、容量单位换算、软盘与历史存储口径  
> **今日目标：** 避开近几日已重点覆盖的 DDR、IEEE 802、BIOS/UEFI、注册表、Linux 用户组与文本处理等内容，补齐存储层次、Windows 文件系统、Linux 进程管理、外设接口、图像格式及容量单位等基础杂项，并用滚动题复习近期知识。

## 一、今日知识点

### 1. Cache、显存与虚拟内存：名字都带“存储”，位置和职责完全不同

| 概念 | 核心作用 | 典型位置/介质 | 高频辨析 |
|---|---|---|---|
| Cache（高速缓存） | 缓解 CPU 与主存速度差距 | CPU 内部或邻近 CPU，常用 SRAM | 容量较小、速度很快 |
| 显存（VRAM） | 存放 GPU 图形处理所需数据 | 显卡或集成图形共享体系 | 服务于图形处理，不是磁盘缓存 |
| 虚拟内存 | 借助存储设备扩展进程可用地址空间，并配合内存管理 | RAM + 磁盘/SSD 后备存储 | 不是“真的把硬盘变成同速内存” |

📌 **国网考点：** Cache 常与 SRAM 联系；主存常与 DRAM 联系。虚拟内存容量大不代表访问速度快。

⚠️ **易错提醒：** 显存不是 Cache，虚拟内存也不是一种新的 RAM 芯片。

### 2. FAT、FAT32、NTFS：Windows 文件系统的考试口径

| 文件系统 | 基础定位 | 典型特点 |
|---|---|---|
| FAT | 较早的文件分配表体系 | 结构较简单，历史设备常见 |
| FAT32 | FAT 家族后续常见版本 | 兼容性较广，但存在单文件大小等限制 |
| NTFS | Windows NT 系列主要文件系统 | 支持权限、日志、较大文件/卷等现代特性 |

- **考试口径：** Windows 系统盘和需要权限控制的现代场景通常优先想到 NTFS。
- **现代情况：** 可移动设备还可能使用 exFAT 等，但不能因此否定教材中 FAT32/NTFS 的典型定位。

📌 **国网考点：** “支持文件级权限、日志等”通常指向 NTFS；FAT32 不具备 NTFS 那套完整的 ACL 权限机制。

### 3. Linux 进程与作业：ps、top、kill、jobs、bg、fg、nohup

| 命令 | 基础用途 |
|---|---|
| `ps` | 查看进程快照 |
| `top` | 动态查看进程与系统资源 |
| `kill` | 向进程发送信号，常用于终止进程 |
| `jobs` | 查看当前 Shell 的作业 |
| `bg` | 让暂停的作业在后台继续 |
| `fg` | 把后台作业调到前台 |
| `nohup` | 使程序忽略挂断信号，常用于退出终端后继续运行 |

```bash
ps -ef
top
jobs
fg %1
nohup command > out.log 2>&1 &
```

⚠️ **易错提醒：** `kill` 的本质是发送信号，不等于任何情况下都“强制杀死”；`jobs` 关注当前 Shell 作业，与 `ps` 的系统进程视角不同。

### 4. USB 与 PS/2：现代通用接口和传统专用接口

| 接口 | 常见用途 | 基础特点 |
|---|---|---|
| USB | 键盘、鼠标、U 盘、打印机等大量外设 | 通用、支持热插拔是常见考试特征 |
| PS/2 | 传统键盘、鼠标 | 圆形 Mini-DIN 接口，现代 PC 已较少见 |

- USB 是 **Universal Serial Bus（通用串行总线）**。
- 传统教材中 PS/2 常按键盘、鼠标专用接口识别。
- 不要把 USB 的“串行”与传统 RS-232 串口混为同一种接口。

🧠 **速记：** USB＝通用串行外设接口；PS/2＝老式键鼠。

### 5. BMP、JPEG、PNG、GIF、TIFF：看“压缩、透明、动画、场景”

| 格式 | 高频特点 | 典型场景 |
|---|---|---|
| BMP | 常见位图格式，传统口径下可不压缩或压缩较少 | 原始位图、教学题 |
| JPEG/JPG | 有损压缩，适合连续色调照片 | 数码照片 |
| PNG | 无损压缩，支持透明 | 网页图形、截图 |
| GIF | 常见 256 色索引色，可支持简单动画与透明 | 简单动画、图标 |
| TIFF | 灵活的图像文件格式，可支持多种压缩方式 | 扫描、出版等 |

📌 **国网考点：** “照片 + 较高压缩率”常选 JPEG；“无损 + 透明”常选 PNG；“简单动画”常选 GIF。

⚠️ **易错提醒：** 文件扩展名只是识别线索，把文件改名为 `.jpg` 不会自动把内部数据转换成 JPEG 编码。

### 6. 浏览器缓存、临时文件与回收站：都不是“永久备份”

- **浏览器缓存**：保存部分已访问资源，以减少重复下载、提升访问体验；不是用户文件的可靠备份。
- **临时文件**：程序或系统运行期间产生的中间数据，通常可在适当条件下清理。
- **回收站**：Windows 中为部分本地删除操作提供恢复机会；并非所有删除都一定进入回收站。

📌 **国网考点：** `Shift + Delete` 常用于绕过回收站直接删除；从某些可移动介质或命令行删除也不能简单假定一定进入回收站。

### 7. bit、Byte、KB、MB、GB、TB：先分 b 和 B，再看换算口径

- `b` 通常表示 **bit（位）**；`B` 通常表示 **Byte（字节）**。
- **1 Byte = 8 bit**。
- 传统计算机基础题常采用二进制口径：`1 KB = 1024 B`、`1 MB = 1024 KB`、`1 GB = 1024 MB`。
- 现代标准中还有 KiB、MiB、GiB 明确表示 1024 进制，而存储厂商容量标称常采用十进制。做题应看题目口径。

⚠️ **易错提醒：** 网络速率中的 Mbps 通常是 megabits per second，不是 MB/s；若忽略协议开销，8 Mbps 理论上约对应 1 MB/s。

### 8. 软盘：淘汰技术仍可能以“历史常识”出现

- 软盘（Floppy Disk）是磁性可移动存储介质，容量远小于现代 U 盘和硬盘。
- 老教材最常见的是 **3.5 英寸软盘**；它不是光存储介质。
- 软盘驱动器曾是 PC 常见外设，如今基本淘汰。

**考试口径 vs 现代情况：** 老题把软盘与硬盘都归为磁存储介质是正确的历史分类；现代 PC 不再常配软驱，不影响这一知识点成立。

🧠 **速记：** 软盘/硬盘＝磁；CD/DVD＝光；U 盘/SSD＝半导体闪存体系。

## 二、国网风格练习题

### 1. 下列最适合用于缓解 CPU 与主存速度差距的是（ ）。
A. Cache  
B. 回收站  
C. 显存  
D. 虚拟光驱

**答案：A**  
**解析：** Cache 位于处理器存储层次的高速位置，常使用 SRAM，用于减少 CPU 等待主存的时间。显存主要服务 GPU；回收站属于文件删除恢复机制。

### 2. 关于虚拟内存，下列说法正确的是（ ）。
A. 它是一种特殊 ROM  
B. 它使磁盘访问速度等于 Cache  
C. 它利用地址空间管理并可借助外存作为后备存储  
D. 它只存在于显卡中

**答案：C**  
**解析：** 虚拟内存是内存管理机制，可借助磁盘/SSD 后备存储；它不会让外存获得 RAM 或 Cache 的访问速度。

### 3. Windows 文件系统中，与文件权限、日志等特性联系最紧密的是（ ）。
A. FAT12  
B. FAT32  
C. NTFS  
D. ISO 9660

**答案：C**  
**解析：** NTFS 支持 ACL 权限、日志等特性。FAT32 兼容性广但没有 NTFS 的完整权限体系；ISO 9660 主要与光盘文件系统相关。

### 4. Linux 中用于动态观察进程和系统资源的常用命令是（ ）。
A. top  
B. mkdir  
C. cut  
D. chmod

**答案：A**  
**解析：** `top` 动态显示进程与资源信息；`mkdir` 创建目录，`cut` 截取文本字段，`chmod` 修改权限。

### 5. Linux 中 `jobs` 命令主要用于（ ）。
A. 查看当前 Shell 的作业  
B. 创建用户  
C. 检查文件系统  
D. 修改文件所有者

**答案：A**  
**解析：** `jobs` 面向当前 Shell 的作业控制，常与 `bg`、`fg` 配合。查看更广泛的进程通常使用 `ps`、`top` 等。

### 6. 关于 USB 和 PS/2，下列说法正确的是（ ）。
A. PS/2 是现代最通用的移动存储协议  
B. USB 只能连接键盘  
C. USB 是通用串行总线，可连接多种外设  
D. USB 与 RS-232 串口完全相同

**答案：C**  
**解析：** USB 可连接键鼠、存储设备、打印机等多种外设。PS/2 主要是传统键鼠接口；USB 虽然是串行总线，但不能等同传统 RS-232。

### 7. 某图片要求采用无损压缩并支持透明背景，较典型的选择是（ ）。
A. JPEG  
B. PNG  
C. MP3  
D. WAV

**答案：B**  
**解析：** PNG 典型特点是无损压缩并支持透明。JPEG 常采用有损压缩，适合照片；MP3、WAV 是音频相关格式。

### 8. 下列最典型地支持简单动画的图像格式是（ ）。
A. GIF  
B. TXT  
C. BMP  
D. PDF

**答案：A**  
**解析：** GIF 可保存多帧形成简单动画，并使用索引色。BMP 主要是位图格式；TXT 不是图像格式。

### 9. 在传统计算机基础题采用 1024 进制换算时，1 MB 等于（ ）。
A. 8 KB  
B. 100 KB  
C. 1024 KB  
D. 1024 bit

**答案：C**  
**解析：** 传统考试口径常取 1 MB = 1024 KB。注意现代 SI 标称与 IEC 的 MiB 等符号可采用更严格区分，做题应遵循题干口径。

### 10. 关于软盘，下列说法正确的是（ ）。
A. 属于典型光存储介质  
B. 属于历史上的磁性可移动存储介质  
C. 与 SSD 原理相同  
D. 是现代 PC 必备启动设备

**答案：B**  
**解析：** 软盘采用磁记录，属于历史上的可移动磁存储介质。CD/DVD 才是典型光存储；SSD 使用半导体存储技术。

### 11. 【滚动复习】DDR 内存通常属于（ ）。
A. SRAM  
B. DRAM  
C. ROM  
D. 光存储器

**答案：B**  
**解析：** DDR SDRAM 属于 DRAM 家族，主要用于主存。SRAM 常用于 Cache，这是两者的高频辨析。

### 12. 【滚动复习】Windows 中用于查看硬件设备及驱动状态的工具是（ ）。
A. 设备管理器  
B. 画图  
C. 记事本  
D. 回收站

**答案：A**  
**解析：** 设备管理器用于查看设备及驱动状态；任务管理器重点是进程、性能和启动项等，二者容易混淆。

### 13. 【滚动复习】IEEE 802.3 主要对应（ ）。
A. Ethernet  
B. WLAN  
C. Bluetooth  
D. WiMAX

**答案：A**  
**解析：** 802.3 对应以太网相关标准；802.11 对应 WLAN。Bluetooth 常与 802.15.1 的历史标准化联系，WiMAX 常与 802.16 联系。

### 14. 【滚动复习】Linux 中用于按模式筛选文本行的命令是（ ）。
A. grep  
B. pwd  
C. mount  
D. passwd

**答案：A**  
**解析：** `grep` 按模式匹配并筛选文本行；`pwd` 显示当前目录，`mount` 挂载文件系统，`passwd` 修改密码。

## 三、今日速记

| 看到关键词 | 快速联想 |
|---|---|
| CPU 与主存速度差 | Cache；常见 SRAM |
| 主存 | DRAM；DDR 属于 DRAM 家族 |
| GPU 图形数据 | 显存 |
| 外存作后备、地址空间 | 虚拟内存 |
| Windows 权限/日志文件系统 | NTFS |
| 动态进程监视 | `top` |
| 进程快照 | `ps` |
| 当前 Shell 作业 | `jobs`；前台 `fg`；后台 `bg` |
| 退出终端后继续运行 | `nohup`（常配合 `&`） |
| 通用串行外设接口 | USB |
| 老式键鼠接口 | PS/2 |
| 照片、有损 | JPEG |
| 无损、透明 | PNG |
| 简单动画 | GIF |
| 位与字节 | 1 B = 8 bit |
| 传统考试容量换算 | 1 KB = 1024 B，逐级 1024 |
| 磁存储 | 软盘、传统 HDD |
| 光存储 | CD/DVD |
| 闪存 | U 盘、SSD |

🧠 **最后一分钟口诀：** Cache 缓 CPU，显存供 GPU，虚拟内存靠地址管理；NTFS 看权限日志；`ps` 看快照、`top` 看动态、`jobs/bg/fg` 管 Shell 作业；JPEG 照片有损、PNG 无损透明、GIF 能动画；B 是字节、b 是位。