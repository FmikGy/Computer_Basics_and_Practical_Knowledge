# 国网计算机基础与实用知识晨练｜2026-09-07

> **今日主题：** 存储介质与接口辨析、Windows 文件系统与扩展名、Linux 管道重定向、网络设备、音频数字化、桌面应用常识、历史显示与总线知识  
> **今日目标：** 避开近几日已重点覆盖的主板芯片组、IEEE 802 总览、注册表、Linux 权限、字符编码等主题，补齐文件系统、Shell 实用操作、网络设备、音频参数、桌面应用及历史硬件杂项。

## 一、今日知识点

### 1. HDD、SSD 与光学/闪存介质：先看存储原理

| 介质 | 典型原理 | 主要特点 |
|---|---|---|
| HDD | 磁性盘片 + 机械磁头 | 容量大、单位成本低，有机械寻道 |
| SSD | NAND Flash 等半导体存储 | 无机械部件、随机访问快、抗震性较好 |
| U 盘 | Flash 闪存 | 便携，通常经 USB 接口连接 |
| CD/DVD | 光学介质 | 通过激光读写，属于光存储 |
| 软盘 | 磁介质 | 历史设备，容量小、速度慢 |

📌 **国网考点：** SSD 没有传统 HDD 的机械寻道过程；U 盘的“USB”主要描述外部接口，内部存储介质通常是闪存。

⚠️ **易错提醒：** “固态硬盘”不等于“内存”，断电后数据通常仍能保存。

### 2. IDE/PATA、SATA、SCSI、NVMe：接口/协议与设备不要混

| 名称 | 考试层面定位 | 典型辨析 |
|---|---|---|
| IDE/PATA | 传统并行 ATA 磁盘接口 | 排线较宽，属于历史 PC 常见接口 |
| SATA | 串行 ATA | 常连接 HDD、2.5 英寸 SATA SSD 等 |
| SCSI | 面向设备通信的一类接口/协议体系 | 历史上服务器、工作站场景较常见 |
| NVMe | 面向非易失性存储的高性能协议 | 常运行于 PCIe 之上 |

🧠 **速记：** `SATA 是串行 ATA；NVMe 常走 PCIe；SSD 是设备类型，不等于 NVMe。`

### 3. FAT32 与 NTFS：Windows 文件系统基础

| 项目 | FAT32 | NTFS |
|---|---|---|
| 定位 | 较老且兼容性广的文件系统 | Windows 常用现代文件系统 |
| 单文件大小 | 存在约 4 GiB 上限 | 支持远大于 FAT32 的文件 |
| 权限/安全 | 功能较简单 | 支持 ACL 权限等特性 |
| 典型场景 | 可移动介质、兼容场景 | Windows 系统盘、日常磁盘 |

📌 **国网考点：** NTFS 相比 FAT/FAT32 提供更丰富的安全、权限和可靠性能力。

⚠️ **易错提醒：** 文件系统和磁盘物理介质不是同一概念；HDD、SSD 都可以使用 NTFS。

### 4. exe、dll、sys、lnk、msi：Windows 常见扩展名

| 扩展名 | 常见含义 |
|---|---|
| `.exe` | 可执行程序 |
| `.dll` | 动态链接库 |
| `.sys` | 常见系统/驱动相关文件 |
| `.lnk` | Windows 快捷方式文件 |
| `.msi` | Windows Installer 安装包 |
| `.bat` / `.cmd` | 命令脚本 |

📌 **国网考点：** `.lnk` 通常只是指向目标的快捷方式，删除快捷方式一般不等于删除原文件。

### 5. Linux 管道与重定向：`|`、`>`、`>>`、`<`

Shell 中常把多个小工具组合起来：

```bash
ps aux | grep nginx
ls -l > list.txt
printf "new line\n" >> log.txt
sort < names.txt
```

| 符号 | 基础含义 |
|---|---|
| `|` | 把前一个命令的标准输出交给后一个命令作为输入 |
| `>` | 输出重定向到文件，通常覆盖原内容 |
| `>>` | 追加输出到文件末尾 |
| `<` | 从文件取得标准输入 |

⚠️ **易错提醒：** 管道连接的是命令的数据流，不等于文件复制；`>` 与 `>>` 最常考“覆盖/追加”区别。

### 6. 集线器、交换机、路由器、AP、调制解调器

| 设备 | 基础作用 | 常见考点 |
|---|---|---|
| 集线器 Hub | 多端口信号转发 | 传统上工作在物理层，不识别 MAC 转发表 |
| 交换机 Switch | 局域网帧转发 | 典型二层交换机依据 MAC 地址转发 |
| 路由器 Router | 不同网络间分组转发 | 依据网络层地址和路由信息选择路径 |
| AP | 提供无线终端接入有线网络的接入点 | WLAN 常见设备 |
| Modem | 调制/解调，适配特定接入链路 | 传统拨号等教材题常见 |

📌 **国网考点：** “交换机看 MAC、路由器看 IP”是基础题常用速记，但现代设备可能集成多层功能。

### 7. 音频数字化：采样频率、采样位数、声道数、码率

未经压缩 PCM 音频的理论数据率可用：

```text
码率 = 采样频率 × 采样位数 × 声道数
```

例如 44.1 kHz、16 bit、双声道 PCM：

```text
44100 × 16 × 2 = 1,411,200 bit/s ≈ 1411.2 kbit/s
```

- **采样频率**：每秒采样次数。
- **采样位数**：每个样本量化所用位数。
- **声道数**：单声道为 1，立体声通常为 2。
- MP3、AAC 通常采用有损压缩，因此实际文件码率不能直接用未压缩 PCM 公式代替。

### 8. 剪贴板、缓存、临时文件、回收站：桌面常识辨析

| 概念 | 核心定位 |
|---|---|
| 剪贴板 | 复制/剪切操作中暂存数据的机制 |
| Cache 缓存 | 保存近期/常用数据以提高访问效率 |
| 临时文件 | 程序运行过程中产生的阶段性文件 |
| 回收站 | Windows 对部分本地删除操作提供的恢复缓冲区 |

⚠️ **易错提醒：** 从可移动磁盘删除文件、使用某些永久删除方式时，文件未必进入回收站；不能把“删除”一概理解为“必进回收站”。

### 9. CRT、VGA、AGP：历史题要按教材口径识别

- **CRT**：阴极射线管显示器，体积大、重量大，是 LCD 普及前的主流显示技术之一。
- **VGA**：既可指历史显示标准，在接口题中通常指 15 针模拟视频接口。
- **AGP**：Accelerated Graphics Port，历史上为显卡设计的高速图形接口，后来被 PCI Express 取代。

📌 **考试口径：** 老题中 AGP 与显卡高度关联；现代独立显卡通常使用 PCIe。VGA 传输模拟视频，而 HDMI、DisplayPort 等现代接口主要传输数字信号。

## 二、国网风格练习题

### 1. 下列存储设备中，不存在传统机械磁头寻道过程的是（ ）。
A. HDD  
B. SSD  
C. 软盘  
D. 磁带

**答案：B**

**解析：** SSD 采用闪存等半导体存储，没有 HDD/软盘一类磁介质设备的机械磁头寻道过程。A、C 都属于磁介质；磁带同样不是固态随机访问设备。

### 2. 关于 SATA 与 NVMe，下列说法正确的是（ ）。
A. NVMe 是一种显示接口  
B. SATA SSD 与 NVMe SSD 完全是同一概念  
C. NVMe 常用于基于 PCIe 的高性能非易失性存储  
D. SATA 只能连接机械硬盘

**答案：C**

**解析：** NVMe 是面向非易失性存储的协议，常运行在 PCIe 上。SATA 既可连接 HDD，也可连接 SATA SSD；“SSD”描述设备类别，不能直接等同于某一种接口或协议。

### 3. 与 FAT32 相比，NTFS 的典型优势是（ ）。
A. 只能用于软盘  
B. 支持更丰富的权限和安全机制  
C. 不支持大文件  
D. 不属于文件系统

**答案：B**

**解析：** NTFS 支持 ACL 权限等丰富能力，且支持远大于 FAT32 单文件上限的文件。A、C、D 均错误。

### 4. Windows 中通常表示快捷方式文件的扩展名是（ ）。
A. `.dll`  
B. `.lnk`  
C. `.sys`  
D. `.msi`

**答案：B**

**解析：** `.lnk` 是 Windows 快捷方式常见扩展名；`.dll` 为动态链接库，`.sys` 常用于系统/驱动相关文件，`.msi` 常见于 Windows Installer 安装包。

### 5. Linux Shell 中，若希望把命令输出追加到已有文件末尾，应使用（ ）。
A. `>`  
B. `>>`  
C. `<`  
D. `|`

**答案：B**

**解析：** `>>` 表示追加重定向；`>` 通常覆盖文件；`<` 是输入重定向；`|` 是管道。

### 6. 在典型以太网局域网中，二层交换机转发帧主要依据（ ）。
A. MAC 地址  
B. 进程 PID  
C. 文件扩展名  
D. CPU 主频

**答案：A**

**解析：** 典型二层交换机根据 MAC 地址表进行帧转发。路由器的基础考法则强调 IP 地址和路由信息。

### 7. 无线局域网中用于为无线终端提供接入的常见设备是（ ）。
A. AP  
B. CRT  
C. AGP  
D. SRAM

**答案：A**

**解析：** AP 即无线接入点，是 WLAN 中常见接入设备。其他三项分别属于显示技术、历史图形接口和存储器类型。

### 8. 44.1 kHz、16 bit、双声道的未压缩 PCM 音频，其理论码率约为（ ）。
A. 176.4 kbit/s  
B. 705.6 kbit/s  
C. 1411.2 kbit/s  
D. 2822.4 kbit/s

**答案：C**

**解析：** `44100 × 16 × 2 = 1,411,200 bit/s`，约为 1411.2 kbit/s。注意这是未压缩 PCM 的理论数据率，不能直接当成 MP3/AAC 文件的实际码率。

### 9. 关于 Windows 快捷方式，下列说法正确的是（ ）。
A. 删除快捷方式一定会删除目标原文件  
B. 快捷方式通常保存对目标的引用  
C. 快捷方式扩展名通常为 `.exe`  
D. 快捷方式只能指向文件，不能指向其他目标

**答案：B**

**解析：** 快捷方式通常保存目标引用，删除 `.lnk` 一般不等于删除目标文件。A、C 错误，D 也过于绝对。

### 10. 传统 PC 中专门面向显卡的 AGP 接口后来主要被哪类接口取代？（ ）
A. PCI Express  
B. PS/2  
C. RJ-45  
D. SATA

**答案：A**

**解析：** AGP 是历史图形接口，现代独立显卡主要使用 PCI Express。PS/2 用于传统键鼠，RJ-45 常见于以太网，SATA 面向存储设备。

### 11. 【间隔复习】传统 PC 教材中，北桥芯片典型负责连接的是（ ）。
A. CPU、内存及高速图形相关部件  
B. 只负责打印机驱动  
C. 只负责文件压缩  
D. 只负责域名解析

**答案：A**

**解析：** 传统教材口径中北桥负责 CPU、内存、高速图形等高速部件；现代 CPU 已集成许多原北桥功能，但不能因此否定老题口径。

### 12. 【间隔复习】Linux 中修改文件权限常用的命令是（ ）。
A. `chmod`  
B. `chkdsk`  
C. `taskkill`  
D. `format`

**答案：A**

**解析：** `chmod` 用于修改 Linux/UNIX 文件模式权限。其余三个是 Windows/DOS 语境中的常见命令。

### 13. 【间隔复习】下列编码中，与 Unicode 字符集编码实现密切相关且在 Web 中极常见的是（ ）。
A. UTF-8  
B. AGP  
C. SATA  
D. WAV

**答案：A**

**解析：** UTF-8 是 Unicode 的一种变长编码形式。AGP/SATA 是硬件接口相关概念，WAV 是音频文件格式/容器。

### 14. 【间隔复习】关于 SRAM 与 DRAM，下列说法正确的是（ ）。
A. SRAM 通常用于 Cache，DRAM 常用于主存  
B. DRAM 不需要刷新  
C. SRAM 断电后一定永久保存数据  
D. DDR5 是第五级 Cache

**答案：A**

**解析：** SRAM 速度快、成本高，常用于 Cache；DRAM 密度高、成本较低，常用于主存并需要刷新。SRAM 仍是易失性存储器，DDR5 是 DDR SDRAM 的代际而非 Cache 层级。

### 15. 【间隔复习】Windows 中查看本机 IP 配置常用的命令是（ ）。
A. `ipconfig`  
B. `grep`  
C. `chmod`  
D. `tar`

**答案：A**

**解析：** `ipconfig` 是 Windows 常见网络配置查看命令；后三项属于 Linux/UNIX 常用工具。

## 三、今日速记

| 看到关键词 | 快速联想 |
|---|---|
| HDD | 磁盘片、机械磁头、寻道 |
| SSD | 闪存/半导体、无机械寻道 |
| SATA | 串行 ATA，HDD/SSD 均可使用 |
| NVMe | 高性能非易失性存储协议，常走 PCIe |
| FAT32 | 兼容广、单文件约 4 GiB 上限 |
| NTFS | Windows 常用、权限/安全功能丰富 |
| `.lnk` | 快捷方式 |
| `.dll` | 动态链接库 |
| `.msi` | Windows Installer 安装包 |
| `>` / `>>` | 覆盖输出 / 追加输出 |
| `|` | 管道：前输出 → 后输入 |
| Hub | 传统物理层转发 |
| Switch | 典型二层看 MAC |
| Router | 网络层路由、看 IP/路由表 |
| AP | WLAN 无线接入点 |
| PCM 码率 | 采样频率 × 位数 × 声道数 |
| CRT | 历史阴极射线管显示器 |
| VGA | 常考 15 针模拟视频接口 |
| AGP | 历史显卡接口 → 后由 PCIe 取代 |

🧠 **一分钟口诀：** `SSD 无磁头；NVMe 常走 PCIe；NTFS 重权限；lnk 是快捷；大于号覆盖、双大于追加；交换看 MAC、路由看 IP；PCM 三乘；AGP 老显卡、PCIe 接班。`
