# 国网计算机基础与实用知识晨练｜2026-09-03

> **今日主题：** 内存与接口、Windows 系统命令、Linux 进程/归档、网络介质与设备、文件格式、多媒体及安全基础杂项  
> **今日目标：** 避开昨日 BIOS/存储协议/PCI/编码等正文重点，继续扩大“408 之外、见过即可拿分”的基础杂项覆盖面，并穿插少量昨日内容进行间隔复习。

---

## 一、今日知识点

### 1. RAM、ROM、SRAM、DRAM：先分“易失性”，再看用途

| 类型 | 是否易失 | 典型特点/用途 |
|---|---|---|
| **RAM** | 通常是 | 可随机读写，运行中的程序和数据主要放这里 |
| **ROM** | 通常否 | 断电后仍可保存内容，传统上用于固件等 |
| **SRAM** | 是 | 速度快、成本高、无需周期性刷新，常用于 Cache |
| **DRAM** | 是 | 密度高、成本较低、需要刷新，常用于主存 |

DDR SDRAM 是现代主存常见技术路线，DDR、DDR2、DDR3、DDR4、DDR5 是不同代际，不能把“DDR5”理解成“第五级 Cache”。

> 📌 **国网考点：** Cache 常采用 SRAM，主存常采用 DRAM。
>
> ⚠️ **易错提醒：** SRAM 的“S”不是“静态保存、断电不丢失”；SRAM 仍属于易失性存储器。

🧠 **速记：** `SRAM → 快、贵、Cache；DRAM → 密、便宜、主存、要刷新`。

---

### 2. USB、PS/2、串口、并口：新旧接口辨析

| 接口 | 典型用途/特征 | 考试关键词 |
|---|---|---|
| **USB** | 通用外设接口，可连接键鼠、U 盘、打印机等 | 通用、热插拔、即插即用 |
| **PS/2** | 传统键盘/鼠标接口 | 圆形 Mini-DIN、老式 PC |
| **RS-232 串口** | 数据按串行方式传输 | COM 口、传统调制解调器/工业设备 |
| **并口** | 多位并行传输 | LPT、传统打印机 |

传统 PC 题里常把 `COM` 与串口对应，把 `LPT` 与并口/打印机对应。

> ⚠️ **历史口径：** PS/2、传统串口和并口在普通消费级 PC 上已大幅减少，但国网老题仍可能直接考名称和用途。

---

### 3. VGA、DVI、HDMI、DisplayPort：显示接口别只记“都能接显示器”

| 接口 | 信号/特点（考试层面） | 是否可携带音频 |
|---|---|---|
| **VGA** | 传统模拟视频接口 | 通常不携带 |
| **DVI** | 数字视频为主，部分规格兼容模拟 | 通常以视频为考点 |
| **HDMI** | 数字音视频接口，电视/显示器常见 | **可以** |
| **DisplayPort** | 数字显示接口，PC 显示领域常见 | **可以** |

> 📌 **国网考点：** VGA 最典型的标签是“模拟视频”；HDMI 的典型标签是“数字音视频”。
>
> ⚠️ **易错提醒：** “接口长得不同”不代表都只是视频信号；HDMI/DP 可同时承载音频。

---

### 4. Windows 系统与网络命令：ipconfig、tracert、tasklist、taskkill

| 命令 | 核心作用 |
|---|---|
| `ipconfig` | 查看/管理 Windows TCP/IP 配置 |
| `ipconfig /all` | 显示更详细的网络适配器配置 |
| `tracert` | 跟踪数据包到目标主机的大致路由路径 |
| `netstat` | 查看网络连接、监听端口、统计等 |
| `tasklist` | 列出正在运行的进程 |
| `taskkill` | 结束指定进程 |
| `chkdsk` | 检查磁盘/文件系统 |
| `attrib` | 查看或修改文件属性 |

示例：

```bat
ipconfig /all
tracert example.com
tasklist
taskkill /PID 1234
```

🧠 **速记：** `list 看进程，kill 杀进程；config 看配置，tracert 看路径`。

---

### 5. Linux 进程控制：ps、top、kill、jobs、bg、fg、nohup

| 命令 | 核心用途 |
|---|---|
| `ps` | 查看进程快照 |
| `top` | 动态查看进程和系统资源 |
| `kill` | 向进程发送信号，常用于终止进程 |
| `jobs` | 查看当前 Shell 的作业 |
| `bg` | 让作业在后台继续运行 |
| `fg` | 将后台作业调到前台 |
| `nohup` | 使程序忽略挂断信号，常用于退出终端后继续运行 |

注意：`kill` 的本质是**发送信号**，并不等于“只能强制杀死”。默认通常发送 `SIGTERM`；`kill -9` 对应 `SIGKILL`，应理解为更强制的终止方式。

> 📌 **国网考点：** `ps` 是快照，`top` 是动态监视；`jobs` 管的是当前 Shell 作业，不等于列出系统全部进程。

---

### 6. Linux 归档与压缩：tar、gzip、bzip2、zip 不要混

`tar` 的核心定位是**归档**：把多个文件组织成一个归档文件；gzip/bzip2 等主要负责压缩。

常见组合：

```bash
tar -czf backup.tar.gz dir/
tar -xzf backup.tar.gz
```

考试层面可记：

| 名称 | 重点 |
|---|---|
| `tar` | 归档，可调用压缩算法形成 `.tar.gz` 等 |
| `gzip` | 常产生 `.gz` |
| `bzip2` | 常产生 `.bz2` |
| `zip` / `unzip` | ZIP 格式压缩/解压 |

> ⚠️ **易错提醒：** “tar = 压缩算法”不严谨。tar 首先是归档工具，常与 gzip/bzip2/xz 等结合。

---

### 7. 双绞线、同轴电缆、光纤与 RJ-45

| 介质/接口 | 典型特点 |
|---|---|
| **双绞线** | 以太网局域网常见铜缆介质，利用绞合降低干扰 |
| **同轴电缆** | 早期以太网、有线电视等场景常见 |
| **光纤** | 以光信号传输，带宽高、距离远、抗电磁干扰能力强 |
| **RJ-45** | 双绞线以太网中常见的模块化连接器俗称 |

双绞线常见 UTP（非屏蔽）和 STP（屏蔽）概念。光纤通常分单模和多模；考试若只问“长距离、高带宽、抗电磁干扰”，优先想到光纤。

> ⚠️ **易错提醒：** RJ-45 是连接器/接口俗称，不是一种传输介质；“网线”通常指双绞线，而不是 RJ-45 本身。

---

### 8. 集线器、交换机、路由器、AP、Modem：设备职责速辨

| 设备 | 典型职责 | 快速关键词 |
|---|---|---|
| **Hub 集线器** | 传统共享式连接，收到信号后向其他端口转发 | 共享、广播式转发 |
| **Switch 交换机** | 局域网内依据 MAC 地址转发以太网帧 | MAC、二层 |
| **Router 路由器** | 在不同 IP 网络之间选择路径并转发分组 | IP、三层、路由 |
| **AP 无线接入点** | 为无线终端提供 WLAN 接入并桥接到有线网络 | Wi-Fi 接入 |
| **Modem 调制解调器** | 完成调制与解调，适配特定接入线路 | 调制/解调 |

> 📌 **国网考点：** “交换机看 MAC，路由器看 IP”是基础选择题最常用的辨析口径。
>
> ⚠️ **现代情况：** 家用“无线路由器”通常把路由、交换、AP 等多种功能集成在一台设备里，不能据此否定教材中的功能分类。

---

### 9. JPEG、PNG、GIF、TIFF：图像格式横向比较

| 格式 | 压缩/特点 | 典型用途 |
|---|---|---|
| **JPEG/JPG** | 常用有损压缩，不适合反复编辑保存 | 照片、连续色调图像 |
| **PNG** | 无损压缩，支持透明 | 网页图形、截图、透明背景 |
| **GIF** | 典型为 8 位索引色，支持简单动画 | 小动画、简单图形 |
| **TIFF** | 灵活，可无损或有损，常用于高质量图像/出版扫描 | 专业图像、扫描 |

> ⚠️ **易错提醒：** GIF 的“支持动画”不代表它适合高色彩照片；JPEG 一般不支持透明通道；PNG 的无损压缩不等于文件一定比 JPEG 小。

---

### 10. 病毒、蠕虫、木马、防火墙、补丁、备份：安全基础常识

| 概念 | 核心辨析 |
|---|---|
| **计算机病毒** | 通常依附于程序/文件等宿主并传播、破坏或干扰 |
| **蠕虫** | 强调可利用网络等方式自我复制和传播，通常不依赖传统宿主文件 |
| **木马** | 伪装或隐藏恶意功能，重点在欺骗、控制、窃取等，不以自我复制为必要特征 |
| **防火墙** | 按安全策略控制网络通信，不能替代所有终端安全措施 |
| **安全补丁** | 修复已知漏洞的重要手段 |
| **备份** | 为误删、故障、勒索等场景保留恢复能力 |

> 📌 **国网考点：** 病毒、蠕虫、木马最常考“是否需要宿主、是否强调自我传播”的区别。
>
> ⚠️ **易错提醒：** 安装防火墙不等于系统“绝对安全”；备份也不是防病毒软件，它解决的是恢复能力问题。

---

## 二、国网风格练习题

### 1. 在典型微型计算机中，通常用作 Cache 存储器的是（ ）。

A. DRAM  
B. SRAM  
C. Flash ROM  
D. HDD

**答案：B**

**解析：** SRAM 速度快、无需周期性刷新，常用于 CPU Cache；DRAM 密度较高、成本较低，通常作为主存。SRAM 和 DRAM 都是易失性存储器。

---

### 2. 下列关于 DRAM 的说法正确的是（ ）。

A. 断电后数据仍能永久保存  
B. 不需要刷新  
C. 常作为计算机主存使用  
D. 只能读取不能写入

**答案：C**

**解析：** DRAM 是动态随机存取存储器，需要周期性刷新，属于易失性存储器，现代计算机主存通常基于 DRAM 技术。

---

### 3. 传统 PC 中，LPT 通常对应（ ）。

A. 并行接口  
B. 串行接口  
C. 显示接口  
D. 网络接口

**答案：A**

**解析：** LPT 是传统并行端口的典型称呼，过去常连接打印机；COM 则常与 RS-232 串口对应。

---

### 4. 下列显示接口中，传统 VGA 接口主要传输的是（ ）。

A. 模拟视频信号  
B. 数字音频信号  
C. 光纤网络信号  
D. SATA 存储信号

**答案：A**

**解析：** VGA 是经典模拟视频接口。HDMI 和 DisplayPort 则是现代数字显示接口，并可承载音频。

---

### 5. Windows 中用于列出当前运行进程的命令是（ ）。

A. `tasklist`  
B. `taskkill`  
C. `tracert`  
D. `attrib`

**答案：A**

**解析：** `tasklist` 用于列出进程；`taskkill` 用于终止进程；`tracert` 用于路由路径跟踪；`attrib` 用于文件属性。

---

### 6. Linux 中能够动态显示进程和系统资源使用情况的常用命令是（ ）。

A. `ps`  
B. `top`  
C. `pwd`  
D. `tar`

**答案：B**

**解析：** `top` 提供动态刷新视图；`ps` 通常显示执行时刻的进程快照。两者是常见易混项。

---

### 7. 关于 Linux `kill` 命令，下列说法最准确的是（ ）。

A. 只能删除磁盘文件  
B. 本质上用于向进程发送信号  
C. 只能强制终止进程且无法选择信号  
D. 用于压缩进程数据

**答案：B**

**解析：** `kill` 本质是向进程发送信号。默认常为 SIGTERM；`kill -9` 使用 SIGKILL。把 kill 简单理解为“只能强制杀进程”不够准确。

---

### 8. 下列关于 `tar` 的说法正确的是（ ）。

A. 它的核心作用是文件归档，并可结合压缩工具使用  
B. 它只能处理图片  
C. 它等同于 gzip 压缩算法  
D. 它是 Linux 用户管理命令

**答案：A**

**解析：** tar 首先是归档工具，常与 gzip 等组合形成 `.tar.gz`。因此把 tar 与 gzip 完全等同是典型错误。

---

### 9. 在常见局域网中，主要依据 MAC 地址转发以太网帧的设备是（ ）。

A. 交换机  
B. 路由器  
C. 调制解调器  
D. 打印机

**答案：A**

**解析：** 二层交换机主要依据 MAC 地址进行帧转发；路由器主要依据 IP 信息在不同网络之间转发分组。

---

### 10. 下列传输介质中，具有高带宽、适合较长距离且不易受电磁干扰影响的是（ ）。

A. 光纤  
B. 双绞线  
C. 普通并口线  
D. PS/2 线缆

**答案：A**

**解析：** 光纤使用光信号传输，典型优势是带宽高、传输距离长、抗电磁干扰能力强。

---

### 11. 下列图像格式中，典型支持简单动画的是（ ）。

A. JPEG  
B. GIF  
C. BMP  
D. TXT

**答案：B**

**解析：** GIF 经典特征之一是支持多帧简单动画；同时其典型索引色限制使它不适合高质量连续色调照片。

---

### 12. 下列恶意软件中，通常强调无需依附传统宿主文件即可自我复制并通过网络传播的是（ ）。

A. 蠕虫  
B. 木马  
C. 防火墙  
D. 补丁

**答案：A**

**解析：** 蠕虫的典型特征是自我复制和传播；传统病毒通常强调依附宿主；木马重点是伪装、欺骗或隐藏恶意功能，自我复制不是其必要特征。

---

### 13. 【昨日滚动复习】关于 M.2 与 NVMe，下列说法正确的是（ ）。

A. 所有 M.2 SSD 都一定使用 NVMe  
B. M.2 主要描述一种外形/接口规格，设备可能使用 SATA 或 PCIe/NVMe  
C. NVMe 是机械硬盘盘片标准  
D. SATA 只能用于光盘驱动器

**答案：B**

**解析：** M.2 与 NVMe 不是同义词。M.2 SSD 既可能采用 SATA，也可能采用 PCIe/NVMe；这是昨日内容中的高频易错点。

---

### 14. 【昨日滚动复习】Linux 中 `uniq` 命令主要处理的是（ ）。

A. 相邻重复行  
B. 用户密码  
C. 磁盘分区  
D. 网络路由

**答案：A**

**解析：** `uniq` 主要处理相邻重复行，因此经常与 `sort` 配合使用，例如先排序再去重/统计。

---

### 15. 【昨日滚动复习】Windows 文件系统中，通常支持 ACL 权限和日志机制的是（ ）。

A. FAT12  
B. FAT32  
C. NTFS  
D. ISO 9660

**答案：C**

**解析：** NTFS 支持较完善的权限控制、日志等特性，是现代 Windows 系统盘常见文件系统。FAT32 的典型优势更多在兼容性，而不是权限和日志能力。

---

## 三、今日速记

```text
【内存】
SRAM → 快、贵、无需刷新 → Cache
DRAM → 密度高、需刷新 → 主存
二者都属于易失性 RAM

【老式接口】
COM → 串口 / RS-232
LPT → 并口 / 传统打印机
PS/2 → 传统键盘鼠标
USB → 通用、热插拔、即插即用

【显示接口】
VGA → 模拟视频
DVI → 数字视频为主
HDMI → 数字音视频
DisplayPort → 数字音视频

【Windows 命令】
ipconfig → IP 配置
tracert  → 路由路径
tasklist → 列进程
taskkill → 结束进程
chkdsk   → 检查磁盘/文件系统
attrib   → 文件属性

【Linux 进程】
ps    → 进程快照
top   → 动态监视
kill  → 发送信号
jobs  → 当前 Shell 作业
bg/fg → 后台/前台
nohup → 退出终端后常用于继续运行

【归档压缩】
tar   → 归档
gzip  → .gz
bzip2 → .bz2
zip/unzip → ZIP

【网络】
双绞线 → 常见局域网铜缆
光纤 → 高带宽、长距离、抗电磁干扰
RJ-45 → 常见双绞线以太网连接器俗称
交换机 → MAC / 二层
路由器 → IP / 三层
AP → 无线接入
Modem → 调制/解调

【图像】
JPEG → 有损、照片
PNG  → 无损、透明
GIF  → 索引色、简单动画
TIFF → 灵活、高质量图像/扫描常见

【安全】
病毒 → 传统上依附宿主
蠕虫 → 自我复制、网络传播
木马 → 伪装/隐藏恶意功能
防火墙 → 控制网络通信
补丁 → 修漏洞
备份 → 保恢复能力

【昨日复习】
M.2 ≠ NVMe
uniq → 相邻重复行
NTFS → ACL + 日志
```

> **今日一句话总结：** 把“接口—用途、命令—作用、设备—职责、格式—特点”建立成快速对应关系，比深挖单一主题更符合国网基础杂项题的拿分方式。
