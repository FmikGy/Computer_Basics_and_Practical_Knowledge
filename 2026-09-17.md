# 国网计算机基础与实用知识晨练｜2026-09-17

> **今日主题：** BIOS/UEFI 启动链路中的 POST 与引导、Windows 注册表、Linux 用户与组、显示接口、办公文件格式、打印机分类、病毒/蠕虫/木马、光盘与 U 盘历史常识  
> **今日目标：** 避开近两日已重点覆盖的 RAM/ROM、Linux 目录树、网络设备、压缩格式、CPU 扩展指令、PCIe、字符编码等内容，补齐 Windows 注册表、Linux 账户管理、显示接口、办公文件格式和基础安全等近期相对欠缺类别，并用习题间隔复习前几日知识。

## 一、今日知识点

### 1. POST、BIOS、UEFI 与启动：不要把“固件”和“自检过程”混为一谈

- **BIOS**：传统 PC 固件体系，负责硬件初始化并把控制权交给后续引导程序。
- **UEFI**：现代 PC 普遍采用的固件接口体系，功能和扩展能力通常比传统 BIOS 更强。
- **POST（Power-On Self-Test）**：开机后的加电自检过程，用于检查内存、键盘等关键硬件是否具备基本工作条件。
- **启动设备**：固件根据启动顺序寻找可启动介质，再进入操作系统引导阶段。

| 概念 | 关键词 | 易错点 |
|---|---|---|
| BIOS/UEFI | 固件、初始化、引导 | 不是操作系统 |
| POST | 加电自检 | 是过程，不是存储芯片 |
| CMOS（传统教材） | 保存部分固件配置参数 | 不等于 BIOS 程序本身 |

📌 **国网考点：** 老题常问“开机自检”对应 POST；BIOS 与 CMOS 要区分“程序/固件”和“配置数据”的传统口径。

⚠️ **现代情况：** 现代机器多使用 UEFI，但考试遇到传统 PC 教材题时，仍应识别 BIOS、CMOS、POST 的经典关系。

### 2. Windows 注册表：集中保存系统和应用配置的重要数据库

Windows 注册表用于保存大量系统、硬件、用户和应用程序配置。常见根键名称包括：

| 根键 | 基础定位 |
|---|---|
| `HKEY_LOCAL_MACHINE` | 本机级硬件、系统和软件配置 |
| `HKEY_CURRENT_USER` | 当前登录用户的配置 |
| `HKEY_CLASSES_ROOT` | 文件关联、COM 等类别信息的常见视图 |
| `HKEY_USERS` | 用户配置相关信息 |
| `HKEY_CURRENT_CONFIG` | 当前硬件配置相关视图 |

常用注册表编辑器命令为：

```text
regedit
```

`.reg` 文件常用于注册表信息的导入、导出或合并。

📌 **国网考点：** 注册表不是普通“文件夹”；`regedit` 是常见注册表编辑工具，`.reg` 与注册表数据关联。

⚠️ **易错提醒：** 注册表与 `.ini` 都可承载配置，但不能说 Windows 的所有配置都只存在注册表中。

### 3. Linux 用户、组与 root：UID/GID 是识别身份的重要数字标识

Linux 是典型的多用户系统。用户通常有 UID，组通常有 GID；`root` 是传统超级用户账户。

常见命令：

```bash
id alice
useradd alice
passwd alice
usermod -aG group1 alice
userdel alice
su - alice
sudo command
```

| 命令 | 基础用途 |
|---|---|
| `id` | 查看用户 UID、GID、所属组等 |
| `useradd` | 创建用户 |
| `userdel` | 删除用户 |
| `usermod` | 修改用户属性 |
| `passwd` | 设置/修改密码 |
| `su` | 切换用户身份 |
| `sudo` | 按授权以其他身份（常见为 root）执行命令 |

⚠️ **易错提醒：** `su` 与 `sudo` 不是同义词；前者强调切换身份，后者强调按授权执行命令。

### 4. VGA、DVI、HDMI、DisplayPort：显示接口先认“模拟还是数字”

| 接口 | 基础特点 | 考试关键词 |
|---|---|---|
| VGA | 传统模拟视频接口 | 模拟、D-Sub、老显示器 |
| DVI | 数字显示接口家族，部分类型兼容模拟信号 | 过渡时代显示接口 |
| HDMI | 数字音视频接口 | 音频+视频、电视/显示器常见 |
| DisplayPort | 数字显示接口 | PC 显示、高带宽、音视频 |

🧠 **速记：** `VGA 经典模拟；HDMI/DP 现代数字音视频。`

📌 **国网考点：** “哪种是模拟显示接口”常选 VGA；不要把 RJ-45 当显示接口，它通常与以太网双绞线连接相关。

### 5. DOCX、XLSX、PPTX、PDF：扩展名能提示格式，但不能决定真实格式

| 扩展名 | 常见用途 |
|---|---|
| `.doc` / `.docx` | 文字处理文档 |
| `.xls` / `.xlsx` | 电子表格 |
| `.ppt` / `.pptx` | 演示文稿 |
| `.pdf` | 固定版式电子文档 |
| `.txt` | 纯文本常见扩展名 |

Office 2007 以后常见的 `docx/xlsx/pptx` 属于基于 Office Open XML 的格式家族，内部通常采用 ZIP 容器组织 XML 等资源，因此用合适工具可以观察其内部结构。

⚠️ **易错提醒：** 把 `abc.txt` 改名为 `abc.pdf` 并不会真正把文本转换成 PDF；扩展名只是名称的一部分，真实格式由文件内容和结构决定。

### 6. 针式、喷墨、激光打印机：击打式与非击打式是老题高频分类

| 类型 | 基础原理/特点 | 常见考点 |
|---|---|---|
| 针式打印机 | 打印针击打色带和纸张 | 击打式、可打印多联票据 |
| 喷墨打印机 | 将微小墨滴喷到介质上 | 非击打式、彩色常见 |
| 激光打印机 | 结合激光成像、静电和碳粉转印等过程 | 非击打式、速度快、办公常见 |

**传统教材口径：** 针式打印机属于击打式打印机；喷墨和激光属于非击打式。

📌 **国网考点：** “需要打印多联复写票据”是针式打印机的典型识别场景。

### 7. 病毒、蠕虫、木马：传播方式和伪装方式是关键辨析点

| 类型 | 基础定位 |
|---|---|
| 计算机病毒 | 常依附于文件、程序等宿主，通过感染和执行传播 |
| 蠕虫 | 典型特点是可利用网络等机制主动、自我复制传播 |
| 木马 | 常伪装成正常程序或诱导用户运行，以实现未授权功能 |

防护基础包括及时打补丁、使用可信安全软件、谨慎处理未知附件和程序、做好重要数据备份、遵循最小权限原则等。

⚠️ **易错提醒：** 木马的核心识别词不是“自我复制”；蠕虫才更强调无需依附传统宿主程序即可自我传播。

### 8. 光盘、U 盘与软盘：存储介质的“时代标签”要认识

| 介质 | 基础类型 | 特点 |
|---|---|---|
| 软盘 | 磁存储 | 容量小、历史 PC 常见，现代基本淘汰 |
| HDD | 磁存储 | 机械结构、容量大 |
| CD/DVD 等光盘 | 光存储 | 激光读写，存在只读/一次写入/可擦写等类型 |
| U 盘 | 半导体闪存 | USB 接口常见、便携、无机械盘片 |
| SSD | 半导体存储 | 现代常见高速存储，无机械磁头盘片 |

📌 **国网考点：** “软盘、硬盘属于磁介质；光盘属于光存储；U 盘/SSD 常基于闪存”是典型基础分类题。

⚠️ **考试口径与现代情况：** 软盘虽已淘汰，但老教材仍可能考其属于磁存储介质，不应因现实中少见而否定传统答案。

## 二、国网风格练习题

### 1. 微型计算机开机时用于检查关键硬件基本状态的过程通常称为（ ）。
A. POST  
B. FTP  
C. DHCP  
D. SMTP

**答案：A**

**解析：** POST 是 Power-On Self-Test，即加电自检。FTP、DHCP、SMTP 均属于网络相关协议或服务概念，与开机硬件自检无关。

### 2. 关于传统教材中的 BIOS 与 CMOS，下列说法较准确的是（ ）。
A. CMOS 就是 Windows 注册表  
B. BIOS 是固件体系，CMOS 常用于保存部分配置参数  
C. BIOS 是一种打印机接口  
D. CMOS 是 Linux 文件系统

**答案：B**

**解析：** 传统 PC 口径下，BIOS 是固件程序体系，CMOS 常与保存 BIOS 设置参数联系。二者不能简单视为同一概念。

### 3. Windows 中用于编辑注册表的常见工具命令是（ ）。
A. regedit  
B. grep  
C. chmod  
D. tar

**答案：A**

**解析：** `regedit` 是 Windows 常见注册表编辑器。`grep`、`chmod`、`tar` 是 UNIX/Linux 环境中常见命令。

### 4. Linux 中用于查看某用户 UID、GID 和所属组信息的常用命令是（ ）。
A. pwd  
B. id  
C. mkdir  
D. df

**答案：B**

**解析：** `id` 显示用户和组身份信息；`pwd` 显示当前目录，`mkdir` 创建目录，`df` 查看文件系统空间使用情况。

### 5. 下列关于 `su` 与 `sudo` 的说法正确的是（ ）。
A. 二者完全相同  
B. su 常用于切换用户，sudo 常用于按授权执行命令  
C. sudo 只能查看目录  
D. su 是压缩命令

**答案：B**

**解析：** `su` 的典型用途是切换用户身份；`sudo` 根据配置的授权以其他身份执行命令。两者使用场景有交集但并非同义。

### 6. 下列接口中，传统上主要传输模拟视频信号的是（ ）。
A. VGA  
B. HDMI  
C. DisplayPort  
D. RJ-45

**答案：A**

**解析：** VGA 是经典模拟视频接口。HDMI 和 DisplayPort 是数字音视频接口；RJ-45 常用于以太网连接。

### 7. 下列扩展名与文件类型对应正确的是（ ）。
A. `.xlsx`——电子表格  
B. `.pptx`——Linux 设备文件  
C. `.pdf`——可执行程序专用格式  
D. `.docx`——磁盘分区表

**答案：A**

**解析：** `.xlsx` 是现代 Microsoft Excel 工作簿常见扩展名；`.pptx` 对应演示文稿，`.docx` 对应文字处理文档，PDF 是固定版式文档格式。

### 8. 需要打印多联票据时，传统办公场景较适合使用（ ）。
A. 针式打印机  
B. 只读光驱  
C. 扫描仪  
D. 调制解调器

**答案：A**

**解析：** 针式打印机属于击打式打印机，机械击打特性使其适合多联复写票据。其余选项均不是这种打印设备。

### 9. 下列恶意程序中，典型特点是能够通过网络等方式主动自我复制传播的是（ ）。
A. 蠕虫  
B. 普通快捷方式  
C. 文本编辑器  
D. 打印驱动

**答案：A**

**解析：** 蠕虫的典型特征是自我复制和传播。病毒通常强调感染宿主，木马通常强调伪装、诱导执行和未授权功能。

### 10. 下列存储介质中属于光存储的是（ ）。
A. CD-ROM  
B. 软盘  
C. HDD  
D. SRAM

**答案：A**

**解析：** CD-ROM 使用光学方式读取。软盘和 HDD 属于磁存储，SRAM 属于半导体存储器。

### 11. 【滚动复习】关于 SRAM 与 DRAM，下列说法正确的是（ ）。
A. SRAM 必须周期性刷新  
B. DRAM 常用于构成大容量主存  
C. DRAM 断电后仍永久保存数据  
D. SRAM 属于 ROM

**答案：B**

**解析：** DRAM 密度高、成本相对低，常作为主存基础；SRAM 通常无需周期性刷新且速度快，常用于 Cache。两者通常都是易失性 RAM。

### 12. 【滚动复习】PCIe 相比传统 PCI 的典型体系特征是（ ）。
A. 高速串行、点对点  
B. 只能连接打印机  
C. 必须使用模拟信号  
D. 属于文件系统

**答案：A**

**解析：** PCIe 采用高速串行、点对点链路，并以 Lane 组合扩展链路宽度；传统 PCI 则是并行共享总线的典型代表。

### 13. 【滚动复习】Linux 中 `/etc` 目录通常用于保存（ ）。
A. 系统级配置文件  
B. 用户个人视频的固定专用目录  
C. CPU Cache 数据  
D. Windows 注册表

**答案：A**

**解析：** `/etc` 是 Linux/UNIX 系统级配置文件的常见位置。用户主目录通常位于 `/home` 下。

### 14. 【滚动复习】局域网交换机在基础考试口径中主要根据（ ）进行二层帧转发。
A. MAC 地址  
B. 文件扩展名  
C. CPU 主频  
D. Unicode 码点

**答案：A**

**解析：** 二层交换机学习并利用 MAC 地址表转发以太网帧；路由器则主要依据 IP 网络信息进行三层转发。

### 15. 【滚动复习】关于 `tar` 和 `gzip`，下列说法较准确的是（ ）。
A. tar 主要负责归档，gzip 主要负责压缩  
B. tar 是 Windows 注册表编辑器  
C. gzip 是显示接口  
D. 二者都只能删除文件

**答案：A**

**解析：** `tar` 的核心职责是归档多个文件，gzip 是压缩工具，因此 `.tar.gz` 常体现“归档 + 压缩”的组合。

## 三、今日速记

| 看到关键词 | 快速联想 |
|---|---|
| 开机加电自检 | POST |
| 传统固件 | BIOS；现代主流固件接口体系 → UEFI |
| 传统配置参数保存 | CMOS（考试口径） |
| Windows 注册表编辑 | `regedit` |
| 当前用户注册表配置 | `HKEY_CURRENT_USER` |
| Linux 身份信息 | `id`、UID/GID |
| 切换用户 | `su` |
| 授权执行 | `sudo` |
| 经典模拟视频 | VGA |
| 数字音视频 | HDMI、DisplayPort |
| 文字/表格/演示 | DOCX / XLSX / PPTX |
| 固定版式文档 | PDF |
| 多联票据 | 针式打印机、击打式 |
| 自我复制网络传播 | 蠕虫 |
| 伪装诱导执行 | 木马 |
| 感染宿主 | 病毒（典型口径） |
| 磁存储 | 软盘、HDD |
| 光存储 | CD/DVD |
| 闪存 | U 盘、常见 SSD |

🧠 **一分钟口诀：** `自检 POST，固件 BIOS/UEFI；注册表 regedit；Linux 身份看 id，su 切人 sudo 授权；VGA 模拟，HDMI/DP 数字；针式打多联；蠕虫自己传播；软硬盘磁、光盘光、U 盘闪存。`