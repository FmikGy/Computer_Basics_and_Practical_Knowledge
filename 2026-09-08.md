# 国网计算机基础与实用知识晨练｜2026-09-08

> **今日主题：** 外设与打印机、容量单位、DOS 文件命令、Linux 压缩与网络命令、网络传输介质、视频编码与容器、软件分类、病毒/木马/蠕虫辨析  
> **今日目标：** 近期已连续触达存储接口、主板芯片组、Linux 权限/管道、Windows 文件系统、IEEE 802、音频与图像等内容，今天主动切换到外设、DOS 实用命令、Linux 压缩/网络工具、传输介质、视频文件、软件分类和基础安全，补齐“看一遍即可拿分”的杂项。

## 一、今日知识点

### 1. 输入/输出外设：打印机、扫描仪、显示器怎么分

计算机外设题常直接考“输入还是输出”。

| 设备 | 基础分类 | 关键作用 |
|---|---|---|
| 键盘、鼠标 | 输入设备 | 向计算机输入字符、位置或控制信息 |
| 扫描仪 | 输入设备 | 把纸面图像/文字转换为数字信息 |
| 显示器 | 输出设备 | 以可视方式呈现计算机处理结果 |
| 打印机 | 输出设备 | 把数字信息输出到纸张等介质 |
| 触摸屏 | 兼具输入/输出 | 显示内容，同时接收触控输入 |

打印机还常按工作原理辨析：

| 类型 | 典型特点 |
|---|---|
| 针式打印机 | 击打式；可用于多联票据等传统场景 |
| 喷墨打印机 | 喷射墨滴成像，常见于家庭/办公彩色输出 |
| 激光打印机 | 利用激光成像、静电与碳粉等完成打印，速度通常较快 |

📌 **国网考点：** 扫描仪是输入设备，打印机是输出设备；触摸屏不能简单归为“纯输出”。

⚠️ **易错提醒：** “显示器”和“显卡”不是一回事：显示器是外部输出设备，显卡/图形处理硬件负责图形处理与显示输出。

### 2. bit、Byte、KB、MB、GB、TB：单位换算先看口径

- `bit（b）`：比特，二进制信息的基本单位。
- `Byte（B）`：字节，通常 `1 B = 8 bit`。
- 传统计算机考试常采用二进制换算：`1 KB = 1024 B`、`1 MB = 1024 KB`、`1 GB = 1024 MB`。
- 严格的 IEC 表述中，1024 倍单位可写作 KiB、MiB、GiB；而存储厂商标称容量常采用十进制倍数。

📌 **考试口径：** 若题目没有另作说明，老教材/基础计算题通常按 1024 进位作答。

🧠 **速记：** `小 b 是 bit，大 B 是 Byte；1 B = 8 b。`

### 3. DOS/Windows 文件操作命令：copy、xcopy、move、del、ren、type、tree

| 命令 | 基础用途 |
|---|---|
| `copy` | 复制一个或多个文件 |
| `xcopy` | 扩展复制，可用于目录树等场景（传统命令） |
| `move` | 移动文件，也可用于目录/文件移动场景 |
| `del` / `erase` | 删除文件 |
| `ren` / `rename` | 重命名文件或目录 |
| `type` | 显示文本文件内容 |
| `tree` | 以树形方式显示目录结构 |
| `cls` | 清屏 |

```bat
copy a.txt D:\backup\
ren old.txt new.txt
type new.txt
tree C:\work
```

⚠️ **易错提醒：** `del` 主要删除文件；删除目录不要机械地选 `del`。`type` 是查看文本内容，不是查看文件类型。

### 4. Linux 压缩与归档：tar、gzip、bzip2、zip/unzip

`tar` 的核心定位是**归档**：把多个文件/目录组织成一个归档文件；压缩可与 gzip、bzip2 等结合。

```bash
tar -czf backup.tar.gz project/
tar -xzf backup.tar.gz
gzip notes.txt
zip -r docs.zip docs/
unzip docs.zip
```

| 工具 | 基础定位 |
|---|---|
| `tar` | 归档，可配合压缩算法 |
| `gzip` | 常见压缩工具，典型扩展名 `.gz` |
| `bzip2` | 压缩工具，典型扩展名 `.bz2` |
| `zip` / `unzip` | 创建/解开 ZIP 归档 |

📌 **国网考点：** `.tar` 本身不必然表示已压缩；`.tar.gz` 常表示先 tar 归档再 gzip 压缩。

### 5. Linux 网络命令：ping、ip、ifconfig、ss、netstat、hostname

| 命令 | 常见用途 |
|---|---|
| `ping` | 测试 IP 层连通性及往返响应情况 |
| `ip` | 查看/配置地址、链路、路由等，现代 Linux 常用 |
| `ifconfig` | 传统网络接口查看/配置工具 |
| `ss` | 查看套接字、连接等信息，现代系统常用 |
| `netstat` | 传统网络连接、路由等查看工具 |
| `hostname` | 查看或处理主机名相关信息 |

```bash
ip addr
ip route
ss -tuln
hostname
```

⚠️ **现代情况：** `ip` 常替代部分 `ifconfig` 用法，`ss` 常替代部分 `netstat` 用法；但国网旧题仍可能直接考 `ifconfig`、`netstat`，不能因其较旧而判错。

### 6. 双绞线、同轴电缆、光纤：网络介质辨析

| 介质 | 信号/结构特点 | 考试关键词 |
|---|---|---|
| 双绞线 | 铜导线成对绞合 | 以太网布线常见，RJ-45 常与之搭配 |
| 同轴电缆 | 中心导体与同轴屏蔽结构 | 早期以太网、有线电视等历史/应用场景 |
| 光纤 | 以光信号传输 | 带宽高、传输距离长、抗电磁干扰能力强 |

双绞线常见 UTP（非屏蔽）与 STP（屏蔽）等类别。

📌 **国网考点：** 光纤不使用电信号在铜线中传输，因此抗电磁干扰是典型优势；RJ-45 是常见以太网双绞线连接器，不是“光纤接口”。

### 7. 视频：编码、容器、分辨率、帧率不要混

视频文件常把多个概念揉在一起考：

| 概念 | 含义 |
|---|---|
| 分辨率 | 一帧图像的像素规模，如 1920×1080 |
| 帧率 | 每秒显示/记录的帧数，常用 fps 表示 |
| 编码/压缩 | 对视频数据进行表示和压缩的方法 |
| 容器 | 封装视频流、音频流、字幕、元数据等的文件结构 |

MP4、MKV、AVI 常作为**容器格式**理解；MPEG 家族相关名称既可能涉及标准体系，也常出现在媒体格式语境中，考试要看题干。

⚠️ **易错提醒：** `.mp4` 不能唯一决定内部使用哪一种视频编码；“容器”和“编码器/编码格式”不是同义词。

🧠 **速记：** `容器像盒子，编码像盒子里音视频数据的压缩表示方法。`

### 8. 系统软件、应用软件、工具软件与中间件

| 类别 | 基础定位 | 常见例子/理解 |
|---|---|---|
| 系统软件 | 管理计算机资源、支撑系统运行 | 操作系统、部分系统程序 |
| 应用软件 | 面向用户具体业务或任务 | 办公、图形处理、浏览器等 |
| 工具软件 | 辅助维护、管理、诊断等 | 压缩、备份、部分磁盘工具 |
| 中间件 | 位于基础平台与应用之间提供通用服务 | 通信、消息、事务等通用支撑 |

📌 **国网考点：** 操作系统是典型系统软件；Word 类办公程序属于应用软件。分类边界在真实软件生态中可能交叉，基础题按典型功能判断。

### 9. 病毒、蠕虫、木马：基础安全只抓传播与伪装

| 类型 | 选择题层面的典型特征 |
|---|---|
| 计算机病毒 | 通常依附于文件、程序等宿主，通过感染机制传播 |
| 蠕虫 | 可利用网络等机制进行自我复制传播，通常不强调依附宿主文件 |
| 木马 | 常伪装成正常/有用程序，诱导运行后执行未授权或恶意功能 |

防火墙主要用于依据安全策略控制网络通信；补丁用于修复软件缺陷/漏洞；备份用于在数据损坏、误删或攻击后提供恢复基础。

⚠️ **易错提醒：** 防火墙不是“安装后绝对不会中病毒”；备份也不是杀毒工具。安全防护通常需要多层措施配合。

## 二、国网风格练习题

### 1. 下列设备中，典型属于输入设备的是（ ）。

A. 激光打印机  
B. 扫描仪  
C. 显示器  
D. 音箱

**答案：B**

**解析：** 扫描仪把纸面图像或文字转换为计算机可处理的数字信息，属于输入设备。打印机、显示器、音箱主要属于输出设备。

### 2. 在传统计算机基础考试口径下，1 Byte 等于（ ）。

A. 2 bit  
B. 4 bit  
C. 8 bit  
D. 16 bit

**答案：C**

**解析：** 1 Byte（字节）= 8 bit（比特）。注意大小写：b 常表示 bit，B 常表示 Byte。

### 3. 在 DOS/Windows 命令中，用于显示文本文件内容的是（ ）。

A. type  
B. tree  
C. ren  
D. cls

**答案：A**

**解析：** `type` 用于显示文本文件内容；`tree` 显示目录树；`ren` 重命名；`cls` 清屏。

### 4. 关于 Linux `tar` 命令，下列说法正确的是（ ）。

A. tar 只能用于网络测试  
B. `.tar` 文件一定已经使用 gzip 压缩  
C. tar 的核心用途之一是将多个文件组织为归档  
D. tar 只能解压，不能创建归档

**答案：C**

**解析：** `tar` 的核心是归档，可结合 gzip、bzip2 等进行压缩。`.tar` 本身并不意味着 gzip 压缩，因此 B 错。

### 5. 在现代 Linux 中，常用于查看网络接口地址信息的命令是（ ）。

A. ip addr  
B. chmod  
C. tar  
D. passwd

**答案：A**

**解析：** `ip addr` 用于查看地址相关信息。`chmod` 修改权限，`tar` 归档，`passwd` 处理用户密码。

### 6. 与铜缆相比，光纤的典型优势是（ ）。

A. 必须使用 RJ-45 连接器  
B. 抗电磁干扰能力强  
C. 只能传输模拟信号  
D. 一定比所有铜缆便宜

**答案：B**

**解析：** 光纤以光信号传输，抗电磁干扰是典型优势。RJ-45 常与双绞线以太网搭配；其他选项均不是光纤的一般特征。

### 7. 关于 MP4，下列说法最准确的是（ ）。

A. MP4 只能封装音频  
B. MP4 是一种常见媒体容器，内部编码并非只能有一种  
C. MP4 就等同于帧率  
D. MP4 就等同于屏幕分辨率

**答案：B**

**解析：** MP4 是常见容器格式，可封装音视频等数据。容器、编码、帧率和分辨率是不同维度。

### 8. 下列软件中，按基础教材分类最典型属于系统软件的是（ ）。

A. 操作系统  
B. 电子表格软件  
C. 图片编辑软件  
D. 演示文稿软件

**答案：A**

**解析：** 操作系统负责管理软硬件资源并提供运行环境，是典型系统软件；其余均属于典型应用软件。

### 9. 下列关于蠕虫的描述，最符合基础安全常识的是（ ）。

A. 必须依附于 Word 文档才能存在  
B. 可利用网络等机制自我复制传播  
C. 本质上是一种磁盘文件系统  
D. 是一种打印机驱动

**答案：B**

**解析：** 蠕虫的典型识别点是可自我复制并传播，尤其常与网络传播联系。它不要求像传统病毒那样强调依附宿主文件。

### 10. 下列关于 SSD 与 HDD 的说法正确的是（ ）。

A. SSD 必须使用机械磁头寻道  
B. HDD 的数据只能存入 RAM  
C. SSD 通常没有 HDD 那样的机械寻道部件  
D. SSD 与 NVMe 完全同义

**答案：C**

**解析：** 这是间隔复习。SSD 典型采用闪存等半导体介质，没有 HDD 的盘片和机械磁头。NVMe 是面向非易失性存储的协议，SSD 是设备/存储形态，二者不能等同。

### 11. Linux Shell 中，把命令输出追加到文件末尾通常使用（ ）。

A. `>`  
B. `>>`  
C. `<`  
D. `|`

**答案：B**

**解析：** 这是间隔复习。`>>` 表示追加输出；`>` 通常覆盖目标文件；`|` 是管道；`<` 用于输入重定向。

### 12. Windows 中，`.lnk` 文件通常表示（ ）。

A. 动态链接库  
B. 快捷方式  
C. 驱动程序  
D. 安装数据库

**答案：B**

**解析：** 这是间隔复习。`.lnk` 是 Windows 常见快捷方式文件；`.dll` 常表示动态链接库，`.sys` 常见于系统/驱动相关文件。

### 13. 传统教材中，北桥芯片典型负责连接的高速部件是（ ）。

A. CPU 与内存等  
B. 打印机墨盒与纸张  
C. 键盘按键与鼠标滚轮  
D. 文件与回收站

**答案：A**

**解析：** 这是历史知识间隔复习。传统 PC 架构中，北桥常与 CPU、内存及高速图形接口等联系；现代处理器已集成许多原北桥功能，但老题口径仍需掌握。

### 14. Windows 文件系统中，相比 FAT32，NTFS 的典型特点是（ ）。

A. 不支持任何权限机制  
B. 支持更丰富的权限和安全特性  
C. 只能用于软盘  
D. 单文件大小上限一定小于 FAT32

**答案：B**

**解析：** 这是间隔复习。NTFS 支持 ACL 等更丰富的权限、安全与可靠性能力；FAT32 的单文件大小限制反而更明显。

### 15. 未压缩 PCM 音频的数据率主要由采样频率、采样位数以及（ ）共同决定。

A. 声道数  
B. 文件扩展名长度  
C. 显示器刷新率  
D. CPU 型号名称

**答案：A**

**解析：** 这是间隔复习。未压缩 PCM 的理论码率可按“采样频率 × 采样位数 × 声道数”估算。

## 三、今日速记

| 看到关键词 | 快速联想 |
|---|---|
| 扫描仪 | 输入设备 |
| 打印机/显示器 | 输出设备 |
| 触摸屏 | 兼具输入与输出属性 |
| 1 Byte | 8 bit |
| 传统考试 KB→MB→GB | 常按 1024 进位 |
| `copy` | 复制文件 |
| `ren` | 重命名 |
| `type` | 显示文本文件内容 |
| `tree` | 显示目录树 |
| `tar` | 归档；不等于必然压缩 |
| `.tar.gz` | tar 归档 + gzip 压缩的常见组合 |
| `ip addr` | 网络地址信息 |
| `ss` | 套接字/连接信息，现代常用 |
| `ifconfig` / `netstat` | 传统 Linux 网络工具，旧题仍要认识 |
| 双绞线 | 铜缆；RJ-45 常见搭配 |
| 光纤 | 高带宽、远距离、抗电磁干扰 |
| MP4/MKV/AVI | 常见媒体容器概念 |
| 分辨率 | 每帧像素规模 |
| 帧率 | 每秒帧数 fps |
| 操作系统 | 典型系统软件 |
| 病毒 | 常强调感染/依附宿主 |
| 蠕虫 | 强调自我复制传播 |
| 木马 | 常强调伪装、诱导运行、恶意功能 |

🧠 **最后一分钟口诀：**  
`扫入打出，触屏两头；小 b 比特大 B 字节；tar 先记归档；ip 看网，ss 看连接；双绞配 RJ-45，光纤抗干扰；MP4 是盒子不是唯一编码；病毒会感染，蠕虫会自传，木马重伪装。`
