# 国网计算机基础与实用知识晨练｜2026-09-09

> **今日主题：** PCI/PCIe/AGP/ISA 历史总线、Windows 任务与网络诊断、Linux 文件类型与链接、字符编码再辨析、图像容量计算、浏览器缓存/剪贴板/回收站、调制解调器与 AP、备份基础  
> **今日目标：** 最近一周已覆盖存储接口、外设、Linux 压缩/权限/管道、Windows 文件系统与 DOS 命令、IEEE 802、音视频格式等。今天重点补齐“历史扩展总线、Linux 文件类型、桌面系统实用常识和图像容量计算”，并用少量编码、网络设备与备份知识做横向扩展。

## 一、今日知识点

### 1. ISA、PCI、AGP、PCIe：老题里的扩展总线演进

| 名称 | 考试层面定位 | 关键辨析 |
|---|---|---|
| ISA | 早期 PC 扩展总线 | 历史技术，速度较低 |
| PCI | 传统通用局部总线 | 并行、共享总线思想 |
| AGP | 主要面向显卡的专用高速接口 | 老式显卡题高频 |
| PCIe | 现代高速扩展互连 | 串行、点对点、多 Lane |

PCIe 常见 `x1/x4/x8/x16` 表示 Lane 数量。现代独立显卡常使用 PCIe x16 形态的插槽；NVMe SSD 也常通过 PCIe 通信。

📌 **国网考点：** `PCI ≠ PCIe`。PCI 典型是并行共享总线，PCIe 采用高速串行点对点链路。

⚠️ **考试口径 vs 现代情况：** AGP、ISA 已基本退出现代 PC，但老教材仍可能直接考其用途，不能因“已淘汰”而否定传统答案。

### 2. Windows 任务管理与网络诊断：tasklist、taskkill、ipconfig、tracert、netstat

| 命令 | 主要用途 |
|---|---|
| `tasklist` | 列出正在运行的进程 |
| `taskkill` | 结束指定进程 |
| `ipconfig` | 查看 Windows TCP/IP 配置 |
| `tracert` | 跟踪数据包到目标主机的大致路径 |
| `netstat` | 查看网络连接、监听端口等信息 |

```bat
tasklist
taskkill /PID 1234
ipconfig /all
tracert example.com
netstat -ano
```

📌 **国网考点：** `ping` 偏连通性测试，`tracert` 偏路径追踪，`ipconfig` 偏本机 IP 配置查看。

### 3. Linux 文件类型：普通文件、目录、设备、链接、管道

Linux 常说“一切皆文件”，是强调许多资源通过统一的文件接口抽象和访问，并不是说所有对象物理上都是磁盘普通文件。

`ls -l` 首字符可帮助识别类型：

| 首字符 | 常见类型 |
|---|---|
| `-` | 普通文件 |
| `d` | 目录 |
| `l` | 符号链接 |
| `c` | 字符设备 |
| `b` | 块设备 |
| `p` | 命名管道 FIFO |

`/dev` 下常见设备文件；普通文本、程序等通常属于普通文件。

🧠 **速记：** `- 普通，d 目录，l 链接，c 字符设备，b 块设备。`

### 4. 硬链接与符号链接：考试只抓核心区别

| 项目 | 硬链接 | 符号链接（软链接） |
|---|---|---|
| 本质 | 目录项指向同一 inode/文件对象 | 独立链接文件，保存目标路径 |
| 原文件名删除后 | 只要仍有硬链接，数据通常仍可访问 | 可能成为悬空链接 |
| 跨文件系统 | 通常不可以 | 可以 |
| 目录 | 通常受限制 | 可以链接目录 |

```bash
ln source.txt hardlink.txt
ln -s source.txt softlink.txt
```

⚠️ **易错提醒：** 符号链接不是“把原文件复制一份”；删除软链接本身通常不会删除目标文件。

### 5. ASCII、Unicode、UTF-8、GB2312、GBK：字符集与编码不要混

- **ASCII**：早期英文字符编码体系，标准 ASCII 使用 7 位表示 128 个字符。
- **Unicode**：为字符分配统一码点的字符集/编码标准体系。
- **UTF-8**：Unicode 的一种可变长度编码方式，与 ASCII 的 0～127 范围兼容。
- **GB2312**：较早的简体中文字符集/编码标准。
- **GBK**：在 GB2312 基础上扩展，覆盖更多汉字和字符。

📌 **国网考点：** Unicode 不能简单等同于 UTF-8；UTF-8 是 Unicode 的一种编码形式。

⚠️ **易错提醒：** 文件扩展名 `.txt` 不能告诉你文本一定使用 UTF-8，它也可能使用 GBK、UTF-16 等编码。

### 6. 位图图像容量：分辨率 × 色深是核心

未压缩位图的理论数据量可近似按：

```text
图像容量（bit） = 水平像素 × 垂直像素 × 色深（bit/像素）
图像容量（Byte） = 上式 ÷ 8
```

例如一幅 `800 × 600`、24 bit 真彩色的未压缩图像：

```text
800 × 600 × 24 ÷ 8 = 1,440,000 Byte
```

实际 BMP 等文件还可能有文件头、行填充等额外开销；JPEG/PNG 等压缩文件大小不能直接用该公式得到。

📌 **国网考点：** 分辨率描述像素数量，色深描述每像素用于表示颜色的位数。

### 7. 浏览器缓存、剪贴板、临时文件、回收站

| 概念 | 基础作用 |
|---|---|
| 浏览器缓存 | 保存部分已访问资源，以减少重复获取、提升加载效率 |
| 剪贴板 | 临时保存复制/剪切的数据，供粘贴使用 |
| 临时文件 | 程序运行过程中产生的临时数据文件 |
| 回收站 | Windows 中对部分本地删除文件提供暂存和恢复机会 |

⚠️ **易错提醒：** 回收站不是备份系统；从某些可移动介质、命令行或特殊方式删除的文件也不一定进入回收站。

🧠 **速记：** `缓存为“快”，剪贴板为“复制粘贴”，回收站为“删除后的暂存机会”。`

### 8. Modem、AP、网卡：三个常见网络部件不要混

| 设备 | 核心定位 |
|---|---|
| 网卡 NIC | 为计算机提供网络接入接口，处理链路接入等 |
| AP | 无线接入点，让无线终端接入网络 |
| Modem | 调制解调器，在特定通信系统中完成调制/解调或接入转换 |

传统教材常把 Modem 解释为数字信号与模拟信号之间的调制/解调设备，这是拨号通信时代的典型考试口径。现代家庭所谓“光猫”通常还集成光接入、以太网等多种功能，不能机械等同于传统电话 Modem。

📌 **国网考点：** AP 不是“无线路由器”的完全同义词；家用无线路由器往往集成路由、交换、AP 等功能。

### 9. 完全备份、增量备份、差异备份：基础概念

| 类型 | 本次备份什么 | 恢复时典型需求 |
|---|---|---|
| 完全备份 | 选定范围内全部数据 | 最近一次完整备份 |
| 增量备份 | 自上一次备份（完整或增量）后变化的数据 | 完整备份 + 后续各次增量 |
| 差异备份 | 自上一次完整备份后变化的数据 | 完整备份 + 最新一次差异 |

一般而言，增量备份每次数据量较小，但恢复链条可能更长；差异备份会随距离完整备份时间增加而逐渐变大，但恢复通常只需完整备份和最新差异备份。

⚠️ **易错提醒：** RAID、同步盘、回收站都不能自动等同于“可靠备份”。备份强调存在可独立恢复的数据副本。

## 二、国网风格练习题

### 1. 下列关于 PCI 与 PCIe 的说法，正确的是（ ）。

A. PCIe 仍采用传统 PCI 的并行共享总线方式  
B. PCIe 采用高速串行、点对点链路  
C. AGP 是现代 NVMe SSD 的主要接口  
D. ISA 比 PCIe 更新

**答案：B**

**解析：** PCIe 与传统 PCI 的关键区别之一就是高速串行、点对点连接；PCI 典型为并行共享总线。AGP 主要是历史显卡接口，ISA 更早。

### 2. 在 Windows 中，用于列出当前运行进程的命令是（ ）。

A. tasklist  
B. tracert  
C. attrib  
D. chkdsk

**答案：A**

**解析：** `tasklist` 列出进程；`taskkill` 用于结束进程。`tracert` 用于网络路径追踪，`attrib` 处理文件属性，`chkdsk` 检查磁盘/文件系统。

### 3. Linux 执行 `ls -l` 时，某项首字符为 `d`，通常表示它是（ ）。

A. 块设备  
B. 目录  
C. 符号链接  
D. 普通文件

**答案：B**

**解析：** `d` 表示 directory；普通文件常为 `-`，符号链接为 `l`，块设备为 `b`。

### 4. 关于 Linux 符号链接，下列说法正确的是（ ）。

A. 本质上一定与目标共享同一个 inode  
B. 不能跨文件系统  
C. 目标删除后链接可能失效  
D. 创建符号链接必须复制目标文件内容

**答案：C**

**解析：** 符号链接保存目标路径，目标不存在时可能成为悬空链接。它可以跨文件系统，也不是复制文件内容；“共享同一 inode”更符合硬链接的核心理解。

### 5. 下列关于 Unicode 与 UTF-8 的表述，正确的是（ ）。

A. Unicode 与 UTF-8 完全是同一个概念  
B. UTF-8 是 Unicode 的一种编码形式  
C. UTF-8 只能表示英文  
D. GBK 是 UTF-8 的另一个名称

**答案：B**

**解析：** Unicode 解决统一字符编码空间等问题，UTF-8 是 Unicode 的一种编码方式。GBK 是中文编码体系中的扩展标准，并非 UTF-8。

### 6. 一幅未经压缩的 100×100 像素、每像素 24 bit 的图像，其像素数据量约为（ ）。

A. 10 000 Byte  
B. 20 000 Byte  
C. 30 000 Byte  
D. 240 000 Byte

**答案：C**

**解析：** `100×100×24÷8 = 30 000 Byte`。注意 24 bit 是每像素 3 Byte。

### 7. 关于 Windows 回收站，下列说法最准确的是（ ）。

A. 回收站等价于完整备份  
B. 所有删除操作都一定进入回收站  
C. 回收站可为部分删除文件提供恢复机会  
D. 回收站用于缓存网页

**答案：C**

**解析：** 回收站是删除文件的暂存机制之一，不是备份，也不是所有删除都一定经过回收站。网页资源缓存属于浏览器缓存功能。

### 8. 无线局域网中，为无线终端提供接入点功能的设备通常称为（ ）。

A. AP  
B. 扫描仪  
C. UPS  
D. 编译器

**答案：A**

**解析：** AP 即 Access Point，无线接入点。家用无线路由器往往集成 AP 功能，但 AP 本身不等于完整路由器。

### 9. 某系统周日做一次完全备份，此后每天做增量备份。若周四恢复到周三备份后的状态，通常需要（ ）。

A. 只需周三增量备份  
B. 周日完整备份以及周一、周二、周三各次增量备份  
C. 只需周日完整备份  
D. 周日完整备份和周三增量备份即可

**答案：B**

**解析：** 增量备份只记录自上一次备份后的变化，因此恢复时通常需要完整备份加上此后连续的各次增量备份。

### 10. 【滚动复习】下列存储技术中，通常直接运行在 PCIe 之上、面向非易失性存储的是（ ）。

A. NVMe  
B. PATA  
C. PS/2  
D. VGA

**答案：A**

**解析：** NVMe 是面向非易失性存储的高性能协议，现代 NVMe SSD 常通过 PCIe 通信。PATA 是传统并行 ATA，PS/2 是键鼠接口，VGA 是显示接口。

### 11. 【滚动复习】Linux 中用于查看磁盘文件系统空间使用情况的命令通常是（ ）。

A. df  
B. grep  
C. passwd  
D. tar

**答案：A**

**解析：** `df` 查看文件系统磁盘空间使用情况；`du` 更侧重目录/文件占用空间。`grep` 搜索文本，`passwd` 修改密码，`tar` 归档。

### 12. 【滚动复习】关于 `.tar.gz` 文件，下列说法正确的是（ ）。

A. `.tar` 必然已经采用 gzip 压缩  
B. `.tar.gz` 通常表示 tar 归档后再用 gzip 压缩  
C. gzip 只能压缩图片  
D. tar 是 Windows 注册表编辑器

**答案：B**

**解析：** tar 的核心是归档，gzip 负责压缩；`.tar.gz` 是二者组合的典型命名。

### 13. 【滚动复习】传统教材中，主要连接 CPU、内存和高速图形部件的芯片通常称为（ ）。

A. 南桥  
B. 北桥  
C. Modem  
D. 网卡

**答案：B**

**解析：** 传统 PC 架构中北桥负责 CPU、内存和高速图形等高速通路，南桥更多负责相对低速 I/O。现代 CPU 已集成许多原北桥功能，但老题口径仍需掌握。

### 14. 【滚动复习】下列文件格式中，通常采用有损压缩且常用于照片的是（ ）。

A. JPEG  
B. BMP  
C. TXT  
D. WAV PCM

**答案：A**

**解析：** JPEG 常采用有损压缩，适合照片类连续色调图像；BMP 常见为位图格式，TXT 是文本，PCM WAV 常保存未压缩音频采样数据。

### 15. 【滚动复习】下列关于恶意软件的说法，正确的是（ ）。

A. 蠕虫通常必须依附于宿主可执行文件才能传播  
B. 木马的典型特点是伪装或诱骗执行并实现未授权功能  
C. 安装防火墙后就不需要补丁和备份  
D. 病毒、木马、蠕虫三个词完全同义

**答案：B**

**解析：** 木马常以伪装、诱骗等方式进入系统并执行恶意功能；蠕虫的重要特点之一是可自主传播。安全防护应包括补丁、权限控制、备份等多种措施，不能依赖单一防火墙。

## 三、今日速记

| 看到关键词 | 立刻想到 |
|---|---|
| ISA | 早期 PC 扩展总线 |
| PCI | 并行、共享总线 |
| AGP | 历史显卡专用高速接口 |
| PCIe | 串行、点对点、x1/x4/x8/x16 |
| `tasklist` / `taskkill` | 列进程 / 结束进程 |
| `ipconfig` | Windows IP 配置 |
| `tracert` | 路径追踪 |
| Linux `- / d / l / c / b` | 普通 / 目录 / 软链接 / 字符设备 / 块设备 |
| `ln` / `ln -s` | 硬链接 / 符号链接 |
| UTF-8 | Unicode 的一种编码方式 |
| GBK | GB2312 的扩展体系 |
| 位图容量 | 宽×高×色深÷8 |
| AP | 无线接入点 |
| Modem | 调制/解调或接入转换（传统教材重点） |
| 完全备份 | 全部选定数据 |
| 增量备份 | 自上一次备份后的变化 |
| 差异备份 | 自上一次完整备份后的变化 |

🧠 **最后 20 秒速记：**

```text
PCI 并行共享，PCIe 串行点对点；AGP 老显卡。
tasklist 看进程，taskkill 结束；ipconfig 看本机，tracert 看路径。
Linux：-普通 d目录 l软链 c字符 b块设备。
Unicode ≠ UTF-8；UTF-8 是 Unicode 编码之一。
位图原始容量 = 像素数 × 色深 ÷ 8。
增量追“上一次备份”，差异追“上一次完整备份”。
```