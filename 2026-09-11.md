# 国网计算机基础与实用知识晨练｜2026-09-11

> **今日主题：** Cache/显存/虚拟内存、计算机性能指标、Windows 扩展名与 DOS 属性命令、Linux 文本检索统计、IEEE 802.15/802.16 与 Bluetooth/WiMAX、文本与二进制文件、驱动程序、FTP/远程登录/电子邮件基础  
> **今日目标：** 最近数日已集中覆盖 BIOS/UEFI、内存类型、PCIe、存储接口、Linux 用户/文件类型/压缩、Windows 注册表/文件系统、图像音视频等。今天避开这些连续热点，补齐容易被忽略的“存储层次辨析、性能指标、文本处理命令、无线网络标准定位、文件本质、驱动与互联网应用”等高性价比杂项。

## 一、今日知识点

### 1. Cache、显存、虚拟内存：三个“内存味”名词不是一回事

| 名称 | 核心作用 | 典型位置/实现 | 考试关键词 |
|---|---|---|---|
| Cache（高速缓存） | 缓解 CPU 与主存速度差异 | CPU 芯片内部为主，通常采用 SRAM 思路 | 快、容量较小、利用局部性 |
| 显存（VRAM/图形存储） | 保存 GPU 图形处理所需数据 | 独显板载显存或核显共享系统内存 | GPU、帧缓冲、纹理等 |
| 虚拟内存 | 扩展进程可用地址空间并支持内存管理 | RAM 与磁盘后备存储配合 | 页面、换入换出、不是“更快的内存” |

- Cache 的目标首先是**提高访存平均速度**，不是扩大硬盘容量。
- 显存服务于图形处理器；“显存越大，CPU 主存就越大”显然错误。
- 虚拟内存会使用磁盘/SSD 作为后备存储，但不能把“磁盘空间”简单等同于“物理内存”。

📌 **国网考点：** Cache 常与 SRAM 联系；主存常与 DRAM 联系；虚拟内存是操作系统内存管理机制。

⚠️ **易错提醒：** 虚拟内存的主要意义不是“用硬盘让程序运行得更快”。发生频繁换页时，性能反而可能明显下降。

### 2. 主频、字长、总线宽度与性能：不要见数字大就判定更快

常见性能/规格指标：

| 指标 | 基础含义 | 易错点 |
|---|---|---|
| 主频 | CPU 时钟频率之一 | 不同架构不能只凭主频比较综合性能 |
| 字长 | CPU 一次能够自然处理的数据位数等体系结构属性 | 不等于总线宽度 |
| 数据总线宽度 | 一次可并行传输的数据位数 | 与地址总线作用不同 |
| 地址总线宽度 | 与可表示的地址范围相关 | 不直接表示每次传多少数据 |
| 容量 | 存储设备可保存的数据规模 | 大容量不等于高速度 |
| 吞吐率 | 单位时间完成的数据/任务量 | 与单次响应时间不是同一指标 |

传统基础题常把“主频越高，CPU 一定越快”写成绝对结论。更准确的说法是：**在其他条件相近时**，主频提高通常有利于性能；跨微架构、核心数、缓存等条件变化时，不能仅凭主频下结论。

🧠 **速记：** `字长看处理位数；数据总线看一次传多少；地址总线看能寻多大范围。`

### 3. Windows/DOS 常见扩展名 + attrib/tree：识别型题很好拿分

| 扩展名 | 常见定位 |
|---|---|
| `.exe` | Windows 可执行程序 |
| `.com` | DOS 时代常见可执行文件格式 |
| `.bat` | 批处理文件 |
| `.cmd` | Windows 命令脚本 |
| `.dll` | 动态链接库 |
| `.sys` | 常见系统/驱动相关文件扩展名 |
| `.ini` | 传统文本配置文件 |
| `.reg` | 注册表导入/导出文本格式 |
| `.lnk` | Windows 快捷方式 |
| `.msi` | Windows Installer 安装包 |

两个容易漏掉的传统命令：

```bat
attrib filename.txt
tree C:\
```

- `attrib`：查看或设置文件属性，传统 DOS/Windows 题常涉及只读（R）、隐藏（H）、系统（S）、存档（A）等属性。
- `tree`：以树形结构显示目录层次。

📌 **国网考点：** `.lnk` 是快捷方式文件，不是目标程序本体；`.dll` 通常由程序加载使用，并非普通意义上直接双击运行的应用程序。

### 4. Linux 的 grep、find、wc、sort、uniq：一个找内容，一个找文件

| 命令 | 基础用途 | 典型辨析 |
|---|---|---|
| `grep` | 按模式搜索文本内容 | “内容里找字符串” |
| `find` | 在目录树中查找文件/目录 | “文件系统里找对象” |
| `wc` | 统计行数、单词数、字节数等 | `wc -l` 常统计行数 |
| `sort` | 对文本行排序 | 不等于查找 |
| `uniq` | 处理相邻重复行 | 常先 `sort` 再配合使用 |
| `head` | 查看开头若干行 | 与 `tail` 相对 |
| `tail` | 查看末尾若干行 | 日志查看常用 |

示例：

```bash
grep "error" app.log
find /var/log -name "*.log"
wc -l app.log
sort names.txt | uniq
```

⚠️ **易错提醒：** `grep` 和 `find` 都有“找”的感觉，但考试最常用的区分是：**grep 查内容，find 查文件路径/属性。** `uniq` 默认只消除/报告相邻重复行，所以与 `sort` 组合很常见。

### 5. IEEE 802.15、802.16 与 Bluetooth、WiMAX：今天不讲 Wi-Fi 代际

IEEE 802 家族中几个容易串号的定位：

| 标准族 | 基础定位 | 常见关联 |
|---|---|---|
| IEEE 802.3 | 有线以太网 | Ethernet |
| IEEE 802.11 | 无线局域网 | WLAN / Wi-Fi |
| IEEE 802.15 | 无线个域网 | WPAN，Bluetooth 等相关技术常在此语境出现 |
| IEEE 802.16 | 宽带无线城域网 | WiMAX |

- **WPAN** 强调个人周边较小范围设备互联。
- **WLAN** 强调局域网范围无线接入。
- **WMAN** 强调城域范围的无线宽带网络概念。
- Bluetooth 是典型短距离无线连接技术；WiMAX 在历史教材和旧题中常与 IEEE 802.16、无线城域网联系。

📌 **考试口径 vs 现代情况：** WiMAX 在当代大众移动接入中已不是主流，但旧教材/老题的标准对应关系仍有考试价值。

🧠 **速记：** `3 以太，11 无局，15 无个，16 无城。`

### 6. 文本文件、二进制文件与扩展名：扩展名不是“身份证”

- **文本文件**：内容按某种字符编码解释后主要表现为字符序列，例如 UTF-8 文本。
- **二进制文件**：通常需要按照特定格式结构解释字节，例如可执行文件、许多图片、音视频文件。
- 从计算机底层看，文件最终都是字节数据；“文本/二进制”更多反映**数据组织和解释方式**。
- 扩展名通常帮助操作系统和用户判断文件类型、选择默认应用，但**修改扩展名不会自动转换文件内部格式**。

例如，把 `photo.jpg` 改名为 `photo.png`，不会把 JPEG 编码的数据真正转换为 PNG。

⚠️ **易错提醒：** `.txt` 不代表世界上只有一种字符编码；文本仍可能采用 UTF-8、GBK、UTF-16 等不同编码。

### 7. 驱动程序：操作系统与硬件之间的重要软件层

**设备驱动程序（Device Driver）**使操作系统能够按统一机制控制具体硬件设备，可理解为操作系统与设备之间的重要适配软件。

典型设备包括：显卡、声卡、网卡、打印机、存储控制器等。

- 驱动程序通常属于**系统软件范畴**。
- 安装新硬件后，系统可能自动匹配驱动，也可能需要厂商驱动。
- “有硬件就一定无需驱动”错误；“驱动程序就是硬件固件”也错误。

| 名词 | 位置/作用 |
|---|---|
| 驱动程序 | 运行在操作系统软件环境中，帮助 OS 控制设备 |
| 固件 | 存储在设备非易失性存储中的底层软件 |
| 应用程序 | 面向用户任务，如文字处理、浏览器等 |

📌 **国网考点：** 设备管理器中出现设备异常，驱动问题是常见排查方向；但驱动、固件、应用软件三者不能混为一谈。

### 8. FTP、远程登录与电子邮件：应用服务先认“干什么”

| 应用/协议 | 基础用途 | 典型关键词 |
|---|---|---|
| FTP | 文件传输 | 上传、下载文件 |
| Telnet | 传统远程终端登录 | 明文传输，安全性较弱 |
| SSH | 安全远程登录/管理 | 加密保护，现代常用 |
| SMTP | 发送/传递电子邮件 | “发邮件” |
| POP3 | 从服务器取邮件的传统协议 | 下载式邮件访问常见 |
| IMAP | 服务器端邮件同步/管理 | 多设备同步更友好 |

📌 **国网考点：** SMTP 主要负责邮件发送/传递；POP3、IMAP 用于邮件接收/访问语境。Telnet 与 SSH 都可远程登录，但 SSH 提供加密保护。

⚠️ **易错提醒：** FTP 与 HTTP 都能传输数据，但“FTP 的典型定位是文件传输”仍是基础选择题常见答案；不要把 FTP 当作远程桌面协议。

## 二、国网风格练习题

### 1. 下列关于 Cache 的说法，正确的是（ ）。

A. 主要用于长期保存用户文件  
B. 主要用于缓解 CPU 与主存之间的速度差异  
C. 本质上就是显存  
D. 断电后一定保持数据不变

**答案：B**

**解析：** Cache 的核心作用是利用局部性提高平均访存效率，缓解 CPU 与主存速度差异。A 是外存用途；C 混淆 Cache 与 GPU 显存；D 错在 Cache 通常属于易失性存储体系。

### 2. 关于虚拟内存，下列说法最准确的是（ ）。

A. 它等同于 CPU Cache  
B. 它等同于独立显卡显存  
C. 它是操作系统内存管理机制，可使用外存作为后备存储  
D. 启用虚拟内存必然使程序运行速度提高

**答案：C**

**解析：** 虚拟内存通过地址空间、分页等机制管理内存，并可借助磁盘/SSD 后备存储。A、B概念错误；D 错在频繁换页可能降低性能。

### 3. 若只知道两款 CPU 的主频分别为 3.0 GHz 和 3.5 GHz，下列判断最合理的是（ ）。

A. 3.5 GHz 的 CPU 在任何任务中一定更快  
B. 3.0 GHz 的 CPU 一定核心数更多  
C. 仅凭主频不能确定跨架构 CPU 的综合性能高低  
D. 主频与 CPU 性能完全无关

**答案：C**

**解析：** CPU 性能还受微架构、IPC、核心数、缓存、功耗限制和工作负载等因素影响。A 过于绝对，B 无依据，D 又走向另一个极端。

### 4. Windows 中通常表示快捷方式文件的扩展名是（ ）。

A. `.dll`  
B. `.lnk`  
C. `.sys`  
D. `.msi`

**答案：B**

**解析：** `.lnk` 是 Windows 快捷方式常见扩展名；`.dll` 为动态链接库，`.sys` 常见于系统/驱动相关文件，`.msi` 为 Windows Installer 安装包。

### 5. 在 Linux 中，要在日志文件内容中搜索字符串 `error`，最直接的命令是（ ）。

A. `grep`  
B. `find`  
C. `mkdir`  
D. `mount`

**答案：A**

**解析：** `grep` 用于按模式搜索文本内容。`find` 主要在目录树中按名称、属性等查找文件；`mkdir` 创建目录；`mount` 挂载文件系统。

### 6. Linux 命令 `wc -l file.txt` 的典型作用是（ ）。

A. 删除 file.txt  
B. 统计 file.txt 的行数  
C. 修改 file.txt 权限  
D. 压缩 file.txt

**答案：B**

**解析：** `wc` 是文本统计工具，`-l` 表示统计行数。权限修改常用 `chmod`，压缩可用 gzip、zip 等工具。

### 7. 在传统教材和基础考试口径中，IEEE 802.16 主要对应（ ）。

A. 以太网  
B. 无线个域网  
C. 宽带无线城域网/WiMAX  
D. 令牌环网

**答案：C**

**解析：** IEEE 802.16 与宽带无线城域网、WiMAX 密切相关。802.3 对应以太网，802.15 对应无线个域网语境。WiMAX 虽已非现代主流接入技术，但老题对应关系仍需掌握。

### 8. 将文件 `a.jpg` 直接改名为 `a.png`，其结果通常是（ ）。

A. JPEG 数据自动重新编码为 PNG  
B. 只改变文件名/扩展名，不会自动转换内部格式  
C. 文件自动变成纯文本  
D. 文件内容必然被清空

**答案：B**

**解析：** 扩展名是文件名的一部分，改名并不执行格式转码。真正从 JPEG 转换为 PNG 需要软件读取原格式并按目标格式重新编码。

### 9. 下列最符合设备驱动程序定位的是（ ）。

A. 用于帮助操作系统控制具体硬件设备的软件  
B. 专门用于编写数据库 SQL 的语言  
C. 一定存放在 CPU Cache 中的程序  
D. 与操作系统和硬件均无关系的办公软件

**答案：A**

**解析：** 驱动程序是操作系统与硬件设备之间的重要软件接口/适配层。B、C、D 都偏离其定义。

### 10. 电子邮件系统中，主要用于发送和传递邮件的协议是（ ）。

A. SMTP  
B. IMAP  
C. POP3  
D. ARP

**答案：A**

**解析：** SMTP 负责邮件发送/传递；POP3 和 IMAP 主要用于用户访问接收邮件。ARP 属于网络地址解析范畴，不是电子邮件应用协议。

### 11. 【间隔复习】下列存储器中，通常需要周期性刷新的为（ ）。

A. SRAM  
B. DRAM  
C. ROM  
D. Flash ROM

**答案：B**

**解析：** DRAM 单元需要刷新，主存广泛采用 DRAM 技术；SRAM 不需要这种周期刷新，常用于高速缓存。此题复习昨日的 SRAM/DRAM 辨析。

### 12. 【间隔复习】传统 PC 架构中，主要面向显卡的专用高速接口是（ ）。

A. AGP  
B. PS/2  
C. SATA  
D. IDE

**答案：A**

**解析：** AGP 是历史上主要面向图形加速卡的接口；现代独立显卡普遍转向 PCIe。PS/2 常接键盘鼠标，SATA/IDE 属于存储设备接口语境。

### 13. 【间隔复习】Windows 中用于显示本机 IP 配置信息的常用命令是（ ）。

A. `ipconfig`  
B. `chkdsk`  
C. `attrib`  
D. `format`

**答案：A**

**解析：** `ipconfig` 用于查看 Windows TCP/IP 配置。`chkdsk` 检查磁盘/文件系统，`attrib` 操作文件属性，`format` 用于格式化卷。

### 14. 【间隔复习】PNG 与 JPEG 的常见区别中，正确的是（ ）。

A. JPEG 通常适合照片类有损压缩，PNG 常见为无损压缩并支持透明  
B. PNG 只能保存声音  
C. JPEG 一定支持动画  
D. 两者都是可执行文件格式

**答案：A**

**解析：** JPEG 常用于照片并采用有损压缩；PNG 常用无损压缩且支持透明通道。B、C、D 均为典型干扰项。

### 15. 【间隔复习】关于 Windows 注册表与 `.reg` 文件，下列说法正确的是（ ）。

A. `.reg` 文件就是 Windows 内核本身  
B. 注册表是保存大量系统和应用配置的层次化数据库，`.reg` 可用于导入/导出注册表项  
C. 注册表只保存图片文件  
D. 删除扩展名 `.reg` 就会删除整个注册表

**答案：B**

**解析：** 注册表承担系统、硬件、用户和应用配置存储；`.reg` 是常见的注册表导入/导出文本格式。A、C、D 都把文件与系统配置数据库混淆。

## 三、今日速记

| 看到关键词 | 立刻想到 |
|---|---|
| CPU—主存速度鸿沟 | Cache |
| Cache 常用存储技术 | SRAM |
| 主存常用存储技术 | DRAM / DDR SDRAM |
| GPU 图形数据 | 显存 |
| 外存作后备、地址空间 | 虚拟内存 |
| 一次自然处理的数据位数 | 字长 |
| 一次并行传输的数据位数 | 数据总线宽度 |
| 可寻址范围 | 地址总线宽度 |
| Windows 快捷方式 | `.lnk` |
| 动态链接库 | `.dll` |
| 安装包 | `.msi` |
| 文件属性 | `attrib` |
| 树形目录 | `tree` |
| Linux 搜文本内容 | `grep` |
| Linux 找文件 | `find` |
| 统计行数 | `wc -l` |
| 排序 | `sort` |
| 相邻重复行 | `uniq` |
| IEEE 802.3 | Ethernet |
| IEEE 802.11 | WLAN / Wi-Fi |
| IEEE 802.15 | WPAN |
| IEEE 802.16 | WMAN / WiMAX（传统考试口径） |
| 改扩展名 | 不等于转换文件格式 |
| OS 控制具体硬件 | 驱动程序 |
| 设备内部底层软件 | 固件 |
| 文件传输 | FTP |
| 传统明文远程登录 | Telnet |
| 加密远程登录 | SSH |
| 发邮件 | SMTP |
| 收/访问邮件 | POP3 / IMAP |

🧠 **最后 20 秒速记：**  
`Cache 不等于虚拟内存；grep 找内容、find 找文件；3 以太、11 无局、15 无个、16 无城；扩展名改了格式不自动变；驱动管硬件，SMTP 管发信，SSH 管安全远程登录。`
