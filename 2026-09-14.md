# 国网计算机基础与实用知识晨练｜2026-09-14

> **今日主题：** CPU 性能指标与字长、IDE/SATA/SCSI/NVMe 存储接口、DOS 文件操作命令、Linux 磁盘与挂载、网络传输介质与设备、视频编码与容器、软件分类、CRT/LCD 历史显示常识  
> **今日目标：** 近期已连续覆盖存储介质与文件系统、Linux 权限、IEEE 802、主板芯片组、传统 I/O、Windows 系统工具、Linux 进程、图像格式、Cache/虚拟内存、BIOS/UEFI 等。今天避开这些近期重点，补齐 CPU 基础指标、存储接口、DOS 文件操作、Linux 磁盘管理、网络介质、视频基础、软件分类和历史显示设备等高性价比杂项。

## 一、今日知识点

### 1. CPU 主频、字长与总线宽度：三个指标不要混

微型计算机题经常把几个“看起来都像性能指标”的概念放在一起考。

| 概念 | 基础含义 | 常见考法 |
|---|---|---|
| 主频 | CPU 时钟工作的频率指标之一 | 同架构下通常与处理速度有关，但不能单独决定整体性能 |
| 字长 | CPU 一次能够直接处理的二进制数据位数的典型表述 | 常见 8 位、16 位、32 位、64 位 |
| 数据总线宽度 | 一次可并行传送的数据位数 | 与数据传输能力相关 |
| 地址总线宽度 | 可表示地址的位数 | 与可寻址空间上限相关 |

传统基础教材常把**字长**作为微型计算机的重要性能指标。64 位处理器中的“64 位”，在基础考试语境下通常首先联系 CPU 字长/体系结构位宽，而不是“主频 64 Hz”或“内存容量 64 GB”。

⚠️ **易错提醒：** 主频越高并不意味着任何情况下 CPU 一定越快。微架构、核心数、缓存、指令效率等都会影响实际性能。选择题若问“主频是什么”，答频率指标；若问“是否唯一决定性能”，答案是否定的。

🧠 **速记：** `主频看快慢节拍，字长看一次处理多少位，地址线看能找多大空间。`

### 2. IDE/PATA、SATA、SCSI、NVMe：存储接口先认定位

| 名称 | 基础定位 | 高频关键词 |
|---|---|---|
| IDE/PATA | 传统 PC 并行 ATA 硬盘接口 | 并行、排线宽、历史技术 |
| SATA | 串行 ATA，长期广泛用于 HDD/SSD | 串行、硬盘、光驱 |
| SCSI | 历史上常见于服务器、工作站及专业存储 | 多设备、服务器/专业场景 |
| NVMe | 面向非易失存储器设计的协议，现代高速 SSD 常通过 PCIe 使用 | PCIe、SSD、低延迟、高并行 |

**PATA 与 SATA**最容易直接出对应题：PATA 是并行 ATA，SATA 是串行 ATA。

NVMe 与 SATA 不能简单理解为“两个长得不同的插头”。NVMe 本质上是面向非易失存储设备的访问协议，常运行在 PCIe 之上；现代 M.2 SSD 既可能采用 SATA，也可能采用 PCIe/NVMe，因此 **M.2 这个外形/接口形态不能自动等同于 NVMe**。

📌 **国网考点：** 老题看到 IDE，多数可联想到 PATA；现代高速固态存储看到 NVMe，多数应联想到 PCIe。

⚠️ **易错提醒：** SATA SSD 仍然是 SSD；“SSD 必然使用 NVMe”错误。

### 3. DOS/Windows 文件操作命令：copy、xcopy、move、del、ren、type

基础命令题重在“命令—用途”对应。

```bat
copy a.txt b.txt
move a.txt D:\backup\
del old.txt
ren old.txt new.txt
type readme.txt
xcopy C:\data D:\backup /E
```

| 命令 | 主要用途 |
|---|---|
| `copy` | 复制文件 |
| `xcopy` | 扩展复制，可用于目录树等较复杂复制 |
| `move` | 移动文件/目录 |
| `del` | 删除文件 |
| `ren` | 重命名 |
| `type` | 显示文本文件内容 |
| `dir` | 列出目录内容 |
| `cd` | 改变当前目录 |
| `cls` | 清屏 |
| `tree` | 以树状形式显示目录结构 |

📌 **国网考点：** `del` 不是删除目录的通用命令；`type` 主要用于查看文本内容；`ren` 负责重命名。

⚠️ **易错提醒：** 命令行删除通常不像资源管理器删除那样天然进入“回收站”，不要把 GUI 行为机械套到命令行。

🧠 **速记：** `copy 复制，move 搬家，del 删除，ren 改名，type 看文本。`

### 4. Linux df、du、mount、umount：磁盘空间和挂载不是一回事

Linux 中这四个命令经常被混淆：

| 命令 | 基础用途 |
|---|---|
| `df` | 查看文件系统整体磁盘空间使用情况 |
| `du` | 统计文件或目录占用空间 |
| `mount` | 挂载文件系统 |
| `umount` | 卸载已挂载的文件系统 |

示例：

```bash
df -h
du -sh /var/log
mount /dev/sdb1 /mnt/usb
umount /mnt/usb
```

Linux 采用统一目录树。磁盘分区、U 盘等文件系统通常需要挂载到某个目录（挂载点），用户再通过目录树访问它，而不是像传统 Windows 使用方式那样主要依赖 `C:`、`D:` 盘符。

📌 **国网考点：** `df` 看“文件系统整体剩多少”，`du` 看“某目录/文件用了多少”。

⚠️ **易错提醒：** 正确命令是 `umount`，不是英语直觉拼出的 `unmount`。

### 5. 双绞线、同轴电缆、光纤与常见网络设备

#### 传输介质

| 介质 | 基础特点 | 常见联想 |
|---|---|---|
| 双绞线 | 成对绞合铜线，局域网常见 | Ethernet、RJ-45 |
| 同轴电缆 | 内外导体同轴结构 | 早期以太网、有线电视等 |
| 光纤 | 以光信号传输 | 高带宽、远距离、抗电磁干扰能力强 |

光纤不以电流作为主要信息载体，因此对电磁干扰具有很强的抵抗能力，是典型选择题考点。

#### 设备定位

| 设备 | 基础作用 |
|---|---|
| 集线器 Hub | 传统共享式网络设备，收到信号后向其他端口转发 |
| 交换机 Switch | 局域网中根据 MAC 等信息转发以太网帧 |
| 路由器 Router | 在不同 IP 网络之间进行分组转发 |
| AP | 为无线终端提供接入，连接无线与有线网络环境 |
| 调制解调器 Modem | 完成调制/解调等信号转换 |

📌 **国网考点：** 基础口径可记“交换机看 MAC、路由器看 IP”；这是选择题定位口诀，不应扩展成对所有高级设备实现的绝对描述。

🧠 **速记：** `双绞线常配 RJ-45；光纤抗电磁干扰；交换机连局域网，路由器跨网络。`

### 6. 视频：帧率、分辨率、编码与容器四个概念

视频类题最容易把“编码格式”和“文件容器”混在一起。

- **分辨率**：每帧图像的像素规模，如 1920×1080。
- **帧率**：单位时间显示/记录的帧数，常用 fps 表示。
- **编码/压缩**：决定视频数据如何压缩表示，例如 H.264、H.265/HEVC 等。
- **容器**：把视频流、音频流、字幕、元数据等组织在一个文件中，例如 MP4、MKV、AVI。

因此：

```text
MP4 文件 ≠ “MP4 就是一种唯一的视频编码”
容器中可以封装特定的视频编码流和音频编码流。
```

同理，`.mkv` 更典型地表示一种多媒体容器，而不是“只能采用某一种压缩算法”。

📌 **国网考点：** AVI、MP4、MKV 常从“容器/文件格式”层面识别；编码与容器要分开。

⚠️ **易错提醒：** 分辨率提高通常意味着每帧包含更多像素，但最终文件大小还受编码方式、码率、时长等多因素影响，不能只看分辨率下绝对结论。

### 7. 系统软件、应用软件、工具软件与中间件

| 类别 | 基础定位 | 典型例子/关键词 |
|---|---|---|
| 系统软件 | 管理计算机资源并为应用提供运行基础 | 操作系统、部分系统支撑程序 |
| 应用软件 | 面向用户具体应用任务 | 办公、图像处理、浏览器等 |
| 工具软件 | 辅助维护、管理、压缩、备份等 | 压缩工具、备份工具、部分安全工具 |
| 中间件 | 位于系统软件与应用之间，为分布式/应用系统提供通用服务 | 通信、事务、消息等通用支撑 |

考试中最稳定的辨析是：**操作系统属于系统软件**，而文字处理、电子表格等属于应用软件。

编译程序、汇编程序等语言处理程序在传统教材分类中通常归入系统软件范畴；具体软件产品在现代生态中的分类可能更复杂，做基础题时应尊重题目教材口径。

📌 **国网考点：** “系统软件 = 操作系统”也不严谨，因为系统软件范围通常比操作系统更广。

### 8. CRT 与 LCD：老教材显示器知识仍可能考

| 项目 | CRT | LCD |
|---|---|---|
| 全称关键词 | 阴极射线管 | 液晶显示 |
| 结构特点 | 显像管，体积较大 | 平板化、较轻薄 |
| 历史定位 | 早期 PC 显示器常见 | 后来成为主流显示技术之一 |

传统 CRT 显示器通过电子束等机制形成图像，体积和重量通常较大。LCD 是液晶显示技术，平板化程度高，功耗与体积等方面相较传统 CRT 更适合现代桌面设备。

**考试口径与现代情况：** 老题可能把 CRT、LCD 作为两类显示器直接比较；现代消费设备中 LCD/OLED 等平板显示已占主流，但这不影响 CRT 在历史教材题中的正确定位。

⚠️ **易错提醒：** VGA、DVI、HDMI、DisplayPort 是显示信号接口/连接标准范畴；CRT、LCD、OLED 是显示技术/设备范畴，两组概念不在同一分类层级。

---

## 二、国网风格练习题

### 1. 在微型计算机基础知识中，CPU 的“字长”通常是指（ ）。

A. CPU 每秒钟产生的时钟脉冲数  
B. CPU 一次能够直接处理的二进制数据位数  
C. 内存条的物理长度  
D. 地址总线的工作频率

**答案：B**

**解析：** 字长是处理器位宽的重要基础概念，传统考试常表述为 CPU 一次能处理的二进制位数。A 描述的是与主频相关的概念；C、D 均不是字长定义。

### 2. 下列关于 NVMe 的说法，最恰当的是（ ）。

A. 它是专门用于机械软盘的并行接口  
B. 它是面向非易失存储设备的协议，现代 SSD 常通过 PCIe 使用  
C. 所有 M.2 设备都必然使用 NVMe  
D. 它与 PCIe 完全无关

**答案：B**

**解析：** NVMe 面向非易失存储设计，现代高速 NVMe SSD 通常使用 PCIe。M.2 主要描述外形/连接形态，M.2 SSD 也可能采用 SATA，因此 C 错。

### 3. 在 DOS/Windows 命令中，用于重命名文件的命令是（ ）。

A. `ren`  
B. `type`  
C. `cls`  
D. `tree`

**答案：A**

**解析：** `ren` 用于重命名；`type` 显示文本文件内容；`cls` 清屏；`tree` 显示目录树。

### 4. Linux 中，要查看 `/var/log` 目录大致占用了多少磁盘空间，较合适的命令是（ ）。

A. `df -h /var/log`  
B. `du -sh /var/log`  
C. `mount /var/log`  
D. `chmod /var/log`

**答案：B**

**解析：** `du` 用于统计文件或目录占用空间，`-sh` 常用于以易读方式给出汇总。`df` 主要查看文件系统整体空间情况，二者是高频易混点。

### 5. 下列传输介质中，抗电磁干扰能力通常最强的是（ ）。

A. 双绞线  
B. 同轴电缆  
C. 光纤  
D. 普通铜质电话线

**答案：C**

**解析：** 光纤利用光信号传输，不以铜导体中的电信号作为主要载体，因此抗电磁干扰能力强，也是远距离、高带宽通信的重要介质。

### 6. 在基础网络设备辨析中，主要用于在不同 IP 网络之间转发分组的设备是（ ）。

A. 路由器  
B. 集线器  
C. 键盘  
D. 扫描仪

**答案：A**

**解析：** 路由器的典型定位是连接不同网络并根据网络层信息进行转发。交换机主要用于局域网帧交换，集线器则是更早期的共享式设备。

### 7. 关于 MP4、MKV 与视频编码，下列说法正确的是（ ）。

A. MP4 文件中绝对不能包含音频  
B. MKV 只能使用一种固定的视频编码  
C. 容器可以组织视频流、音频流、字幕等内容  
D. 帧率就是视频分辨率

**答案：C**

**解析：** MP4、MKV 等可作为多媒体容器组织多种媒体流。编码、容器、帧率、分辨率是不同概念，不能混为一谈。

### 8. 下列软件中，按传统计算机基础教材分类，最典型的系统软件是（ ）。

A. 操作系统  
B. 电子表格文档  
C. 用户制作的演示文稿  
D. 数码照片

**答案：A**

**解析：** 操作系统负责管理硬件和软件资源，并为应用提供运行环境，是最典型的系统软件。B～D 中后两项甚至不是软件本身。

### 9. 关于 CRT 与 LCD，下列说法正确的是（ ）。

A. CRT 是一种压缩文件格式  
B. LCD 是一种网络协议  
C. CRT 属于传统阴极射线管显示技术，LCD 属于液晶显示技术  
D. VGA 和 LCD 属于完全相同层次的概念

**答案：C**

**解析：** CRT 与 LCD 都是显示技术/显示设备类别。VGA 更常作为显示接口/信号标准相关概念出现，与 LCD 并非同一分类层次。

### 10. 【滚动复习】Windows 中，相比 FAT32，NTFS 的典型特点不包括（ ）。

A. 支持访问控制等安全机制  
B. 支持日志机制  
C. 更适合现代 Windows 系统盘  
D. 单文件大小上限固定为约 4 GiB

**答案：D**

**解析：** 约 4 GiB 的著名单文件大小限制属于 FAT32 的高频考点，不是 NTFS 的典型限制。NTFS 在权限、日志、大文件支持等方面通常更强。

### 11. 【滚动复习】Linux 命令 `chmod 755 test.sh` 中，数字 5 对应的权限组合是（ ）。

A. `r--`  
B. `-wx`  
C. `r-x`  
D. `rw-`

**答案：C**

**解析：** Linux 权限数字中 `r=4`、`w=2`、`x=1`，因此 5=4+1=`r-x`。755 即 `rwxr-xr-x`。

### 12. 【滚动复习】在传统 PC 芯片组架构中，通常负责 USB、SATA 等相对低速 I/O 的是（ ）。

A. 南桥  
B. 北桥  
C. 显存  
D. Cache

**答案：A**

**解析：** 老教材口径常记“北桥管高速、南桥管 I/O”。现代平台大量北桥功能已进入 CPU，PCH 承接许多传统南桥职责，但历史题仍应按题目时代口径判断。

### 13. 【滚动复习】下列关于 SSD 的说法正确的是（ ）。

A. 主流 SSD 依靠磁头读取磁性盘片  
B. SSD 必须使用 SATA 接口  
C. 主流 SSD 通常使用 NAND Flash，且没有 HDD 那样的机械磁头和旋转盘片  
D. SSD 断电后一定立即丢失全部数据

**答案：C**

**解析：** 主流 SSD 属于基于 Flash 的非易失存储，没有 HDD 的机械寻道结构。SSD 可以使用 SATA，也可以使用 PCIe/NVMe 等，因此 B 错。

### 14. 【滚动复习】IEEE 802.3 在基础考试中主要对应（ ）。

A. Ethernet 以太网  
B. WLAN 无线局域网  
C. WiMAX  
D. Token Ring

**答案：A**

**解析：** IEEE 802.3 对应以太网；802.11 对应无线局域网；802.16 常与宽带无线城域网/WiMAX 联系；802.5 是历史令牌环相关标准。

### 15. 【滚动复习】关于病毒、蠕虫和木马，下列说法最恰当的是（ ）。

A. 三者在任何情况下都是完全相同的概念  
B. 蠕虫常强调可利用网络等方式自我传播  
C. 安装系统补丁一定会使任何恶意软件永久失效  
D. 防火墙可以替代所有备份措施

**答案：B**

**解析：** 蠕虫的典型特征之一是具有较强的自我传播能力。病毒、蠕虫、木马在基础安全分类中有不同侧重点；补丁、防火墙、备份分别解决不同风险，不能互相完全替代。

---

## 三、今日速记

### 1. CPU 三个易混指标

```text
主频 → 时钟频率指标
字长 → 一次直接处理多少位
地址总线宽度 → 可表示多少地址
```

### 2. 存储接口一眼识别

| 看到 | 想到 |
|---|---|
| IDE / PATA | 传统并行 ATA |
| SATA | 串行 ATA，HDD/SSD 常见 |
| SCSI | 服务器/工作站历史高频 |
| NVMe | 非易失存储协议，现代常走 PCIe |
| M.2 | 外形/连接形态，不等于必然 NVMe |

### 3. DOS 文件命令

```text
copy → 复制
xcopy → 扩展复制
move → 移动
del → 删除文件
ren → 重命名
type → 看文本
dir → 列目录
cd → 切目录
cls → 清屏
tree → 目录树
```

### 4. Linux 磁盘四兄弟

```text
df     → 文件系统整体空间
du     → 文件/目录占用
mount  → 挂载
umount → 卸载（注意没有 n）
```

### 5. 网络介质与设备

```text
双绞线 → 局域网常见 → RJ-45
光纤 → 光信号 → 高带宽、远距离、抗电磁干扰
交换机 → 基础口诀：看 MAC，局域网帧转发
路由器 → 基础口诀：看 IP，连接不同网络
AP → 无线接入
Modem → 调制/解调
```

### 6. 视频四概念

```text
分辨率 → 每帧多少像素
帧率 → 每秒多少帧
编码 → 数据如何压缩表示
容器 → 视频 + 音频 + 字幕 + 元数据的组织方式
MP4 / MKV / AVI：先想到容器/文件格式，不要机械等同某一种编码
```

### 7. 软件分类

```text
操作系统 → 系统软件
办公/浏览器等 → 应用软件
压缩/备份等 → 工具软件
中间件 → 为应用提供通用支撑服务
```

### 8. 历史显示口径

```text
CRT → 阴极射线管 → 老式、体积大
LCD → 液晶显示 → 平板化
VGA/DVI/HDMI/DP → 接口/连接标准
CRT/LCD/OLED → 显示技术
```

> **最后一分钟：** `字长不是主频；M.2 不等于 NVMe；df 看盘、du 看目录；ren 改名、type 看文本；光纤抗电磁干扰；容器不等于编码；CRT/LCD 是显示技术，VGA/HDMI 是接口。`
