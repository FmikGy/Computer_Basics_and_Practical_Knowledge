# 国网计算机基础与实用知识晨练｜2026-09-04

> **今日主题：** CPU 性能指标与指令扩展、DDR/Cache/虚拟内存辨析、DOS 文件操作命令、Linux 目录与文本工具、办公/压缩文件格式、多媒体容量计算、打印机与扫描仪、历史存储介质  
> **今日目标：** 避开前两天 BIOS、存储协议、内存类型、显示接口等正文重点，继续补齐“性能指标、DOS 实操、Linux 目录/文本、办公格式、多媒体计算、外设与历史介质”等基础杂项。

---

## 一、今日知识点

### 1. CPU 性能指标：主频、字长、核心数、总线宽度不能混为一谈

| 指标 | 核心含义 | 常见误区 |
|---|---|---|
| **主频** | CPU 时钟工作的频率，常用 GHz 表示 | 主频更高不代表任何场景都一定更快 |
| **字长** | CPU 一次可处理的二进制数据位数的典型描述 | 不等于主频，也不等于内存容量 |
| **核心数** | 单个处理器中可独立执行任务的处理核心数量 | 核心翻倍不代表单线程性能翻倍 |
| **总线宽度** | 总线一次并行传送的数据位数 | 与总线频率是两个不同概念 |

影响整机性能的因素很多，包括微架构、Cache、内存、存储、软件并行性等。因此考试中若问“衡量 CPU 性能的指标”，可识别主频、字长等；若说“主频越高，计算机性能必然越高”，通常过于绝对。

> 📌 **国网考点：** `Hz` 是频率单位；`bit` 常用于字长、总线宽度等位数描述。

---

### 2. MMX、SSE、AVX：都是 CPU 扩展指令集，但时代和能力不同

| 名称 | 考试层面的定位 | 关键词 |
|---|---|---|
| **MMX** | 早期多媒体扩展指令技术 | 老题、多媒体、SIMD 思想 |
| **SSE** | Streaming SIMD Extensions | 流式 SIMD 扩展 |
| **AVX** | Advanced Vector Extensions | 更宽的向量运算能力 |

**SIMD** 可理解为“单指令、多数据”：一条指令同时对多个数据元素执行相同或类似运算，适合图像、音视频、科学计算等向量化场景。

> ⚠️ **历史口径：** 老题若问“Intel 的多媒体扩展指令集”，常把 **MMX** 作为典型答案；现代处理器已经发展出 SSE、AVX 等多代扩展，不能据此否定旧题。

🧠 **速记：** `MMX（早） → SSE → AVX（更宽向量）`。

---

### 3. DDR、Cache、显存、虚拟内存：四种“内存相关名词”分清位置和用途

| 名称 | 典型位置/介质 | 主要作用 |
|---|---|---|
| **DDR SDRAM** | 主存内存条 | 保存正在运行的程序和数据 |
| **Cache** | CPU 内部或紧邻 CPU | 缓解 CPU 与主存速度差异 |
| **显存（VRAM）** | 显卡/GPU 使用 | 保存图形、纹理、帧缓冲等数据 |
| **虚拟内存** | OS 内存管理机制，通常借助磁盘/SSD 后备存储 | 扩展进程地址空间并进行内存管理 |

DDR 中的 **DDR = Double Data Rate**，典型含义是在时钟的上升沿和下降沿都可进行数据传输。DDR2/3/4/5 是代际名称。

> ⚠️ **易错提醒：** 虚拟内存不是“装在主板上的一种内存芯片”；Cache 也不是硬盘上的缓存文件。

---

### 4. DOS/Windows 文件操作命令：copy、xcopy、move、ren、del、type

| 命令 | 核心用途 |
|---|---|
| `copy` | 复制一个或多个文件 |
| `xcopy` | 扩展复制，可用于目录树等场景；现代 Windows 还有更强的 `robocopy` |
| `move` | 移动文件/目录，也可用于重命名某些对象 |
| `ren` / `rename` | 重命名文件或目录 |
| `del` / `erase` | 删除文件 |
| `type` | 在命令行显示文本文件内容 |
| `tree` | 以树状形式显示目录结构 |
| `cls` | 清屏 |

示例：

```bat
copy a.txt D:\backup\
ren old.txt new.txt
type new.txt
tree C:\work
```

> ⚠️ **易错提醒：** `del` 主要删除文件；删除目录常见 `rmdir`/`rd`。命令行删除通常不等同于资源管理器中“放入回收站”。

---

### 5. Linux 常见目录：/etc、/var、/usr、/proc、/tmp 一眼定位

| 目录 | 典型用途（考试层面） |
|---|---|
| `/home` | 普通用户主目录的常见上级目录 |
| `/root` | root 用户的主目录 |
| `/etc` | 系统级配置文件 |
| `/var` | 经常变化的数据，如日志、缓存、队列等 |
| `/usr` | 大量用户空间程序、库和共享数据 |
| `/dev` | 设备文件 |
| `/proc` | 虚拟文件系统，提供进程和内核相关信息 |
| `/tmp` | 临时文件 |

> 📌 **国网考点：** “系统配置”优先想到 `/etc`；“设备文件”想到 `/dev`；“进程/内核运行信息”想到 `/proc`。
>
> ⚠️ `/proc` 中很多内容不是普通磁盘文件，而是内核动态提供的接口。

---

### 6. Linux 文本工具：grep、find、cut、sort、uniq、wc 各管什么

| 命令 | 典型用途 |
|---|---|
| `grep` | 按模式搜索文本内容 |
| `find` | 在目录树中按名称、类型、时间等条件查找文件 |
| `cut` | 按字段或字符位置截取文本 |
| `sort` | 对文本行排序 |
| `uniq` | 处理**相邻**重复行，常与 `sort` 配合 |
| `wc` | 统计行数、单词数、字节数等 |

常见组合：

```bash
sort names.txt | uniq
wc -l access.log
grep "error" app.log
find /tmp -name "*.log"
```

> ⚠️ **易错提醒：** `grep` 主要找“内容”，`find` 主要找“文件/目录”；`uniq` 并不会自动把文件中所有分散的重复行都全局去重，因此常先 `sort`。

---

### 7. 办公与压缩文件格式：扩展名先建立“家族感”

| 类型 | 常见扩展名 | 典型含义 |
|---|---|---|
| Word | `.doc` / `.docx` | 文本文档/Office Open XML 文档 |
| Excel | `.xls` / `.xlsx` | 电子表格 |
| PowerPoint | `.ppt` / `.pptx` | 演示文稿 |
| PDF | `.pdf` | 便携式文档格式 |
| ZIP | `.zip` | 通用压缩归档格式 |
| RAR | `.rar` | 压缩归档格式 |
| 7-Zip | `.7z` | 压缩归档格式 |
| gzip | `.gz` | 常见于 UNIX/Linux，通常压缩单个数据流 |

`.docx/.xlsx/.pptx` 属于 Office Open XML 家族，内部本质上采用 ZIP 容器组织 XML 等资源，但考试通常只要求识别文件类型。

> ⚠️ **易错提醒：** 扩展名只是文件名的一部分，不能从安全角度保证文件真实格式；选择题中则通常按标准扩展名对应关系作答。

---

### 8. 多媒体容量计算：图像和 PCM 音频的两个基础公式

#### 未压缩位图图像

忽略文件头、行填充等额外开销时：

```text
容量(bit) ≈ 水平像素 × 垂直像素 × 色深(bit/像素)
容量(Byte) ≈ 上式 ÷ 8
```

例如 `800 × 600`、24 位真彩色图像：

```text
800 × 600 × 24 ÷ 8 = 1,440,000 Byte
```

#### 未压缩 PCM 音频

```text
码率(bit/s) = 采样频率 × 量化位数 × 声道数
容量 = 码率 × 时间
```

例如 44.1 kHz、16 bit、双声道 PCM：

```text
44100 × 16 × 2 = 1,411,200 bit/s
```

> ⚠️ 实际 BMP/WAV 文件还有文件头等开销；MP3、JPEG 等经过压缩后不能直接用未压缩公式估算最终文件大小。

---

### 9. 打印机与扫描仪：一个输出，一个输入，历史类型也会考

| 设备 | I/O 分类 | 常见类型/指标 |
|---|---|---|
| **打印机** | 输出设备 | 针式、喷墨、激光；常见指标 dpi、ppm |
| **扫描仪** | 输入设备 | 将纸质图文数字化；常见指标 dpi、色彩位数 |

传统教材常见打印机分类：

- **针式打印机**：击打式，适合多联票据等历史/特定场景。
- **喷墨打印机**：喷射墨滴成像。
- **激光打印机**：利用激光成像和静电复印原理体系，速度快、办公场景常见。

> 📌 **国网考点：** 键盘、鼠标、扫描仪通常属于输入设备；显示器、打印机通常属于输出设备。

---

### 10. 软盘、光盘、磁带：老介质仍可能以“存储原理/用途”出现

| 介质 | 存储原理/特点 | 老题关键词 |
|---|---|---|
| **软盘** | 磁存储 | 容量小、可移动、历史 PC |
| **CD/DVD** | 光存储 | 光盘、激光读写 |
| **磁带** | 磁存储，顺序访问特征明显 | 备份、归档、容量/成本优势 |

经典 3.5 英寸高密度软盘常见容量为 **1.44 MB**，属于非常典型的历史教材记忆点。

> ⚠️ **现代情况：** 软盘已基本退出日常使用，但磁带在大规模冷数据备份和长期归档中仍有实际用途。

---

## 二、国网风格练习题

### 1. 下列关于 CPU 主频和字长的说法，正确的是（ ）。

A. 主频的单位通常是 Byte  
B. 字长越大，CPU 主频必然越高  
C. 主频常用 Hz 表示，字长通常用 bit 描述  
D. 字长就是 CPU 的核心数量

**答案：C**

**解析：** 主频是频率，单位通常为 Hz 及其倍数；字长描述处理二进制数据的位数，常用 bit。两者不是同一指标，也不存在简单的必然正相关。

---

### 2. 传统教材中，最典型的早期多媒体扩展指令技术是（ ）。

A. MMX  
B. NTFS  
C. SATA  
D. UEFI

**答案：A**

**解析：** MMX 是 Intel 早期面向多媒体处理的扩展指令技术。NTFS 是文件系统，SATA 是存储接口体系，UEFI 是固件接口。

---

### 3. 下列关于虚拟内存的描述，正确的是（ ）。

A. 是一种固定焊接在主板上的 SRAM 芯片  
B. 是操作系统内存管理机制，可借助外存作为后备存储  
C. 专门用于保存 GPU 纹理  
D. 与地址空间无关

**答案：B**

**解析：** 虚拟内存属于操作系统内存管理机制，通常结合磁盘/SSD 后备存储，为进程提供虚拟地址空间。GPU 纹理等主要与显存相关；SRAM 常见于 Cache。

---

### 4. 在 Windows/DOS 命令中，用于重命名文件的典型命令是（ ）。

A. `ren`  
B. `type`  
C. `cls`  
D. `tree`

**答案：A**

**解析：** `ren`（rename）用于重命名；`type` 显示文本内容；`cls` 清屏；`tree` 显示目录树。

---

### 5. Linux 中通常用于存放系统级配置文件的目录是（ ）。

A. `/dev`  
B. `/etc`  
C. `/proc`  
D. `/tmp`

**答案：B**

**解析：** `/etc` 是系统配置文件的典型位置；`/dev` 放设备文件，`/proc` 提供进程/内核信息接口，`/tmp` 用于临时文件。

---

### 6. 若要在 Linux 目录树中查找名称为 `test.txt` 的文件，最直接的常用命令是（ ）。

A. `grep`  
B. `find`  
C. `wc`  
D. `cut`

**答案：B**

**解析：** `find` 用于按名称、类型等条件搜索文件/目录；`grep` 主要在文本中匹配内容。典型写法如 `find . -name "test.txt"`。

---

### 7. Linux 命令 `wc -l access.log` 的主要作用是（ ）。

A. 统计文件行数  
B. 删除重复行  
C. 压缩日志文件  
D. 修改文件权限

**答案：A**

**解析：** `wc` 是统计工具，`-l` 表示统计行数。去相邻重复行常用 `uniq`，压缩可用 gzip 等，权限修改使用 `chmod`。

---

### 8. 下列扩展名与文件类型对应正确的是（ ）。

A. `.xlsx` —— 演示文稿  
B. `.pptx` —— 电子表格  
C. `.docx` —— Word 文档  
D. `.gz` —— Windows 注册表文件

**答案：C**

**解析：** `.docx` 是 Word 文档；`.xlsx` 对应 Excel，`.pptx` 对应 PowerPoint；`.gz` 是 gzip 压缩数据常见扩展名，Windows 注册表导入/导出文件常见 `.reg`。

---

### 9. 一幅未压缩的 800×600、24 位真彩色图像，在忽略文件头和填充的情况下，其数据量约为（ ）。

A. 480,000 Byte  
B. 1,440,000 Byte  
C. 11,520,000 Byte  
D. 14,400 Byte

**答案：B**

**解析：** `800 × 600 × 24 ÷ 8 = 1,440,000 Byte`。选项 C 是未除以 8 的 bit 数，属于典型干扰项。

---

### 10. 44.1 kHz、16 bit、双声道的未压缩 PCM 音频，其理论码率约为（ ）。

A. 705.6 kbit/s  
B. 1,411.2 kbit/s  
C. 44.1 kbit/s  
D. 16 kbit/s

**答案：B**

**解析：** `44100 × 16 × 2 = 1,411,200 bit/s ≈ 1411.2 kbit/s`。双声道的“×2”很容易漏掉。

---

### 11. 下列设备中，通常属于输入设备的是（ ）。

A. 激光打印机  
B. 显示器  
C. 扫描仪  
D. 音箱

**答案：C**

**解析：** 扫描仪把纸质图文输入计算机，属于输入设备；打印机、显示器和音箱通常属于输出设备。

---

### 12. 传统教材中，3.5 英寸高密度软盘最常见的标称容量是（ ）。

A. 1.44 MB  
B. 4.7 GB  
C. 700 MB  
D. 8.5 GB

**答案：A**

**解析：** 1.44 MB 是经典 3.5 英寸高密度软盘容量。700 MB 常与 CD-R 典型容量关联，4.7 GB 常见于单层 DVD，8.5 GB 常见于双层 DVD 的典型标称容量。

---

### 13. 【滚动复习】关于 SRAM 和 DRAM，下列说法正确的是（ ）。

A. SRAM 通常需要周期性刷新  
B. DRAM 常用于 CPU Cache，且速度通常快于 SRAM  
C. SRAM 常用于 Cache，DRAM 常用于主存  
D. SRAM 断电后仍能永久保存数据

**答案：C**

**解析：** SRAM 速度快、成本高，常用于 Cache；DRAM 密度高、成本较低且需要刷新，常用于主存。两者通常都属于易失性存储器。

---

### 14. 【滚动复习】Windows 中用于跟踪到目标主机大致路由路径的命令是（ ）。

A. `tasklist`  
B. `tracert`  
C. `attrib`  
D. `chkdsk`

**答案：B**

**解析：** Windows 的 `tracert` 用于跟踪路由路径；`tasklist` 查看进程，`attrib` 修改/查看文件属性，`chkdsk` 检查磁盘和文件系统。

---

### 15. 【滚动复习】传统 PC 接口中，与打印机最经典对应的并行端口名称是（ ）。

A. COM  
B. LPT  
C. PS/2  
D. VGA

**答案：B**

**解析：** LPT 是传统并行端口，经典用途之一是连接打印机；COM 对应串口，PS/2 常连接键盘/鼠标，VGA 是模拟视频接口。

---

## 三、今日速记

```text
CPU：
主频 → Hz/GHz
字长 → bit
核心数 ≠ 字长 ≠ 主频
MMX → 早期多媒体扩展
SSE / AVX → SIMD/向量扩展

内存相关：
DDR → 主存
Cache → CPU 高速缓存，常用 SRAM
显存 → GPU 图形数据
虚拟内存 → OS 机制 + 后备存储

DOS/Windows：
copy  → 复制
move  → 移动
ren   → 重命名
del   → 删除文件
type  → 显示文本
tree  → 目录树
cls   → 清屏

Linux 目录：
/etc  → 配置
/dev  → 设备
/proc → 进程/内核信息
/var  → 变化数据、日志等
/tmp  → 临时文件
/home → 普通用户主目录

Linux 文本：
grep → 查内容
find → 找文件
cut  → 截字段
sort → 排序
uniq → 相邻去重
wc   → 统计

办公/压缩：
docx → Word
xlsx → Excel
pptx → PowerPoint
pdf  → 便携式文档
zip/rar/7z/gz → 压缩/归档相关

多媒体：
位图容量(bit) ≈ 宽 × 高 × 色深
PCM码率 = 采样率 × 量化位数 × 声道数
Byte = bit ÷ 8

I/O：
扫描仪 → 输入
打印机 → 输出
针式打印机 → 击打式/票据场景

历史介质：
3.5英寸高密度软盘 → 1.44 MB
CD/DVD → 光存储
磁带 → 顺序访问、备份/归档
```

> **今日一句话总结：** 今天重点补“性能指标—命令—目录—格式—容量公式—外设—历史介质”这类零散但非常适合选择题直接得分的知识，后续继续轮换未覆盖的基础杂项。