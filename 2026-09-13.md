# 国网计算机基础与实用知识晨练｜2026-09-13

> **今日主题：** HDD/SSD/光盘存储介质、FAT32/NTFS、Linux 权限、IEEE 802 系列定位、数字音频容量与码率、病毒/蠕虫/木马、防火墙与补丁、bit/Byte 容量单位、剪贴板/回收站/快捷方式  
> **今日目标：** 最近几天已覆盖主板芯片组、传统 I/O、Cache/显存/虚拟内存、BIOS/UEFI、DDR、PCIe、Windows 注册表与系统工具、Linux 进程/文本处理/用户、图像与办公格式等。今天主动切换到存储介质、文件系统、Linux 权限、IEEE 802 总览、数字音频和基础安全等方向，继续补齐“看一遍就能拿分”的基础杂项。

## 一、今日知识点

### 1. HDD、SSD、光盘：存储介质的原理与考试口径

| 介质 | 基本原理 | 典型特点 | 高频考点 |
|---|---|---|---|
| HDD 机械硬盘 | 磁性盘片 + 磁头 | 容量大、单位容量成本较低，有机械结构 | 有寻道、旋转等机械动作 |
| SSD 固态硬盘 | 通常使用 NAND Flash | 无机械运动部件、随机访问快、抗震性较好 | 不能把 SSD 说成“磁存储” |
| CD/DVD 等光盘 | 激光读写光学介质 | 便于分发、保存，现代 PC 使用减少 | 属于光存储 |
| U 盘 | 通常使用 Flash | 便携、非易失 | 属于半导体存储，不是磁盘 |

HDD 的访问涉及磁头定位等机械过程，因此传统性能指标中常见**寻道时间、旋转延迟**等概念。SSD 没有机械磁头和旋转盘片，因此随机访问延迟通常显著低于 HDD。

📌 **国网考点：**
- HDD → 磁存储；CD/DVD → 光存储；SSD/U 盘 → 半导体 Flash 存储。
- SSD 断电后数据通常仍可保存，因此属于**非易失性存储**。

⚠️ **易错提醒：** SSD 虽然日常也叫“固态硬盘”，但其主流实现不是磁盘原理；“硬盘”这个日常名称不能用来判断物理存储机制。

🧠 **速记：** `机械盘看磁，光盘看激光，SSD/U盘看 Flash。`

### 2. FAT32 与 NTFS：Windows 文件系统基础辨析

| 项目 | FAT32 | NTFS |
|---|---|---|
| 历史定位 | 较老、兼容性广 | 现代 Windows 常用文件系统 |
| 单文件大小 | 存在约 4 GiB 上限 | 支持远大于 4 GiB 的文件 |
| 权限/安全 | 功能较简单 | 支持访问控制权限等 |
| 日志 | 无 NTFS 式日志能力 | 支持日志，提高文件系统一致性恢复能力 |
| 常见场景 | 部分 U 盘、兼容设备 | Windows 系统盘及普通磁盘常见 |

**考试口径**常强调 NTFS 相比 FAT/FAT32 具有更强的安全性、可靠性和大文件支持能力。

例如，一个 8 GB 的单个视频文件即使 U 盘剩余空间足够，也不能直接存入 FAT32 分区，关键原因是 FAT32 的**单文件大小限制**，而不是“U 盘容量不够”。

📌 **国网考点：** 文件系统与存储设备不是同一概念。同一个 U 盘可以根据支持情况被格式化为不同文件系统。

⚠️ **易错提醒：** “FAT32 最大只能管理 4 GB 的磁盘”错误。约 4 GiB 是其著名的**单文件大小限制**，不要与分区容量混为一谈。

### 3. Linux r/w/x 与 chmod：文件权限和目录权限含义不同

Linux 常见权限分为三组：

```text
rwx rwx rwx
 u   g   o
```

- `u`：user，文件所有者
- `g`：group，所属组
- `o`：others，其他用户

数字表示：

| 权限 | 数值 |
|---|---:|
| r（read） | 4 |
| w（write） | 2 |
| x（execute） | 1 |

所以：

```bash
chmod 755 file
```

表示所有者 `rwx=7`，组用户 `r-x=5`，其他用户 `r-x=5`。

但权限作用于**普通文件**和**目录**时含义不能机械照搬：

| 权限 | 对普通文件 | 对目录 |
|---|---|---|
| r | 读取文件内容 | 列出目录项名称 |
| w | 修改文件内容 | 在相应条件下创建、删除、重命名目录项 |
| x | 执行文件 | 进入/遍历目录，访问其中对象 |

常见命令：

```bash
chmod 644 report.txt
chown alice report.txt
chgrp staff report.txt
```

📌 **国网考点：** `chmod` 改权限，`chown` 改所有者，`chgrp` 改所属组。

⚠️ **易错提醒：** 目录的 `x` 不是“执行目录”，而主要体现能否进入/遍历目录。

🧠 **速记：** `r=4，w=2，x=1；chmod 权限，chown 主人，chgrp 组。`

### 4. IEEE 802.1/802.2/802.3/802.5/802.11/802.15/802.16：先认门牌号

国网基础题常不要求深挖标准细节，但很喜欢考“编号—技术”的对应关系。

| IEEE 802 分支 | 传统教材/基础考试定位 | 关键词 |
|---|---|---|
| 802.1 | LAN/MAN 架构、桥接与管理等 | VLAN、桥接等常与其家族相关 |
| 802.2 | LLC 逻辑链路控制 | LLC |
| 802.3 | Ethernet 以太网 | 有线局域网 |
| 802.5 | Token Ring 令牌环网 | 历史技术 |
| 802.11 | WLAN 无线局域网 | Wi-Fi 相关 |
| 802.15 | WPAN 无线个域网 | Bluetooth 等相关技术领域 |
| 802.16 | 宽带无线城域网 | WiMAX |

其中 802.5 和 802.16 在现代日常使用中的存在感远低于以太网和 Wi-Fi，但仍具有**老教材与基础选择题价值**。

📌 **国网考点：** 最优先记住 `802.3 = Ethernet`、`802.11 = WLAN`、`802.15 = WPAN`、`802.16 = WiMAX/无线城域网`。

⚠️ **易错提醒：** 802.11 只是 IEEE 802 家族中的一个分支，不能把 IEEE 802 等同于 Wi-Fi。

🧠 **速记：** `3 以太，11 无局，15 个域，16 城域；5 令牌环。`

### 5. 数字音频：采样频率、采样位数、声道数与容量估算

未经压缩的 PCM 数字音频，基础容量估算公式：

```text
数据量(bit) = 采样频率(Hz) × 采样位数(bit) × 声道数 × 时间(s)
数据量(Byte) = 上式 ÷ 8
```

三个核心参数：

- **采样频率**：每秒对模拟声音取样多少次，单位 Hz。
- **采样位数**：每个采样点用多少 bit 表示，位数越高通常能表示更细的幅度层次。
- **声道数**：单声道为 1，双声道立体声通常为 2。

例：44.1 kHz、16 bit、双声道的未压缩音频，其原始码率约为：

```text
44100 × 16 × 2 = 1,411,200 bit/s
≈ 1411.2 kbit/s
```

📌 **国网考点：** 在其他条件不变时，采样频率、采样位数、声道数或时长翻倍，未压缩数据量也相应翻倍。

⚠️ **易错提醒：** MP3/AAC 通常经过有损压缩，不能直接用 PCM 原始容量公式推断其最终文件大小；WAV 是容器/文件格式，常见 PCM WAV 才适合直接按上述公式估算。

### 6. 病毒、蠕虫、木马：基础安全名词不要混

| 类型 | 典型特征 | 高频辨析 |
|---|---|---|
| 计算机病毒 | 常依附于其他程序/文件并传播、破坏 | 强调寄生、感染 |
| 蠕虫 | 可利用网络等自行复制传播 | 强调自我复制、网络扩散 |
| 木马 | 伪装成正常或有用程序，诱使用户运行，暗中执行恶意行为 | 强调伪装、欺骗 |

这些概念在现实恶意软件中可能组合出现，考试通常按其**典型特征**进行辨析。

例如：某恶意程序不需要附着宿主文件，能主动扫描网络并复制自身到其他主机，更符合“蠕虫”的典型描述。

📌 **国网考点：** 病毒 ≠ 所有恶意软件的严格同义词；木马也不以“必须自我复制”作为定义特征。

🧠 **速记：** `病毒爱寄生，蠕虫会自繁，木马靠伪装。`

### 7. 防火墙、补丁、杀毒与备份：四种措施解决的问题不同

| 措施 | 主要作用 | 不能简单替代什么 |
|---|---|---|
| 防火墙 | 按规则控制网络通信访问 | 不能保证主机绝不感染恶意软件 |
| 安全补丁 | 修复已知软件漏洞等问题 | 不是数据备份 |
| 杀毒/终端防护 | 检测、阻止或清除恶意软件 | 不能替代所有访问控制 |
| 备份 | 在数据丢失、损坏等情况下提供恢复副本 | 不负责阻止攻击发生 |

**纵深防御**的基本思想就是不要把所有希望寄托在一种措施上。

📌 **国网考点：** “安装防火墙后就不需要打补丁”“有杀毒软件就无需备份”都属于典型错误表述。

⚠️ **易错提醒：** 备份重点是**恢复能力**，不是攻击阻断能力。勒索软件场景下，可靠且隔离良好的备份尤其重要。

### 8. bit、Byte、KB/MB/GB/TB：单位换算与速率陷阱

最基本关系：

```text
1 Byte = 8 bit
```

传统计算机基础题常采用二进制换算口径：

```text
1 KB = 1024 B
1 MB = 1024 KB
1 GB = 1024 MB
1 TB = 1024 GB
```

但现代标准中严格区分十进制前缀和二进制前缀：

- `kB/MB/GB` 可按 SI 十进制前缀理解；
- `KiB/MiB/GiB` 明确表示 1024 进位。

**考试口径：** 若传统基础题没有额外说明，通常按 `1 KB = 1024 B` 计算；若题目明确给定换算规则，应严格按题干。

网络速率常写 `Mb/s`、`Gb/s`，这里小写 `b` 是 **bit**；文件容量常写 MB、GB，大写 `B` 是 **Byte**。

例如理论速率 80 Mb/s 若完全忽略协议开销等，仅做单位换算：

```text
80 Mb/s ÷ 8 = 10 MB/s
```

这不代表实际文件下载一定达到 10 MB/s，因为还有协议开销、链路状况等影响。

🧠 **速记：** `B 是字节，b 是位；8 bit = 1 Byte；网速常看 b，容量常看 B。`

### 9. 剪贴板、回收站、快捷方式：桌面系统“小题王”

| 概念 | 本质 | 易错点 |
|---|---|---|
| 剪贴板 | 用于复制/剪切与粘贴的数据交换区域 | 复制后原对象仍在；剪切通常用于移动语义 |
| 回收站 | Windows 对部分删除操作提供的暂存/恢复机制 | 并非所有删除都必然进入回收站 |
| 快捷方式 | 指向目标对象的链接/入口 | 删除快捷方式通常不等于删除目标文件 |

常见快捷键：

| 操作 | Windows 常见快捷键 |
|---|---|
| 复制 | `Ctrl + C` |
| 剪切 | `Ctrl + X` |
| 粘贴 | `Ctrl + V` |
| 撤销 | `Ctrl + Z` |
| 全选 | `Ctrl + A` |

📌 **国网考点：** 快捷方式本身和目标文件是两个对象。快捷方式失效可能只是目标被移动、删除或路径改变。

⚠️ **易错提醒：** 可移动磁盘、网络位置或使用永久删除等场景，不应笼统认为“删除的文件一定先进回收站”。

---

## 二、国网风格练习题

### 1. 下列存储介质中，主要依靠磁性盘片和磁头完成数据读写的是（ ）。

A. SSD  
B. HDD  
C. DVD  
D. U 盘

**答案：B**

**解析：** HDD 是典型磁存储设备，具有盘片、磁头等机械结构。SSD 和 U 盘主流使用 Flash 半导体存储；DVD 属于光存储。易错点是不要因 SSD 名称中也有“硬盘”二字，就认为它与 HDD 一样使用磁性盘片。

### 2. 某 FAT32 格式 U 盘剩余空间为 20 GB，但一个 6 GB 的单文件无法直接复制进去，最可能的原因是（ ）。

A. FAT32 存在约 4 GiB 的单文件大小限制  
B. U 盘不能保存视频文件  
C. FAT32 只能管理 4 GB 容量的磁盘  
D. USB 接口不支持大文件传输

**答案：A**

**解析：** FAT32 著名限制之一是单文件最大约 4 GiB。它不是说整个分区只能有 4 GB，也与 USB 接口是否能传大文件无关。题干特意说明“剩余空间 20 GB”，就是在排除总容量不足。

### 3. Linux 中执行 `chmod 754 test.sh` 后，文件所属组用户具有的权限是（ ）。

A. rwx  
B. r-x  
C. r--  
D. rw-

**答案：B**

**解析：** 754 分别对应所有者、组、其他用户。组权限为 5，即 `4+1=r+x`，写作 `r-x`。数字权限要牢记 r=4、w=2、x=1。

### 4. 对 Linux 目录权限的描述，正确的是（ ）。

A. 目录的 x 权限表示把目录作为程序执行  
B. 目录的 r 权限主要与列出目录项名称有关  
C. 目录的 w 权限等同于读取目录中文件内容  
D. 文件和目录的 r/w/x 含义完全相同

**答案：B**

**解析：** 对目录而言，r 主要涉及列出目录项，w 主要涉及创建、删除、重命名目录项，x 主要涉及进入/遍历目录。不能机械套用普通文件的“读、写、执行”解释。

### 5. IEEE 802.3 最典型对应的技术是（ ）。

A. Ethernet  
B. WLAN  
C. Token Ring  
D. WiMAX

**答案：A**

**解析：** 802.3 对应以太网；802.11 对应 WLAN；802.5 对应令牌环网；802.16 与宽带无线城域网/WiMAX 相关。这类“标准号—技术”是基础杂项中的高频记忆题。

### 6. 下列对应关系正确的是（ ）。

A. IEEE 802.11—令牌环网  
B. IEEE 802.15—无线个域网  
C. IEEE 802.16—以太网  
D. IEEE 802.5—Wi-Fi

**答案：B**

**解析：** 802.15 对应无线个域网（WPAN）方向。802.11 是 WLAN，802.16 是宽带无线城域网方向，802.5 是历史上的 Token Ring。注意本题考的是整个 802 家族，而非 Wi-Fi 代际细节。

### 7. 对未经压缩的 PCM 音频，在其他条件不变时，将声道数从单声道改为双声道，其数据量约变为原来的（ ）。

A. 1/2  
B. 不变  
C. 2 倍  
D. 4 倍

**答案：C**

**解析：** PCM 原始数据量与采样频率、采样位数、声道数、时间均成正比。单声道 1 个声道变为双声道 2 个声道，其他条件不变时数据量约翻倍。

### 8. 下列恶意软件中，典型特征是可通过网络等自行复制传播、不一定需要寄生于宿主文件的是（ ）。

A. 蠕虫  
B. 木马  
C. 普通快捷方式  
D. 设备驱动程序

**答案：A**

**解析：** 蠕虫强调自我复制和网络传播；传统病毒常强调寄生、感染其他文件或程序；木马强调伪装和欺骗。现实恶意程序可能同时具有多种特征，但基础题按典型定义判断。

### 9. 下列关于安全措施的说法，正确的是（ ）。

A. 安装防火墙后无需再安装系统补丁  
B. 有杀毒软件就无需进行数据备份  
C. 备份的核心价值之一是发生数据丢失或损坏后提供恢复手段  
D. 防火墙可以保证主机绝不会感染恶意软件

**答案：C**

**解析：** 防火墙、补丁、终端防护和备份解决的问题不同，不能互相完全替代。备份主要提升数据恢复能力；防火墙主要控制网络访问；补丁用于修复漏洞等问题。

### 10. 若只做理论单位换算且忽略一切协议开销，80 Mb/s 相当于（ ）。

A. 5 MB/s  
B. 10 MB/s  
C. 80 MB/s  
D. 640 MB/s

**答案：B**

**解析：** 1 Byte = 8 bit，所以 `80 Mb/s ÷ 8 = 10 MB/s`。但现实下载速度还受协议开销、网络拥塞、服务器性能等因素影响，不能把理论换算值当成实际保证值。

### 11. 在 Windows 中，删除一个指向某文档的桌面快捷方式，通常会（ ）。

A. 同时删除目标文档内容  
B. 只删除快捷方式，目标文档仍然存在  
C. 自动格式化目标文档所在磁盘  
D. 把目标文档转换为隐藏文件

**答案：B**

**解析：** 快捷方式只是指向目标对象的入口。删除快捷方式通常不会删除目标文件本身。这是桌面系统常见的概念辨析题。

### 12. 【滚动复习】现代 PC 中，过去传统北桥承担的部分功能已被集成进（ ）。

A. CPU  
B. 打印机  
C. 显示器  
D. 键盘控制器

**答案：A**

**解析：** 现代处理器常集成内存控制器及部分 PCIe 等过去与北桥密切相关的功能。传统教材仍可记“北桥管高速、南桥管 I/O”，但现代实现已发生显著变化。

### 13. 【滚动复习】Linux 中用于动态观察进程及系统资源使用情况的常见命令是（ ）。

A. tail  
B. top  
C. type  
D. tree

**答案：B**

**解析：** `top` 动态显示进程和系统资源；`tail` 查看文本尾部；Windows/DOS 中 `type` 可显示文本内容；`tree` 常用于显示目录树。该题复习昨日的 Linux 进程管理内容。

### 14. 【滚动复习】需要保存照片且希望获得较高压缩率时，最常见的图像格式是（ ）。

A. JPEG  
B. TXT  
C. WAV  
D. EXE

**答案：A**

**解析：** JPEG 采用有损压缩，特别适合照片等连续色调图像。PNG 常用于需要无损压缩和透明背景的图形；GIF 常见于简单动画。该题用于间隔复习图像格式。

### 15. 【滚动复习】下列接口中，传统教材常用于连接老式打印机的是（ ）。

A. 并口（LPT）  
B. PS/2  
C. VGA  
D. RJ-45 只能连接打印机

**答案：A**

**解析：** 传统 PC 中并口（LPT）常用于打印机，串口（COM）常见于调制解调器、工业设备等。现代打印机更多使用 USB 或网络，因此要区分“历史考试口径”和“现代实际使用”。

---

## 三、今日速记

### 1. 存储介质一眼认

| 关键词 | 对应 |
|---|---|
| 磁性盘片 + 磁头 | HDD |
| NAND Flash、无机械部件 | SSD |
| 激光读写 | CD/DVD 光盘 |
| Flash、便携 | U 盘 |

**口诀：** `HDD 磁，光盘光，SSD/U盘 Flash 忙。`

### 2. FAT32 vs NTFS

- FAT32：兼容广，**单文件约 4 GiB 上限**。
- NTFS：Windows 常用，支持权限、日志、大文件。
- 文件系统 ≠ 存储设备；U 盘也可以使用不同文件系统。

### 3. Linux 权限

```text
r = 4
w = 2
x = 1
755 = rwx r-x r-x
644 = rw- r-- r--
```

- `chmod`：改权限
- `chown`：改所有者
- `chgrp`：改组
- 目录 `x`：进入/遍历，不是“执行目录程序”。

### 4. IEEE 802 门牌号

```text
802.1  → 桥接/管理等 LAN/MAN 基础
802.2  → LLC
802.3  → Ethernet
802.5  → Token Ring（历史）
802.11 → WLAN
802.15 → WPAN
802.16 → 无线城域网 / WiMAX
```

**口诀：** `3 以太，5 令牌，11 无局，15 个域，16 城域。`

### 5. PCM 音频公式

```text
数据量(bit)
= 采样频率 × 采样位数 × 声道数 × 时间
```

- 单声道 → 双声道：其他不变，数据量 ×2。
- MP3/AAC 有压缩，不能直接把 PCM 公式当最终文件大小。

### 6. 恶意软件三分法

```text
病毒：寄生 / 感染
蠕虫：自我复制 / 网络扩散
木马：伪装 / 欺骗用户运行
```

### 7. 四种安全措施

```text
防火墙 → 控制网络访问
补丁   → 修复漏洞
杀毒   → 检测/阻止恶意软件
备份   → 出事后恢复数据
```

**结论：** 四者不能互相完全替代。

### 8. bit 与 Byte

```text
1 Byte = 8 bit
传统题：1 KB = 1024 B
        1 MB = 1024 KB
        1 GB = 1024 MB
```

- `Mb/s`：兆比特每秒
- `MB/s`：兆字节每秒
- 理论 `80 Mb/s ÷ 8 = 10 MB/s`，实际速度通常还受其他因素影响。

### 9. 桌面三小件

- `Ctrl+C` 复制，`Ctrl+X` 剪切，`Ctrl+V` 粘贴。
- 快捷方式只是入口，删快捷方式通常不删目标。
- 回收站不是所有删除操作的必经之路。

> **考前 30 秒速扫：** `HDD=磁，SSD=Flash；FAT32 单文件约 4 GiB；chmod 权限/chown 主人；802.3 以太、802.11 WLAN；PCM 容量=频率×位数×声道×时间；病毒寄生、蠕虫自繁、木马伪装；1B=8b；快捷方式≠目标文件。`
