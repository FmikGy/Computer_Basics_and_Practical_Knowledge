# 国网计算机基础与实用知识晨练｜2026-09-02

> **今日主题：** IEEE 802 与 Wi‑Fi 代际、传统 PC 主板架构、BIOS/CMOS/UEFI、PCI/PCIe、Linux/UNIX 文件与权限、Windows 常见文件类型  
> **今日目标：** 把“知道就能拿分”的杂项知识整理成可直接秒选的对应关系，同时分清传统教材口径与现代实现。

---

## 一、今日知识点

### 1. IEEE 802 系列：先把“大类”记牢

IEEE 802 主要规定局域网和城域网相关标准。国网老题最常考下面几组：

| 标准 | 典型含义 | 速记 |
|---|---|---|
| **IEEE 802.3** | Ethernet，以太网 | **3 = 以太网** |
| **IEEE 802.11** | WLAN，无线局域网 | **11 = Wi‑Fi** |
| **IEEE 802.15** | WPAN，无线个人区域网 | **15 = 个人** |
| **IEEE 802.16** | WMAN，无线城域网，典型技术 WiMAX | **16 = 城域** |

> ⚠️ **易错提醒**：802.15 不是“无线广域网”；802.16 也不是“无线个人区域网”。

老题里还可能遇到：

- **802.15.1**：历史上与 Bluetooth 相关，很多旧教材直接写“蓝牙标准”。
- **802.15.4**：低速率无线个人区域网（LR-WPAN），Zigbee 等技术常建立在其物理层/MAC 基础上。
- **802.16**：WiMAX，强调宽带无线城域网。

---

### 2. IEEE 802.11a/b/g/n/ac/ax/be：国网最值得扩展的一张表

| 标准 | 常见频段 | 理论最高速率（常见教材口径） | 关键技术/特点 | Wi‑Fi 代际 |
|---|---|---:|---|---|
| **802.11a** | 5 GHz | 54 Mb/s | OFDM；频率高、干扰相对少 | — |
| **802.11b** | 2.4 GHz | 11 Mb/s | DSSS/CCK；覆盖较好但速率低 | — |
| **802.11g** | 2.4 GHz | 54 Mb/s | OFDM；兼容 802.11b | — |
| **802.11n** | 2.4/5 GHz | 600 Mb/s | **MIMO**、信道绑定 | Wi‑Fi 4 |
| **802.11ac** | 5 GHz | 约 6.9 Gb/s | 80/160 MHz、MU‑MIMO 等 | Wi‑Fi 5 |
| **802.11ax** | 2.4/5 GHz；6 GHz 对应 Wi‑Fi 6E | 约 9.6 Gb/s | **OFDMA、MU‑MIMO、1024-QAM、BSS Coloring** | Wi‑Fi 6/6E |
| **802.11be** | 2.4/5/6 GHz | 可达数十 Gb/s | **320 MHz、MLO、4096-QAM** | Wi‑Fi 7 |

#### 高频辨析

- **a 和 ac 主要工作在 5 GHz。**
- **b/g 主要在 2.4 GHz。**
- **n 同时支持 2.4 GHz 和 5 GHz。**
- **ax 并不是“只在 6 GHz”**；Wi‑Fi 6 是 2.4/5 GHz，扩展到 6 GHz 后通常称 **Wi‑Fi 6E**。
- **Wi‑Fi 6E 不是一个新的 IEEE 字母后缀标准**，本质仍是 802.11ax 在 6 GHz 频段的扩展使用。
- **Wi‑Fi 7 = 802.11be**，最有辨识度的关键词是 **MLO（Multi-Link Operation，多链路操作）**。

#### 速记

```text
b：2.4G，11M
 a：5G，54M
 g：2.4G，54M
 n：双频 + MIMO
ac：5G + 更宽信道
ax：OFDMA + 高并发
be：MLO + 320MHz
```

---

### 3. BIOS、CMOS、UEFI：程序、参数、现代固件要分开

#### BIOS

BIOS（Basic Input/Output System）是传统 PC 固件，典型职责：

1. 上电自检（POST）
2. 初始化硬件
3. 提供基本 I/O 服务
4. 寻找启动设备并引导操作系统

#### CMOS

传统教材常说：

> **BIOS 是程序，CMOS 保存 BIOS 设置参数。**

例如：

- 系统时间
- 启动顺序
- 部分硬件配置参数

这里的 CMOS 原本是指一种低功耗存储/电路技术。现代主板实现已变化，但国网老题通常仍按上述传统口径回答。

#### UEFI

UEFI 是现代固件接口，相比传统 BIOS 具有：

- 更灵活的启动机制
- 支持 GPT 大容量磁盘启动
- 图形化界面和更丰富驱动支持
- 可支持 Secure Boot

> ⚠️ **考试口径**：题目如果明确说“传统 PC/传统 BIOS”，按 BIOS+CMOS 的经典模型答；不要用现代 UEFI 实现去否定老教材。

---

### 4. 北桥与南桥：这是“传统主板架构”

传统 PC 主板芯片组常分成北桥和南桥：

```text
                 CPU
                  │
                北桥
          ┌───────┴───────┐
        内存            高速图形接口
                  │
                南桥
       ┌──────────┼──────────┐
      PCI        USB       SATA/IDE 等
```

| 芯片 | 传统职责 | 关键词 |
|---|---|---|
| **北桥** | CPU、内存、显卡/高速总线 | **高速** |
| **南桥** | PCI、USB、磁盘、声卡、网卡等 I/O | **低速/外设** |

口诀：**北快南慢**。

#### 现代架构变化

今天大量北桥功能已经被集成进 CPU，例如：

- 内存控制器
- PCIe 控制器

Intel 平台常见的 **PCH**（Platform Controller Hub）承担的很多职责更接近传统南桥。

> ⚠️ 老题问“主板芯片组通常由哪两部分组成”，标准答案一般仍是：**北桥芯片 + 南桥芯片**。

---

### 5. PCI 与 PCIe：最容易考“并行/串行、共享/点对点”

| 对比项 | PCI | PCI Express（PCIe） |
|---|---|---|
| 传输方式 | **并行** | **高速串行** |
| 拓扑 | 多设备共享总线 | **点对点**连接 |
| 带宽使用 | 多设备竞争共享 | 每个链路独享 |
| 常见标识 | 传统 PCI 插槽 | x1、x4、x8、x16 |
| 现代 PC 地位 | 基本淘汰 | 主流扩展总线 |

PCIe 中的：

```text
x1 / x4 / x8 / x16
```

表示 **Lane（通道）数量**，并不表示 PCIe 版本。

例如：

- PCIe 4.0 x16
- PCIe 5.0 x4

“4.0/5.0”是代际，“x16/x4”是通道数。

> 📌 国网传统题问“支持 PCI 总线结构的是谁”，通常答 **主板芯片组**，而不是 CPU、显卡或系统软件。

---

### 6. UNIX/Linux 的“一切皆文件”与文件类型

UNIX/Linux 把很多系统资源统一抽象成文件接口，设备文件通常位于：

```bash
/dev
```

常见类型可以从 `ls -l` 最前面的字符判断：

| 首字符 | 类型 |
|---|---|
| `-` | 普通文件 |
| `d` | 目录 |
| `l` | 符号链接 |
| `c` | 字符设备 |
| `b` | 块设备 |
| `p` | 命名管道 |
| `s` | Socket |

例如：

- 终端设备常属于字符设备
- 磁盘设备常属于块设备

旧教材经常把设备文件统称为：**特殊文件**。

---

### 7. Linux 权限：文件和目录中的 r/w/x 含义并不完全一样

数字权限：

```text
r = 4
w = 2
x = 1
```

典型组合：

| 数字 | 权限 |
|---:|---|
| 7 | rwx |
| 6 | rw- |
| 5 | r-x |
| 4 | r-- |

例如：

```text
644 = rw-r--r--
755 = rwxr-xr-x
444 = r--r--r--
```

三组依次表示：

```text
所有者(owner) | 所属组(group) | 其他用户(others)
```

#### 对普通文件

- `r`：读取文件内容
- `w`：修改文件内容
- `x`：执行文件

#### 对目录

- `r`：列出目录中的文件名
- `w`：在目录中创建、删除、重命名目录项
- `x`：进入/遍历目录，访问目录中的对象

> ⚠️ **易错点**：目录的 `x` 很关键。只有 `r` 而没有 `x`，即使能看到部分目录项，也无法正常访问其中对象。

常见命令：

```bash
chmod 755 file
chmod u+x script.sh
chmod g-w file
```

---

### 8. Linux 用户管理与文本处理：`userdel -r`、`tr` 都是老题常客

#### 删除用户

```bash
userdel user
```

删除用户账号。

```bash
userdel -r user
```

删除用户账号，并删除其主目录、邮件目录等相关用户文件。

#### 字符转换 `tr`

`tr` 从标准输入读入字符并进行转换，再写到标准输出。

例如：

```bash
echo "abc" | tr 'a-z' 'A-Z'
```

输出：

```text
ABC
```

常见命令不要混：

| 命令 | 典型作用 |
|---|---|
| `tr` | 字符转换/删除 |
| `grep` | 文本匹配查找 |
| `sort` | 排序 |
| `uniq` | 去除相邻重复行/统计 |
| `wc` | 统计行数、单词数、字节数等 |
| `ps` | 查看进程 |
| `pwd` | 显示当前工作目录 |

---

### 9. Windows/DOS 常见文件扩展名：典型“见过即拿分”

| 扩展名 | 常见含义 |
|---|---|
| `.exe` | 可执行程序 |
| `.com` | DOS 时代常见可执行文件 |
| `.bat` | 批处理文件 |
| `.dll` | 动态链接库 |
| `.sys` | 系统/驱动相关文件 |
| `.ini` | 配置文件 |
| `.reg` | Windows 注册表导入/导出文件 |

速记：

```text
REG = Registry
BAT = Batch
DLL = Dynamic Link Library
```

---

## 二、国网风格练习题

### 1. IEEE 802.15 主要对应的网络类型是（ ）。

A. 以太网  
B. 无线局域网  
C. 无线个人区域网  
D. 无线城域网

**答案：C**

**解析：** IEEE 802.15 对应 WPAN（Wireless Personal Area Network，无线个人区域网）。802.3 是以太网，802.11 是 WLAN，802.16 是 WMAN。国网很喜欢在 802.15 与 802.16 之间做互换陷阱。

---

### 2. 下列 IEEE 802.11 标准中，理论最高速率为 11 Mb/s、主要工作在 2.4 GHz 的是（ ）。

A. 802.11a  
B. 802.11b  
C. 802.11g  
D. 802.11n

**答案：B**

**解析：** 802.11b 的经典教材指标是 2.4 GHz、最高 11 Mb/s。802.11a 为 5 GHz/54 Mb/s；802.11g 为 2.4 GHz/54 Mb/s；802.11n 支持 2.4/5 GHz，并通过 MIMO 等技术大幅提升速率。

---

### 3. Wi‑Fi 5 对应的 IEEE 标准是（ ）。

A. 802.11n  
B. 802.11ac  
C. 802.11ax  
D. 802.11be

**答案：B**

**解析：** Wi‑Fi 4 = 802.11n，Wi‑Fi 5 = 802.11ac，Wi‑Fi 6 = 802.11ax，Wi‑Fi 7 = 802.11be。这个“代际—标准”对应关系很适合作为直接记忆题。

---

### 4. 下列关于 Wi‑Fi 6E 的说法，正确的是（ ）。

A. 它是独立于 802.11ax 的新 IEEE 标准  
B. 它本质上仍采用 802.11ax，并扩展到 6 GHz 频段  
C. 它只支持 2.4 GHz 频段  
D. 它等同于 802.11be

**答案：B**

**解析：** Wi‑Fi 6E 本质仍是 Wi‑Fi 6/802.11ax 技术，区别在于扩展使用 6 GHz 频段。802.11be 对应 Wi‑Fi 7。考试中不要把“6E”误认为新的 IEEE 字母后缀。

---

### 5. Wi‑Fi 7（802.11be）最有代表性的技术之一是（ ）。

A. 仅使用 DSSS  
B. MLO 多链路操作  
C. 只能使用 2.4 GHz  
D. 最高信道宽度只有 20 MHz

**答案：B**

**解析：** 802.11be/Wi‑Fi 7 的重要特征包括 MLO、最高 320 MHz 信道、4096-QAM 等。DSSS 是早期 802.11b 的典型技术之一。

---

### 6. 在传统 PC 主板架构中，负责 CPU、内存以及高速图形接口之间通信的通常是（ ）。

A. 南桥  
B. 北桥  
C. BIOS  
D. CMOS

**答案：B**

**解析：** 传统教材中北桥负责高速部分，如 CPU、内存和显卡；南桥负责 USB、PCI、磁盘等较低速 I/O。现代 CPU 已集成许多北桥功能，但老题仍按“北桥+南桥”答。

---

### 7. 下列关于 BIOS 与 CMOS 的传统教材表述，正确的是（ ）。

A. BIOS 主要用于保存用户文档  
B. CMOS 是操作系统的一部分  
C. BIOS 是固件程序，CMOS 传统上保存 BIOS 配置参数  
D. CMOS 与系统时间、启动设置毫无关系

**答案：C**

**解析：** 传统考试口径是“BIOS 是程序，CMOS 保存配置参数”。现代 UEFI/NVRAM 的实现方式有所变化，但遇到传统 BIOS 题仍按经典概念答。

---

### 8. 下列关于 PCI 与 PCIe 的说法，正确的是（ ）。

A. PCIe 是并行共享总线  
B. PCI 是高速串行点对点总线  
C. PCIe 采用高速串行、点对点结构  
D. PCIe x16 中的 16 表示 PCIe 第 16 代

**答案：C**

**解析：** PCI 是传统并行共享总线；PCIe 是高速串行、点对点。`x16` 指 16 条 Lane，不是版本号。PCIe 版本写作 3.0、4.0、5.0、6.0 等。

---

### 9. 在 UNIX/Linux 中，磁盘设备通常属于（ ）。

A. 普通文件  
B. 块设备文件  
C. 字符设备文件  
D. 目录文件

**答案：B**

**解析：** 磁盘等按块进行 I/O 的设备通常是块设备（`b`）；终端等流式处理的设备常见为字符设备（`c`）。旧教材把这些统一称为“特殊文件”。

---

### 10. Linux 文件权限 `755` 对应的是（ ）。

A. `rwxr-xr-x`  
B. `rw-r--r--`  
C. `r--r--r--`  
D. `rwxrwxrwx`

**答案：A**

**解析：** 7=4+2+1=`rwx`，5=4+1=`r-x`，因此 `755 = rwxr-xr-x`。B 是 644，C 是 444，D 是 777。

---

### 11. 删除 Linux 用户 `user`，同时删除其主目录，正确命令是（ ）。

A. `userdel user`  
B. `userdel -r user`  
C. `rm -u user`  
D. `deluser -p user`

**答案：B**

**解析：** `userdel -r user` 在删除账户的同时删除其主目录及相关用户文件。单独 `userdel user` 通常不删除主目录。

---

### 12. 下列命令中，主要用于把标准输入中的一个字符集合转换为另一个字符集合的是（ ）。

A. `grep`  
B. `tr`  
C. `ps`  
D. `pwd`

**答案：B**

**解析：** `tr` 用于字符转换或删除，例如 `tr 'a-z' 'A-Z'`。`grep` 查找匹配文本，`ps` 查看进程，`pwd` 显示当前工作目录。

---

### 13. Windows 注册表导出文件通常采用的扩展名是（ ）。

A. `.bat`  
B. `.sys`  
C. `.reg`  
D. `.dll`

**答案：C**

**解析：** `.reg` 来自 Registry；`.bat` 是批处理；`.dll` 是动态链接库；`.sys` 通常与系统或驱动相关。

---

### 14. 下列 IEEE 标准与技术的对应关系中，错误的是（ ）。

A. 802.3 —— Ethernet  
B. 802.11 —— WLAN  
C. 802.15 —— WPAN  
D. 802.16 —— WLAN

**答案：D**

**解析：** 802.16 对应 WMAN（无线城域网），WiMAX 是典型技术。WLAN 对应 802.11。

---

## 三、今日速记

### 1. IEEE 802 核心对应

```text
802.3  → Ethernet
802.11 → WLAN / Wi‑Fi
802.15 → WPAN
802.16 → WMAN / WiMAX
```

### 2. Wi‑Fi 代际

```text
802.11n  → Wi‑Fi 4
802.11ac → Wi‑Fi 5
802.11ax → Wi‑Fi 6
802.11ax + 6GHz → Wi‑Fi 6E
802.11be → Wi‑Fi 7
```

### 3. a/b/g/n/ac/ax 速记

```text
b：2.4G / 11M
 a：5G   / 54M
 g：2.4G / 54M
 n：2.4G + 5G，MIMO
ac：5G，更宽信道
ax：OFDMA，高并发
be：MLO，320MHz
```

### 4. 传统 PC 架构

```text
北桥：CPU / 内存 / 显卡 → 高速
南桥：PCI / USB / 磁盘等 → I/O

BIOS：固件程序
CMOS：传统上保存 BIOS 设置
UEFI：现代固件接口
```

### 5. PCI 与 PCIe

```text
PCI  = 并行 + 共享总线
PCIe = 串行 + 点对点
x1/x4/x8/x16 = Lane 数，不是版本号
```

### 6. Linux 权限

```text
r=4, w=2, x=1
7=rwx
6=rw-
5=r-x
4=r--

644 = rw-r--r--
755 = rwxr-xr-x
444 = r--r--r--
777 = rwxrwxrwx
```

### 7. Linux/UNIX 命令

```text
userdel -r → 删除用户及主目录等
tr         → 字符转换
grep       → 文本匹配
ps         → 查看进程
pwd        → 当前目录
```

### 8. Windows 常见扩展名

```text
.reg → 注册表
.bat → 批处理
.dll → 动态链接库
.sys → 系统/驱动相关
.ini → 配置文件
.exe → 可执行文件
```

---

> **今日一句话总结：** 国网这类杂项题的关键不是深挖原理，而是建立“标准号—技术、命令—作用、扩展名—文件类型、传统硬件—职责”的快速对应关系。
