# 国网计算机基础与实用知识晨练｜2026-09-02

> **今日主题：** 微机硬件 + Windows/DOS + Linux/UNIX + 文件格式/编码 + 多媒体 + 计算机应用基础 + 网络设备杂项  
> **今日目标：** 不围绕某一个细分专题深挖，而是尽可能扩大“408之外、见过就能拿分”的基础杂项覆盖面。

---

## 一、今日知识点

### 1. BIOS、CMOS、UEFI、POST：四个概念一次分清

| 概念 | 核心含义 | 国网常见考法 |
|---|---|---|
| **BIOS** | 传统 PC 的基本输入输出固件 | 问“负责上电初始化、引导启动的程序” |
| **CMOS** | 传统教材中用于保存 BIOS 设置参数 | 问“系统时间、启动顺序等参数保存在哪里” |
| **UEFI** | 现代主流固件接口 | 问“传统 BIOS 的现代替代方案” |
| **POST** | Power-On Self-Test，上电自检 | 问“开机时检测内存、键盘等硬件的过程” |

传统启动思路可以记成：

```text
上电 → POST → 固件初始化 → 查找启动设备 → 装入操作系统
```

> 📌 **国网考点：** 老题里常用“BIOS 是程序，CMOS 保存参数”的教材口径。
>
> ⚠️ **现代情况：** 现代机器大量使用 UEFI，CMOS 的实际实现也已经变化，但做传统教材题时仍按经典定义作答。

---

### 2. HDD、SSD、SATA、NVMe：设备和接口/协议不要混

首先区分“存储设备”和“连接接口/协议”。

| 名称 | 本质 | 关键特点 |
|---|---|---|
| **HDD** | 机械硬盘 | 磁性盘片、机械寻道 |
| **SSD** | 固态硬盘 | 闪存，无机械部件 |
| **SATA** | 存储接口/协议体系 | 常见于 HDD、2.5 英寸 SATA SSD |
| **NVMe** | 面向非易失性存储的协议 | 常运行在 PCIe 之上，低延迟、高并发 |

容易混淆的一点：

> **M.2 是外形/接口规格之一，不等于 NVMe。**

M.2 SSD 既可能使用 SATA，也可能使用 PCIe/NVMe。

🧠 **速记：**

```text
HDD/SSD → “是什么设备”
SATA/NVMe → “怎么通信”
M.2      → “长什么样/怎么插”
```

---

### 3. PCI、PCIe、AGP：老题常见扩展总线

| 对比项 | PCI | PCIe | AGP |
|---|---|---|---|
| 典型时代 | 较老 | 现代主流 | 较老 |
| 传输方式 | 并行 | 高速串行 | 面向图形加速 |
| 连接方式 | 共享总线 | 点对点链路 | 主要服务显卡 |
| 常见用途 | 网卡、声卡等 | 显卡、网卡、NVMe 等 | 老式独立显卡 |

PCIe 常见：

```text
x1 / x4 / x8 / x16
```

这里的 `xN` 表示 **Lane（通道）数量**，不是 PCIe 版本。

例如：

```text
PCIe 4.0 x16
```

- `4.0`：代际
- `x16`：16 条 Lane

> ⚠️ **考试口径：** AGP 已基本淘汰，但老题问“专用于图形显示的局部总线”时，往往就是 AGP。

---

### 4. Windows/DOS 常见扩展名与命令：典型记忆分

#### 常见文件扩展名

| 扩展名 | 常见含义 |
|---|---|
| `.exe` | 可执行程序 |
| `.com` | DOS 时代常见可执行文件 |
| `.bat` | 批处理文件 |
| `.cmd` | Windows 命令脚本 |
| `.dll` | 动态链接库 |
| `.sys` | 系统/驱动相关文件 |
| `.ini` | 配置文件 |
| `.reg` | Windows 注册表导入/导出文件 |
| `.lnk` | Windows 快捷方式 |
| `.msi` | Windows Installer 安装包 |

#### 常见 DOS/Windows 命令

| 命令 | 作用 |
|---|---|
| `dir` | 列出目录内容 |
| `cd` | 切换目录 |
| `copy` | 复制文件 |
| `del` | 删除文件 |
| `ren` | 重命名 |
| `type` | 显示文本文件内容 |
| `cls` | 清屏 |
| `tree` | 树形显示目录结构 |
| `ipconfig` | 查看 Windows IP 配置 |
| `tracert` | 跟踪到目标的路由路径 |

🧠 **速记：** `REG = Registry`，`BAT = Batch`，`DLL = Dynamic Link Library`。

---

### 5. FAT32 与 NTFS：Windows 文件系统基础

| 对比项 | FAT32 | NTFS |
|---|---|---|
| 历史 | 较老 | Windows NT 系列主流 |
| 权限控制 | 弱 | 支持 ACL 权限控制 |
| 单文件大小 | 经典限制约 4 GB | 可支持远大于 4 GB 的文件 |
| 日志功能 | 无完整日志机制 | 支持日志机制 |
| 常见场景 | U 盘、兼容性场景 | Windows 系统盘、现代磁盘 |

> 📌 **国网考点：** 若问“支持文件权限、日志、较强安全性的 Windows 文件系统”，通常选 **NTFS**。

> ⚠️ **易错提醒：** FAT32 的“4 GB”是单个文件大小的经典上限，不是整个分区只能 4 GB。

---

### 6. Linux/UNIX 文件类型与常见目录

UNIX/Linux 的经典思想之一：

> **很多系统资源都可以通过文件接口访问。**

使用 `ls -l` 时，第一位字符常可判断类型：

| 首字符 | 类型 |
|---|---|
| `-` | 普通文件 |
| `d` | 目录 |
| `l` | 符号链接 |
| `c` | 字符设备 |
| `b` | 块设备 |
| `p` | 命名管道 |
| `s` | Socket |

常见目录：

| 目录 | 典型用途 |
|---|---|
| `/home` | 普通用户主目录 |
| `/etc` | 系统配置文件 |
| `/dev` | 设备文件 |
| `/var` | 经常变化的数据，如日志 |
| `/tmp` | 临时文件 |
| `/proc` | 进程与内核信息的虚拟文件系统 |
| `/usr` | 大量用户级程序、库和共享数据 |

> 📌 **老题口径：** UNIX 中设备常被称为“特殊文件”。

---

### 7. Linux 文本处理命令：grep、find、tr、wc、sort、uniq

| 命令 | 核心作用 | 常见混淆 |
|---|---|---|
| `grep` | 按模式搜索文本 | 不是按文件名查找 |
| `find` | 在目录树中查找文件 | 可按名称、类型、时间等条件 |
| `tr` | 字符转换/删除 | 典型是大小写转换 |
| `wc` | 统计行、词、字节/字符等 | `wc -l` 常用于统计行数 |
| `sort` | 行排序 | 默认是按文本规则排序 |
| `uniq` | 处理相邻重复行 | 常与 `sort` 搭配 |

示例：

```bash
echo "abc" | tr 'a-z' 'A-Z'
```

输出：

```text
ABC
```

```bash
wc -l file.txt
```

统计文件行数。

> ⚠️ **易错提醒：** `uniq` 主要处理**相邻**重复行，所以经常先 `sort` 再 `uniq`。

---

### 8. ASCII、Unicode、UTF-8、GB2312、GBK：字符集与编码基础

这部分最重要的是先区分：

> **Unicode 更像是“字符编号体系”，UTF-8/UTF-16 是 Unicode 的具体编码方式。**

| 名称 | 重点 |
|---|---|
| **ASCII** | 早期英文字符编码，标准 ASCII 使用 7 位表示 128 个字符 |
| **Unicode** | 为全球字符统一分配码点 |
| **UTF-8** | Unicode 的可变长编码，ASCII 字符仍占 1 字节 |
| **UTF-16** | Unicode 编码方式之一，常以 16 位代码单元为基础 |
| **GB2312** | 较早的简体中文字符集/编码标准 |
| **GBK** | 对 GB2312 的扩展，覆盖更多汉字和符号 |

📌 **国网常见考法：**

- 问 ASCII 标准码使用多少位：**7 位**。
- 问 UTF-8 是否固定每个字符 8 位：**不是**，它是可变长编码。
- 问 GBK 与 GB2312：GBK 覆盖范围更大。

---

### 9. 图像、音频、多媒体：三个最常见容量概念

#### 图像

未压缩位图的粗略容量：

```text
容量 ≈ 水平像素 × 垂直像素 × 色深 ÷ 8
```

例如 1024×768、24 位真彩色图像：

```text
1024 × 768 × 24 ÷ 8 Byte
```

实际文件还可能有文件头、对齐、压缩等因素。

#### 音频

未压缩 PCM 音频的粗略码率：

```text
采样频率 × 量化位数 × 声道数
```

例如：

```text
44.1 kHz × 16 bit × 2 声道
```

约为 1411.2 kb/s。

#### 常见格式

| 格式 | 类型 | 典型特点 |
|---|---|---|
| BMP | 图像 | 常见为未压缩或较少压缩，体积大 |
| JPEG | 图像 | 有损压缩，适合照片 |
| PNG | 图像 | 无损压缩，支持透明 |
| GIF | 图像 | 支持简单动画，颜色数有限 |
| WAV | 音频容器/格式 | 常用于未压缩 PCM 音频 |
| MP3 | 音频 | 有损压缩 |
| MP4 | 多媒体容器 | 可封装视频、音频等 |

> ⚠️ **易错提醒：** MP4 是容器格式之一，不等于“某一种固定视频编码”。

---

### 10. 系统软件、应用软件、编译程序、解释程序

#### 软件分类

| 类别 | 例子 |
|---|---|
| **系统软件** | 操作系统、设备驱动、语言处理程序等 |
| **应用软件** | Office、浏览器、图像处理软件等 |
| **工具软件** | 压缩、备份、磁盘维护等工具 |

#### 编译与解释

| 对比项 | 编译 | 解释 |
|---|---|---|
| 基本方式 | 通常先整体翻译，再运行目标程序 | 边读取边解释执行 |
| 是否生成独立目标程序 | 通常会 | 通常不会形成传统意义上的独立目标程序 |
| 典型特点 | 运行阶段通常较快 | 调试和交互性较方便 |

此外：

- **汇编程序**：把汇编语言翻译为机器代码。
- **链接程序**：把目标模块、库等链接成可执行程序。
- **装入程序**：把程序装入内存准备执行。

🧠 **速记：**

```text
编译：先翻译，再运行
解释：边翻译，边执行
链接：把模块“拼起来”
装入：把程序“放进内存”
```

---

## 二、国网风格练习题

### 1. 传统 PC 中，开机时首先进行硬件自检的过程通常称为（ ）。

A. POST  
B. FTP  
C. RAID  
D. DHCP

**答案：A**

**解析：** POST 是 Power-On Self-Test，即上电自检。它在系统启动初期检查和初始化基本硬件。FTP 是文件传输协议，RAID 是磁盘阵列技术，DHCP 用于动态分配网络参数。

---

### 2. 下列关于 NVMe 的说法正确的是（ ）。

A. NVMe 是一种机械硬盘结构  
B. NVMe 通常运行在 PCIe 之上  
C. NVMe 与 M.2 完全等价  
D. SATA SSD 一定使用 NVMe

**答案：B**

**解析：** NVMe 是面向非易失性存储的协议，现代 NVMe SSD 通常通过 PCIe 通信。M.2 是外形/接口规格之一，并不等于 NVMe；M.2 设备也可能使用 SATA。

> ⚠️ 易错：**M.2 ≠ NVMe**。

---

### 3. 在传统 PC 中，主要面向图形加速卡设计的总线是（ ）。

A. AGP  
B. USB  
C. SATA  
D. PS/2

**答案：A**

**解析：** AGP（Accelerated Graphics Port）是历史上专门面向图形显示/显卡的接口技术。现代显卡主要使用 PCIe，但国网老题若出现“图形加速端口”，通常选 AGP。

---

### 4. Windows 中，扩展名 `.reg` 通常表示（ ）。

A. 批处理文件  
B. 注册表导入/导出文件  
C. 动态链接库  
D. 快捷方式

**答案：B**

**解析：** `.reg` 与 Windows Registry（注册表）相关。`.bat` 是批处理，`.dll` 是动态链接库，`.lnk` 是快捷方式。

---

### 5. 与 FAT32 相比，NTFS 的典型特点是（ ）。

A. 不支持文件权限控制  
B. 单个文件最大只能约 4 GB  
C. 支持更完善的权限与日志机制  
D. 只能用于软盘

**答案：C**

**解析：** NTFS 支持 ACL 权限控制、日志等机制。FAT32 的经典单文件大小上限约为 4 GB，而不是 NTFS。

---

### 6. Linux 系统中，设备文件通常集中位于（ ）。

A. `/etc`  
B. `/dev`  
C. `/home`  
D. `/tmp`

**答案：B**

**解析：** `/dev` 中包含大量设备文件。`/etc` 主要放配置文件，`/home` 是普通用户主目录，`/tmp` 用于临时文件。

---

### 7. Linux 命令中，用于按模式在文本中查找匹配内容的是（ ）。

A. `grep`  
B. `find`  
C. `tr`  
D. `wc`

**答案：A**

**解析：** `grep` 用于文本模式匹配；`find` 主要在目录树中查找文件；`tr` 做字符转换；`wc` 做统计。

---

### 8. 关于 UTF-8，下列说法正确的是（ ）。

A. 每个字符固定占 8 位  
B. 它是一种可变长 Unicode 编码方式  
C. 它只能表示英文字符  
D. 它与 ASCII 完全不兼容

**答案：B**

**解析：** UTF-8 是 Unicode 的可变长编码方式。ASCII 范围内的字符在 UTF-8 中仍以单字节表示，因此具有良好兼容性。

---

### 9. 一幅未压缩的 800×600、24 位真彩色图像，其像素数据量约为（ ）。

A. 800×600 Byte  
B. 800×600×24 Byte  
C. 800×600×24÷8 Byte  
D. 800×600÷24 Byte

**答案：C**

**解析：** 24 位表示每个像素 24 bit，即 3 Byte，所以容量约为 `800×600×24÷8 Byte`。实际图像文件还可能包含文件头、对齐和压缩等额外因素。

---

### 10. 下列图像格式中，通常采用有损压缩并适合照片的是（ ）。

A. JPEG  
B. PNG  
C. BMP  
D. TXT

**答案：A**

**解析：** JPEG 典型采用有损压缩，适合自然照片；PNG 典型为无损压缩且支持透明；BMP 常见体积较大；TXT 不是图像格式。

---

### 11. 将源程序通常整体翻译成目标程序后再执行，这种语言处理方式称为（ ）。

A. 解释  
B. 编译  
C. 装入  
D. 备份

**答案：B**

**解析：** 编译通常先把源程序整体翻译成目标代码/目标程序，再运行；解释则通常边解释边执行。装入是把程序放入内存准备执行。

---

### 12. 下列网络设备中，主要用于连接不同网络并依据网络层地址转发分组的是（ ）。

A. 路由器  
B. 集线器  
C. 中继器  
D. 键盘

**答案：A**

**解析：** 路由器工作重点在网络层，用于在不同网络之间转发 IP 分组。集线器和中继器更接近物理层设备。

---

### 13. IEEE 802.3 主要对应（ ）。

A. 以太网  
B. 无线个人区域网  
C. 无线城域网  
D. 蓝牙文件传输

**答案：A**

**解析：** 802.3 对应 Ethernet（以太网）。这里仅作为网络标准的基础滚动复习，不展开 Wi-Fi 子标准。

---

### 14. Linux 中，如果希望统计文本文件的行数，最直接的命令是（ ）。

A. `wc -l file.txt`  
B. `tr file.txt`  
C. `pwd file.txt`  
D. `ps file.txt`

**答案：A**

**解析：** `wc -l` 用于统计行数。`tr` 是字符转换，`pwd` 显示当前工作目录，`ps` 查看进程。

---

## 三、今日速记

```text
【固件/启动】
BIOS → 传统固件程序
CMOS → 传统教材中的 BIOS 参数保存位置
UEFI → 现代主流固件接口
POST → 上电自检

【存储】
HDD → 机械硬盘
SSD → 固态硬盘
SATA → 存储接口/协议体系
NVMe → 常运行在 PCIe 上的高性能存储协议
M.2 ≠ NVMe

【扩展总线】
PCI → 并行、共享总线
PCIe → 串行、点对点、x1/x4/x8/x16 表示 Lane 数
AGP → 历史上的显卡专用/面向图形加速接口

【Windows】
.reg → 注册表
.bat → 批处理
.dll → 动态链接库
.lnk → 快捷方式
.msi → Windows Installer 安装包

FAT32 → 兼容性好，经典单文件约 4GB 上限
NTFS → 权限 + 日志 + 更强现代能力

【Linux】
/dev  → 设备文件
/etc  → 配置
/home → 用户主目录
/var  → 日志等经常变化的数据
/proc → 进程/内核信息虚拟文件系统

grep → 文本匹配
find → 查文件
tr → 字符转换
wc → 统计
sort → 排序
uniq → 相邻重复行处理

【编码】
ASCII → 标准 7 位，128 个字符
Unicode → 统一码点体系
UTF-8 → Unicode 的可变长编码
GBK → 比 GB2312 覆盖范围更大

【多媒体】
图像容量 ≈ 宽 × 高 × 色深 ÷ 8
PCM 码率 ≈ 采样频率 × 位数 × 声道数
JPEG → 有损、照片
PNG → 无损、透明
GIF → 简单动画
MP3 → 有损音频
MP4 → 容器，不等于固定视频编码

【软件】
编译 → 先翻译，再运行
解释 → 边翻译，边执行
链接 → 把目标模块/库组合起来
装入 → 把程序放入内存

【网络滚动复习】
802.3 → Ethernet
路由器 → 连接不同网络，按网络层地址转发
```

> **今日一句话总结：** 今天的重点不是把某一个领域讲深，而是横向补齐微机硬件、Windows、Linux、编码、多媒体、软件基础和网络设备等“国网杂项盲区”。
