# 国网计算机基础与实用知识晨练｜2026-09-23

> **今日主题：** HDD/SSD 与闪存、Windows/DOS 属性与磁盘命令、Linux 用户切换与身份查看、以太网设备与 RJ-45、压缩格式、音频容量与码率、邮件/FTP/远程登录、北桥/南桥与 PCH  
> **今日目标：** 近几日已重点覆盖 POST、CPU 扩展指令、注册表、PCI/PCIe、Windows 网络命令、Linux 文件类型/挂载、字符编码、图像与视频等。今天避开这些近期主角，补齐存储介质、传统系统命令、Linux 身份管理、基础网络设备、压缩与音频、互联网应用和芯片组历史口径。

## 一、今日知识点

### 1. HDD、SSD 与闪存：机械存储和固态存储不要混

| 项目 | HDD 机械硬盘 | SSD 固态硬盘 |
|---|---|---|
| 核心介质 | 磁性盘片 | NAND Flash 等非易失性闪存 |
| 机械部件 | 有盘片、磁头等运动部件 | 无机械寻道部件 |
| 随机访问 | 受寻道、旋转延迟影响 | 通常明显更快 |
| 抗震性 | 相对较弱 | 通常更好 |
| 常见接口/协议 | SATA、SAS 等 | SATA SSD；NVMe SSD 常经 PCIe |

- SSD 不等于 NVMe：SSD 是存储设备类型，NVMe 是面向非易失性存储的协议；SATA SSD 同样是 SSD。
- U 盘、存储卡也常采用闪存，但其控制器、接口和性能定位与 SSD 不完全相同。
- RAM 断电通常丢失数据，而 NAND Flash 属于非易失性存储。

📌 **国网考点：** “SSD 无机械寻道”“NVMe 与 SATA 不是 SSD/非 SSD 的同义词”是高价值辨析。

### 2. attrib、chkdsk、format：属性、检查、格式化各管一件事

Windows/DOS 基础题常把几个命令混在一起考：

| 命令 | 基础用途 | 典型辨析 |
|---|---|---|
| `attrib` | 查看或修改文件属性 | 可涉及只读、隐藏、系统等属性 |
| `chkdsk` | 检查磁盘/文件系统状态并可按参数处理错误 | 不是格式化命令 |
| `format` | 格式化卷/磁盘介质 | 具有破坏原有数据结构的风险 |
| `type` | 显示文本文件内容 | 不用于改文件属性 |
| `tree` | 树状显示目录结构 | 不等于 `dir` 的普通列表显示 |
| `cls` | 清屏 | 不删除文件 |

常见示意：

```bat
attrib +r report.txt
chkdsk C:
tree C:\work
```

⚠️ **易错提醒：** `format` 不是“整理磁盘碎片”；考试中不要把格式化、磁盘检查、磁盘清理混为一谈。

### 3. su、sudo、id：切换身份、授权执行、查看身份

Linux 中这三个命令名字短，选择题很爱混：

| 命令 | 核心用途 |
|---|---|
| `su` | 切换用户身份或启动相应 Shell |
| `sudo` | 按配置的授权规则以其他身份（常见为 root）执行命令 |
| `id` | 显示用户 UID、GID 及组成员等身份信息 |
| `passwd` | 设置/修改用户密码 |

- `root` 是传统 Unix/Linux 的超级用户名称，其 UID 通常为 0。
- `sudo` 不等于“永久变成 root”；它强调按授权执行命令。
- 用户与组用于权限管理，UID/GID 是系统识别身份的重要数字标识。

📌 **国网考点：** `id` 查身份，`passwd` 管密码，`su` 切用户，`sudo` 授权执行。

### 4. 网卡、Hub、Switch、Router 与 RJ-45：设备和接口分清

| 名称 | 基础定位 | 典型关键词 |
|---|---|---|
| 网卡 NIC | 主机接入网络的网络接口硬件 | MAC 地址、网络接口 |
| 集线器 Hub | 传统共享式以太网集中连接设备 | 多端口转发/广播式、历史设备 |
| 交换机 Switch | 局域网中按 MAC 等信息转发以太网帧 | 二层交换最常见 |
| 路由器 Router | 在不同 IP 网络间选择路径并转发分组 | 网络层、路由 |
| RJ-45（常见俗称） | 以太网双绞线连接中常见模块化接口称呼 | 接口，不是网络设备 |

⚠️ **考试口径：** 基础题常把“交换机工作在数据链路层、路由器工作在网络层”作为典型结论；现实设备可能具有多层功能，但传统选择题应先按基本定位作答。

🧠 **速记：** 网卡让主机入网，交换机连局域网节点，路由器连不同网络。

### 5. ZIP、RAR、7z、gzip：都是压缩相关，但组织方式不同

| 格式/工具 | 基础常识 |
|---|---|
| ZIP | 常见归档压缩格式，跨平台支持广 |
| RAR | 常见专有归档压缩格式 |
| 7z | 7-Zip 常用归档格式，支持多种压缩方法 |
| gzip | 常见于 Unix/Linux，通常压缩单个数据流/文件 |
| tar | 主要负责把多个文件归档打包，本身并不等于 gzip 压缩 |

Linux 中常见组合是 `tar` 先归档，再配合 gzip/bzip2/xz 等压缩。因此 `.tar.gz` 可以理解为“tar 归档 + gzip 压缩”的常见组合。

📌 **国网考点：** “tar = 打包归档”与“gzip = 压缩”不要简单画等号；ZIP 本身通常兼具归档和压缩能力。

### 6. 音频采样与容量：频率、位数、声道共同决定原始数据量

未经压缩 PCM 音频的基础估算：

`数据量(bit) = 采样频率(Hz) × 采样位数(bit) × 声道数 × 时间(s)`

例如，44.1 kHz、16 bit、双声道的理论原始码率：

`44100 × 16 × 2 = 1,411,200 bit/s ≈ 1411.2 kbit/s`

- 采样频率：每秒采样次数。
- 采样位数：每个样本量化使用的位数，通常越高可表示的幅度层次越丰富。
- 声道数：单声道为 1，双声道为 2。
- MP3、AAC 等通常是有损压缩编码，文件码率不能直接套用未压缩 PCM 公式。
- WAV 是一种容器/文件格式语境，常见 WAV 可存 PCM，但不能简单说“WAV 永远无压缩”。

⚠️ **易错提醒：** 采样频率单位是 Hz，不是 bit；采样位数与声道数也不是一回事。

### 7. E-mail、FTP、远程登录：互联网应用见名识用途

| 应用 | 核心用途 | 常见相关协议/工具 |
|---|---|---|
| 电子邮件 | 异步收发邮件 | SMTP 用于邮件发送/传输；POP3、IMAP 常用于收取/访问邮箱 |
| FTP | 文件传输 | FTP 客户端/服务器 |
| 远程登录 | 在远端主机上获得交互式操作环境 | 历史 Telnet；现代常见 SSH |
| Web | 浏览超文本与 Web 应用 | HTTP/HTTPS |

📌 **国网考点：** SMTP 与“发邮件”关联最紧；POP3/IMAP 与邮件接收/访问相关。Telnet 明文传输、安全性弱，现代远程管理通常优先 SSH。

### 8. 北桥、南桥与 PCH：老教材架构和现代实现要双口径

传统 PC 教材常把主板芯片组分成北桥和南桥：

| 传统部件 | 经典教材定位 |
|---|---|
| 北桥 | 更靠近 CPU，传统上负责高速部分，如内存、显卡总线等 |
| 南桥 | 传统上负责较低速 I/O，如 USB、SATA、音频等外围功能 |
| PCH | Intel 较现代平台中的 Platform Controller Hub，承接许多传统南桥类 I/O 功能 |

随着处理器集成度提高，内存控制器、部分 PCIe 控制功能等已大量集成进 CPU，传统“北桥芯片”在现代 PC 中不再按旧形态普遍存在。

📌 **考试口径：** 老题问“北桥主要连接高速设备、南桥主要管理低速 I/O”时，应识别经典教材答案。

⚠️ **现代情况：** 不要反过来认为今天所有主板仍必须存在独立北桥和南桥两颗芯片。

## 二、国网风格练习题

### 1. 下列关于 SSD 的说法，正确的是（ ）。

A. SSD 必须采用 NVMe 协议  
B. SSD 依靠机械磁头完成寻道  
C. SSD 通常采用非易失性闪存且无机械寻道部件  
D. SSD 断电后一定丢失全部数据

**答案：C**

**解析：** SSD 常采用 NAND Flash 等非易失性存储，无 HDD 那样的机械寻道。SSD 可以采用 SATA，也可以采用 NVMe 等，因此 A 错；B 描述的是 HDD 特征；D 与非易失性存储特性矛盾。

### 2. 在 Windows/DOS 命令中，主要用于查看或修改文件属性的是（ ）。

A. attrib  
B. cls  
C. tree  
D. type

**答案：A**

**解析：** `attrib` 用于查看/修改文件属性；`cls` 清屏，`tree` 显示目录树，`type` 显示文本文件内容。

### 3. Linux 中用于显示当前用户 UID、GID 等身份信息的命令是（ ）。

A. passwd  
B. id  
C. sudo  
D. mount

**答案：B**

**解析：** `id` 用于显示用户和组身份信息；`passwd` 管理密码，`sudo` 用于按授权执行命令，`mount` 用于挂载文件系统。

### 4. 在典型基础网络设备中，主要根据 MAC 地址信息转发以太网帧的是（ ）。

A. 二层交换机  
B. 路由器  
C. 调制解调器  
D. 打印机

**答案：A**

**解析：** 二层交换机典型地依据 MAC 地址表转发以太网帧。路由器主要依据网络层信息在不同 IP 网络间转发。

### 5. 下列关于 tar 与 gzip 的说法，正确的是（ ）。

A. tar 的唯一作用是对单文件进行有损压缩  
B. gzip 主要用于创建目录结构  
C. tar 主要负责归档，gzip 可用于压缩数据  
D. `.tar.gz` 与归档、压缩均无关

**答案：C**

**解析：** tar 的核心是归档打包，gzip 是压缩工具/格式；二者常组合形成 `.tar.gz`。A、B、D 均把职责混淆。

### 6. 未压缩 PCM 音频的数据量估算通常不直接取决于（ ）。

A. 采样频率  
B. 采样位数  
C. 声道数  
D. 显示器刷新率

**答案：D**

**解析：** PCM 音频原始数据量与采样频率、采样位数、声道数和时长相关，显示器刷新率与音频数据量无关。

### 7. 在电子邮件相关协议中，通常与发送邮件关系最密切的是（ ）。

A. SMTP  
B. IMAP  
C. POP3  
D. ARP

**答案：A**

**解析：** SMTP 用于邮件发送/传输；POP3、IMAP 主要用于用户收取或访问邮箱；ARP 属于网络地址解析范畴。

### 8. 按传统 PC 教材口径，主要负责较低速外围 I/O 的芯片是（ ）。

A. 北桥  
B. 南桥  
C. CPU ALU  
D. Cache

**答案：B**

**解析：** 经典北桥/南桥架构中，南桥负责 USB、磁盘接口等较低速 I/O；北桥传统上连接 CPU、内存和高速图形等。现代架构已发生明显变化，但老题需按经典口径识别。

### 9. 下列说法正确的是（ ）。

A. RJ-45 是一种路由协议  
B. Hub 与 Router 完全同义  
C. 网卡是主机接入网络的重要接口硬件  
D. 交换机只能用于无线网络

**答案：C**

**解析：** 网卡承担主机网络接口功能。RJ-45 是常见接口称呼而非路由协议；Hub 与 Router 职责不同；交换机广泛用于有线以太网。

### 10. 若某未压缩 PCM 音频的采样频率和采样位数不变，从单声道改为双声道，其他条件相同，则理论原始数据量约变为（ ）。

A. 原来的 1/4  
B. 原来的 1/2  
C. 原来的 2 倍  
D. 完全不变

**答案：C**

**解析：** 原始数据量与声道数成正比。单声道 1 个声道变为双声道 2 个声道，在其他条件不变时数据量约翻倍。

### 11. 【滚动复习】Windows 中用于查看本机 IP 配置的常用命令是（ ）。

A. ipconfig  
B. chmod  
C. grep  
D. tar

**答案：A**

**解析：** `ipconfig` 是 Windows 常用网络配置查看命令。其余三个是 Unix/Linux 语境中常见的权限、文本检索和归档命令。

### 12. 【滚动复习】关于 PCIe 的基础描述，正确的是（ ）。

A. 典型 PCIe 采用点对点串行链路  
B. PCIe 与传统 PCI 一样只能使用共享并行总线  
C. PCIe 是一种音频压缩格式  
D. PCIe 只能连接键盘

**答案：A**

**解析：** PCIe 的基础特征是高速串行、点对点链路；传统 PCI 则常以并行共享总线为典型特征。

### 13. 【滚动复习】下列字符编码中，ASCII 最典型的基础定位是（ ）。

A. 一种视频容器  
B. 一种早期字符编码标准  
C. 一种磁盘接口  
D. 一种 CPU 插槽

**答案：B**

**解析：** ASCII 是经典字符编码标准；它与视频容器、磁盘接口和 CPU 插槽无关。

### 14. 【滚动复习】关于 Windows 快捷方式 `.lnk`，正确的是（ ）。

A. 快捷方式文件就是目标程序的完整副本  
B. 删除快捷方式通常等同于卸载目标程序  
C. 快捷方式通常保存对目标对象的引用信息  
D. `.lnk` 是 Linux 文件权限表示

**答案：C**

**解析：** Windows 快捷方式保存指向目标的相关信息，本身不是目标程序的完整副本。删除快捷方式通常不会删除目标程序。

## 三、今日速记

| 看到关键词 | 快速联想 |
|---|---|
| HDD | 磁性盘片、机械寻道 |
| SSD | 闪存、无机械寻道；不等于 NVMe |
| `attrib` | 文件属性 |
| `chkdsk` | 磁盘/文件系统检查 |
| `format` | 格式化 |
| `su` | 切换用户 |
| `sudo` | 授权执行 |
| `id` | UID/GID/组身份 |
| Switch | 局域网帧转发、MAC |
| Router | 不同 IP 网络间路由转发 |
| RJ-45 | 常见双绞线以太网接口称呼 |
| tar | 归档打包 |
| gzip | 压缩 |
| `.tar.gz` | tar 归档 + gzip 压缩 |
| PCM 原始数据量 | 采样频率 × 位数 × 声道 × 时间 |
| SMTP | 发/传邮件 |
| POP3 / IMAP | 收取/访问邮箱 |
| Telnet | 历史远程登录，明文风险 |
| SSH | 现代常见安全远程登录 |
| 北桥 | 传统高速侧 |
| 南桥 | 传统低速 I/O 侧 |
| PCH | 现代平台承接许多传统南桥类功能 |

🧠 **一分钟口诀：** **机械盘看寻道，固态盘看闪存；属性 attrib、检查 chkdsk、格式 format；su 换人、sudo 授权、id 查身份；交换看 MAC、路由看网络；tar 打包 gzip 压；SMTP 发信；老题北桥高速、南桥 I/O，现代看 CPU 集成 + PCH。**
