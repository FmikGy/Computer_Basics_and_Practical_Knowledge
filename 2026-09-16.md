# 国网计算机基础与实用知识晨练｜2026-09-16

> **今日主题：** RAM/ROM 与 SRAM/DRAM、Windows 常见系统扩展名、Linux 目录树、网络设备职责、压缩格式、MMX/SSE/AVX、互联网基础应用、串口/并口历史口径  
> **今日目标：** 最近几天已集中覆盖 PCI/PCIe、CPU 指标、存储接口、Linux 权限/磁盘/管道、Windows 网络命令、字符编码、图像/音视频、IEEE 802、文件系统等。今天主动切换到内存分类、Linux 目录、网络设备、压缩文件、CPU 扩展指令和互联网应用等近期相对欠缺的“见过即可拿分”知识。

## 一、今日知识点

### 1. RAM、ROM、SRAM、DRAM：先分“能否随机读写”，再看实现方式

| 名称 | 基础定位 | 是否易失 | 高频关键词 |
|---|---|---|---|
| RAM | 随机存取存储器，通常作为可读写工作存储器 | 通常易失 | 内存、运行数据 |
| ROM | 只读存储器家族，传统口径用于保存较固定的信息 | 通常非易失 | 固件、断电保存 |
| SRAM | 静态 RAM | 易失 | 快、贵、无需周期性刷新，常用于 Cache |
| DRAM | 动态 RAM | 易失 | 密度高、成本低、需刷新，主存常见基础 |

SRAM 的“静态”不是“不掉电”，而是只要持续供电，存储状态不需要像 DRAM 那样周期性刷新。DRAM 单元结构更适合做大容量主存，因此现代 DDR 系列内存本质上属于 DRAM 家族。

📌 **国网考点：** “Cache 常采用哪类存储器”常答 SRAM；“主存常用哪类”常答 DRAM。

⚠️ **易错提醒：** SRAM 和 DRAM 都属于 RAM，通常都具有易失性；不要把“静态”误解为“断电不丢失”。

🧠 **速记：** `S 快贵不刷新，D 密度高要刷新；Cache 偏 S，主存偏 D。`

### 2. Windows 常见扩展名：程序、脚本、库、配置别混

| 扩展名 | 常见定位 |
|---|---|
| `.exe` | Windows 可执行程序 |
| `.com` | DOS 时代常见的可执行程序格式之一 |
| `.bat` | DOS/Windows 批处理脚本 |
| `.cmd` | Windows 命令脚本 |
| `.dll` | 动态链接库 |
| `.sys` | 常见系统/驱动相关文件扩展名 |
| `.ini` | 传统文本配置文件 |
| `.reg` | 注册表导入/导出文本文件 |
| `.lnk` | Windows 快捷方式文件 |
| `.msi` | Windows Installer 安装包 |

`.dll` 通常由其他程序装入并调用，并不等价于普通独立应用程序；`.lnk` 保存的是快捷方式信息，不是目标文件本体。

📌 **国网考点：** 高频配对包括 `.dll → 动态链接库`、`.bat → 批处理`、`.lnk → 快捷方式`、`.msi → 安装包`。

⚠️ **易错提醒：** 改扩展名不会自动改变文件的真实格式。

### 3. Linux 目录树：/etc、/home、/var、/dev、/proc 各司其职

Linux/UNIX 风格系统通常从根目录 `/` 组织统一目录树，而不是以 `C:`、`D:` 盘符作为顶层入口。

| 目录 | 基础用途 |
|---|---|
| `/` | 根目录，整个目录树起点 |
| `/home` | 普通用户主目录常见位置 |
| `/etc` | 系统级配置文件常见位置 |
| `/var` | 经常变化的数据，如日志、缓存、队列等 |
| `/usr` | 大量用户空间程序、库及共享数据 |
| `/dev` | 设备文件 |
| `/proc` | 常见的虚拟文件系统，用于呈现进程和内核信息 |
| `/tmp` | 临时文件常见位置 |

示例：

```bash
cd /etc
ls /home
cat /proc/cpuinfo
ls /dev
```

📌 **国网考点：** 配置想到 `/etc`，用户家目录想到 `/home`，设备想到 `/dev`，进程/内核运行信息想到 `/proc`。

⚠️ **易错提醒：** `/proc` 中大量内容并非普通磁盘文件，而是内核动态提供的信息接口。

### 4. 集线器、交换机、路由器、AP、调制解调器：设备职责要会认

| 设备 | 基础定位 | 典型关键词 |
|---|---|---|
| 集线器 Hub | 传统共享式以太网设备 | 多端口转发/广播式重复，物理层口径 |
| 交换机 Switch | 局域网内按 MAC 地址转发帧 | 二层交换、MAC 地址表 |
| 路由器 Router | 在不同 IP 网络之间转发分组 | IP、路由选择、网络互联 |
| 无线 AP | 为无线终端提供接入局域网的接入点 | WLAN 接入 |
| 调制解调器 Modem | 调制与解调，完成信号形式转换 | Modulation/Demodulation |
| 网卡 NIC | 主机接入网络的接口设备 | MAC、网络接口 |

传统选择题最爱把“交换机”和“路由器”互换：交换机基础口径主要解决同一局域网内的二层转发，路由器主要负责不同 IP 网络之间的三层互联。

🧠 **速记：** `交换看 MAC，路由看 IP，AP 管无线接入，Modem 管调制解调。`

### 5. ZIP、RAR、7z、gzip：压缩格式与“打包”不是一回事

| 名称 | 基础特点 |
|---|---|
| ZIP | 常见归档压缩格式，跨平台支持广 |
| RAR | 常见专有归档格式，常见扩展名 `.rar` |
| 7z | 7-Zip 常用归档格式，通常具有较灵活的压缩能力 |
| gzip | 常见于 UNIX/Linux，通常压缩单个数据流/文件 |
| tar | 主要负责把多个文件归档打包，本身并不等同于压缩算法 |

Linux 中常见组合：

```bash
tar -czf backup.tar.gz folder/
tar -xzf backup.tar.gz
```

这里 `tar` 负责归档，gzip 负责压缩，所以 `.tar.gz` 可以理解为“先归档，再 gzip 压缩”的常见组合。

📌 **国网考点：** `tar` 与 `gzip` 的职责区分非常适合概念辨析题。

⚠️ **易错提醒：** “压缩包”是日常统称，但从技术职责看，归档与压缩不是完全相同的动作。

### 6. MMX、SSE、AVX：CPU 扩展指令集的考试定位

MMX、SSE、AVX 都可视为 x86 处理器发展过程中出现的扩展指令技术，常与 SIMD（单指令多数据）思想联系，用于提高多媒体、科学计算、向量数据等场景中并行处理多个数据元素的效率。

| 名称 | 历史定位 |
|---|---|
| MMX | 较早面向多媒体数据处理的扩展技术 |
| SSE | Streaming SIMD Extensions，后续形成多代扩展 |
| AVX | Advanced Vector Extensions，更晚期的向量扩展家族 |

**考试口径：** 老题若问 MMX 的主要目的，通常联系“增强多媒体处理能力”，而不是把 MMX 当作存储器、总线或操作系统。

**现代情况：** 现代 CPU 已有更丰富的 SIMD/向量指令扩展，但国网基础题通常只要求识别这些缩写的大致定位和演进关系。

🧠 **速记：** `MMX → SSE → AVX：都往并行处理数据、提升多媒体/向量计算能力方向理解。`

### 7. 浏览器、电子邮件、FTP、远程登录：互联网应用会看“用途”

| 应用/协议 | 基础用途 |
|---|---|
| HTTP/HTTPS | Web 页面与 Web 应用访问 |
| SMTP | 电子邮件发送/传输常见协议 |
| POP3 | 从邮件服务器获取邮件的传统协议之一 |
| IMAP | 邮件访问与服务器端同步管理 |
| FTP | 文件传输 |
| Telnet | 传统远程终端登录，明文传输，安全性较弱 |
| SSH | 安全远程登录及相关安全通信 |

📌 **国网考点：** SMTP 常对应“发邮件”；POP3/IMAP 对应“收取/访问邮件”；FTP 对应文件传输；SSH 比 Telnet 更适合现代安全远程管理。

⚠️ **易错提醒：** 浏览器不是“HTTP 协议本身”，浏览器是应用软件，HTTP/HTTPS 是其访问 Web 时常用的应用层协议。

### 8. 串口与并口：历史教材答案要保留时代背景

传统 PC 外设接口题仍可能出现串行口（Serial Port）与并行口（Parallel Port）。

- **串口：** 数据按串行方式传输，传统 PC 上常见 RS-232 相关接口，用于早期鼠标、调制解调器、工业设备等。
- **并口：** 多位数据并行传送，传统 PC 中尤其常与打印机连接联系，常见老式 DB-25 接口形态。
- **现代情况：** 普通个人电脑上 USB 已大规模替代传统串口/并口，但工业、嵌入式和设备维护场景仍可能使用串行通信。

📌 **考试口径：** 老教材问“打印机常接哪个接口”且选项处于传统 PC 语境时，并口可能是标准答案；不能因为现代打印机常用 USB/网络就否定历史口径。

⚠️ **易错提醒：** “串行一定慢、并行一定快”不是现代接口的普遍规律。PCIe、USB、SATA 等高速现代接口都采用串行传输思想。

## 二、国网风格练习题

### 1. 在典型微型计算机中，最常用于 CPU Cache 的存储器类型是（ ）。

A. DRAM  
B. SRAM  
C. 磁盘  
D. Flash ROM

**答案：B**

**解析：** SRAM 速度快且无需像 DRAM 那样周期性刷新，典型基础口径下常用于 Cache。DRAM 密度高、成本较低，更常用于主存。

### 2. 下列关于 SRAM 和 DRAM 的说法，正确的是（ ）。

A. SRAM 断电后仍能永久保存数据  
B. DRAM 不需要刷新  
C. 二者通常都属于易失性 RAM  
D. DDR 内存属于 SRAM

**答案：C**

**解析：** SRAM、DRAM 通常都属于易失性 RAM。DRAM 需要刷新，DDR 系列属于同步动态随机存取存储器体系，即 DRAM 家族。

### 3. Windows 中通常表示动态链接库的扩展名是（ ）。

A. `.dll`  
B. `.lnk`  
C. `.bat`  
D. `.msi`

**答案：A**

**解析：** `.dll` 是动态链接库；`.lnk` 是快捷方式，`.bat` 是批处理脚本，`.msi` 常用于 Windows Installer 安装包。

### 4. Linux 系统中，系统级配置文件最常见的目录是（ ）。

A. `/dev`  
B. `/etc`  
C. `/proc`  
D. `/tmp`

**答案：B**

**解析：** `/etc` 是系统配置文件的典型位置；`/dev` 放设备文件，`/proc` 提供大量进程/内核运行信息，`/tmp` 常放临时文件。

### 5. 在局域网中，基础口径下主要依据 MAC 地址进行二层帧转发的设备是（ ）。

A. 路由器  
B. 调制解调器  
C. 交换机  
D. 无线网卡

**答案：C**

**解析：** 交换机典型依据 MAC 地址表完成二层转发；路由器主要依据 IP 路由信息连接不同网络。

### 6. 下列关于 tar 和 gzip 的说法，正确的是（ ）。

A. tar 的核心职责是病毒扫描  
B. gzip 只能用于 Windows  
C. tar 主要用于归档，gzip 主要用于压缩  
D. `.tar.gz` 一定是不压缩的文件

**答案：C**

**解析：** tar 主要把文件组织成归档，gzip 提供压缩。`.tar.gz` 正是二者组合的典型结果。

### 7. MMX、SSE、AVX 最适合归入下列哪一类技术（ ）。

A. CPU 扩展指令技术  
B. 磁盘文件系统  
C. 无线局域网标准  
D. 数据库查询语言

**答案：A**

**解析：** 三者都是 x86 处理器发展中的扩展指令技术，常与 SIMD、多媒体和向量数据处理联系。

### 8. 电子邮件系统中，通常用于发送或传输邮件的协议是（ ）。

A. SMTP  
B. FTP  
C. ARP  
D. ICMP

**答案：A**

**解析：** SMTP 主要负责邮件发送/传输；POP3、IMAP 常用于邮件访问，FTP 用于文件传输。

### 9. 与传统 Telnet 相比，更适合安全远程登录的协议是（ ）。

A. SSH  
B. SMTP  
C. SNMP  
D. DHCP

**答案：A**

**解析：** SSH 提供加密的远程登录和安全通信能力；传统 Telnet 通信缺少这种安全保护，现代远程管理通常优先 SSH。

### 10. 【滚动复习】Windows 中要跟踪到目标主机所经过的大致路由路径，最适合使用（ ）。

A. `ipconfig`  
B. `tracert`  
C. `tasklist`  
D. `format`

**答案：B**

**解析：** `tracert` 用于逐跳跟踪到目标的大致路径；`ipconfig` 主要查看本机 TCP/IP 配置。这是昨日内容的间隔复习。

### 11. 【滚动复习】Linux Shell 中，将命令输出追加到已有文件末尾通常使用（ ）。

A. `>`  
B. `>>`  
C. `<`  
D. `|`

**答案：B**

**解析：** `>>` 表示追加输出；`>` 通常覆盖写入；`|` 是管道。这一题用于巩固近期 Shell 基础。

### 12. 【滚动复习】下列关于 PCIe 的描述，正确的是（ ）。

A. PCIe 是典型共享并行总线  
B. PCIe x16 表示 16 位并行数据总线  
C. PCIe 采用高速串行、点对点连接思想  
D. PCIe 已被 AGP 全面取代

**答案：C**

**解析：** PCIe 是现代高速串行、点对点互连；x16 指 16 条 Lane 组合，并非传统意义的 16 位并行总线。AGP 反而是被 PCIe 取代的历史显卡接口。

### 13. 【滚动复习】关于 FAT32 与 NTFS，下列更符合 Windows 基础考试口径的是（ ）。

A. NTFS 通常支持更丰富的权限和文件系统功能  
B. FAT32 天生提供完整的 NTFS ACL 权限机制  
C. NTFS 只能用于软盘  
D. FAT32 是 Linux 的 `/proc` 文件系统

**答案：A**

**解析：** NTFS 相比 FAT32 提供更丰富的权限、安全和文件系统特性。其余选项均混淆了文件系统的用途与能力。

### 14. 【滚动复习】无损未压缩位图图像容量估算时，最直接相关的因素是（ ）。

A. 像素总数与每像素位数  
B. CPU 主频与磁盘转速  
C. 路由跳数与端口号  
D. 文件名长度与扩展名

**答案：A**

**解析：** 未压缩位图基础估算可用“水平像素 × 垂直像素 × 每像素位数 ÷ 8”。文件扩展名本身不会决定图像的原始像素数据量。

### 15. 【滚动复习】传统教材中，AGP 最典型的用途是连接（ ）。

A. 显卡  
B. 键盘  
C. 机械硬盘  
D. 调制解调器电话线

**答案：A**

**解析：** AGP 即 Accelerated Graphics Port，历史上主要面向图形加速卡，后来主流显卡转向 PCIe。

## 三、今日速记

| 看到关键词 | 立即联想 |
|---|---|
| Cache 常用 | SRAM：快、贵、不需周期刷新 |
| 主存基础 | DRAM：密度高、需刷新；DDR 属 DRAM 家族 |
| `.dll` | 动态链接库 |
| `.bat` / `.cmd` | 命令脚本 |
| `.lnk` | Windows 快捷方式 |
| `.msi` | Windows Installer 安装包 |
| `/etc` | 系统配置 |
| `/home` | 普通用户主目录 |
| `/dev` | 设备文件 |
| `/proc` | 进程/内核运行信息的虚拟文件系统 |
| Switch | 二层、MAC |
| Router | 三层/IP、不同网络互联 |
| AP | 无线接入 |
| Modem | 调制/解调 |
| tar | 归档 |
| gzip | 压缩 |
| `.tar.gz` | tar 归档 + gzip 压缩 |
| MMX/SSE/AVX | CPU 扩展指令、SIMD/多媒体/向量处理 |
| SMTP | 发/传邮件 |
| POP3/IMAP | 访问邮件 |
| FTP | 文件传输 |
| Telnet | 传统明文远程终端 |
| SSH | 安全远程登录 |
| 传统并口 | 老教材常联系打印机 |
| 现代高速接口 | “串行”不等于“低速”，PCIe/SATA/USB 都是反例 |

🧠 **一分钟口诀：**  
`Cache SRAM，主存 DRAM；配置 /etc，设备 /dev；交换看 MAC，路由看 IP；tar 打包，gzip 压；SMTP 发信，SSH 安全登录；MMX-SSE-AVX 都是 CPU 扩展。`
