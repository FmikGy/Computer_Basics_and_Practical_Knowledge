# 国网计算机基础与实用知识晨练｜2026-09-21

> **今日主题：** PCI/PCIe/AGP、Windows 常用网络命令、Linux 文件类型与链接、网络设备辨析、字符编码、图像容量估算、串口/并口历史口径、快捷方式与环境变量  
> **今日目标：** 近几日已覆盖 CPU 指标、SATA/SCSI/NVMe、DOS 文件命令、Linux 压缩与进程、IEEE 802、文件系统、DDR、BIOS/UEFI 等。今天重点补齐扩展总线、Windows 网络诊断、Linux 文件类型、网络设备、字符编码与图像容量，并穿插历史接口和桌面系统杂项。

## 一、今日知识点

### 1. PCI、PCIe 与 AGP：并行共享、串行点对点和显卡专用历史总线

| 技术 | 基础定位 | 典型特征 | 考试识别词 |
|---|---|---|---|
| PCI | 传统局部总线 | 并行、共享总线 | 老式扩展卡 |
| PCIe | 现代高速扩展互连 | 串行、点对点、Lane | x1/x4/x8/x16 |
| AGP | 历史图形加速端口 | 主要服务显卡 | PCIe 普及前的显卡接口 |

- PCIe 的 `x1/x4/x8/x16` 表示通道（Lane）数量，不是“几倍主频”。
- 现代独立显卡通常使用 PCIe；AGP 属于历史教材常见考点。

📌 **国网考点：** PCIe 相比传统 PCI 的核心辨析是“高速串行 + 点对点”，不要因为名称含 PCI 就认为两者都采用并行共享总线。

### 2. Windows 网络诊断命令：ipconfig、ping、tracert、netstat

| 命令 | 主要用途 | 常见考法 |
|---|---|---|
| `ipconfig` | 查看 Windows IP 配置 | IP 地址、网关、DNS 等 |
| `ping` | 测试基本 IP 连通性/往返响应 | 常借助 ICMP 回显机制 |
| `tracert` | 跟踪到目标的路径 | 观察经过的中间跳 |
| `netstat` | 查看网络连接、监听端口等 | 连接状态辨析 |

```cmd
ipconfig /all
ping 192.168.1.1
tracert example.com
netstat -ano
```

⚠️ **易错提醒：** `ping` 成功能说明一定程度的网络可达性，但不能证明目标上的所有应用服务都正常。

### 3. Linux 文件类型与软/硬链接

Linux 常见文件类型包括普通文件、目录、字符设备、块设备、符号链接、管道和套接字等。“一切皆文件”强调很多资源可通过统一的文件接口访问，并不表示所有对象物理上都是磁盘普通文件。

| 对比 | 硬链接 | 符号链接（软链接） |
|---|---|---|
| 本质 | 为同一 inode 增加目录项 | 独立文件，保存目标路径 |
| 跨文件系统 | 通常不可以 | 可以 |
| 链接目录 | 普通用户通常受限制 | 可以指向目录 |
| 原目标删除 | 只要仍有硬链接，数据仍可访问 | 可能成为悬空链接 |

常见命令：`ln source hardlink` 创建硬链接；`ln -s target symlink` 创建符号链接。

🧠 **速记：** 硬链接“同 inode”，软链接“存路径”。

### 4. 集线器、交换机、路由器、AP 与调制解调器

| 设备 | 基础作用 | 选择题关键词 |
|---|---|---|
| 集线器 Hub | 多端口信号转发，传统共享式以太网设备 | 不按 MAC 智能转发 |
| 交换机 Switch | 依据 MAC 地址进行二层转发 | 局域网、MAC 表 |
| 路由器 Router | 根据网络层地址进行分组转发 | 连接不同网络、路由 |
| AP | 为无线终端提供 WLAN 接入 | 无线接入点 |
| Modem | 调制与解调，适配特定接入链路信号 | 调制解调 |

📌 **国网考点：** 家用“无线路由器”通常集成路由、交换、AP 等多种功能，不能据此把 AP 和路由器的概念定义混为一谈。

### 5. ASCII、Unicode、UTF-8、UTF-16、GB2312 与 GBK

- **ASCII**：早期英文字符编码体系，标准 ASCII 使用 7 位表示 128 个字符。
- **Unicode**：为全球字符分配统一码点的字符集/编码标准体系；不能简单等同于 UTF-8。
- **UTF-8**：Unicode 的一种变长编码方式，与 ASCII 的基本字符编码保持兼容。
- **UTF-16**：Unicode 编码形式之一，以 16 位代码单元为基础，部分字符需要代理项对。
- **GB2312**：面向简体中文的早期国家标准字符集/编码。
- **GBK**：在 GB2312 基础上扩展，覆盖字符更多。

⚠️ **易错提醒：** “Unicode = UTF-8”错误。Unicode 解决字符编号体系问题，UTF-8/UTF-16 是常见编码形式。

### 6. 图像容量估算：分辨率 × 色深是高频简单计算

未压缩位图的基础估算：

`容量(bit) = 水平像素 × 垂直像素 × 每像素位数`

再除以 8 可换算为 Byte。例如 800×600、24 bit 色深的未压缩图像：

`800 × 600 × 24 ÷ 8 = 1,440,000 Byte`

实际 BMP 等文件还可能包含文件头、调色板等额外数据，因此公式常用于考试中的近似裸数据量估算。

📌 **国网考点：** JPEG/PNG 等压缩文件的实际大小不能仅靠“分辨率×色深”准确推出，因为还取决于编码、内容和压缩参数。

### 7. 串口与并口：老题要按传统用途识别

| 接口 | 传统教材口径 | 常见设备 |
|---|---|---|
| 串口（Serial/COM） | 数据按串行方式传输 | 早期鼠标、调制解调器、设备控制 |
| 并口（Parallel/LPT） | 多位数据并行传输 | 传统打印机 |

**考试口径：** COM 常与串口对应，LPT 常与并口/打印机端口对应。  
**现代情况：** 普通 PC 外设大量转向 USB、网络或无线连接，但工业设备中串行通信思想及相关接口仍有应用。

### 8. Windows 快捷方式、路径与环境变量

- `.lnk` 通常是 Windows 快捷方式文件，它指向目标，不等同于目标文件本身。
- **绝对路径**从确定位置描述完整路径；**相对路径**相对于当前工作位置解析。
- 环境变量用于保存供系统和程序读取的配置信息，如 `PATH` 可影响命令搜索路径。
- 删除桌面上的普通快捷方式通常不会删除其指向的原始程序或文档。

🧠 **速记：** 快捷方式是“指针式入口”，环境变量是“进程可读取的配置环境”。

## 二、国网风格练习题

### 1. 下列关于 PCIe 的说法，正确的是（ ）。
A. PCIe 是典型并行共享总线  
B. PCIe 采用高速串行、点对点连接  
C. PCIe 只能连接显卡  
D. PCIe 已被 AGP 全面取代

**答案：B**

**解析：** PCIe 的典型特点是高速串行、点对点连接，并可按 Lane 数形成 x1、x4、x8、x16 等链路。A 更接近传统 PCI 的特征；C 错在 PCIe 可连接多种设备；D 方向相反，PCIe 取代了 AGP 在主流显卡接口中的地位。

### 2. Windows 中用于查看本机 IP 配置信息的常用命令是（ ）。
A. tracert  
B. ipconfig  
C. tasklist  
D. format

**答案：B**

**解析：** `ipconfig` 用于查看 Windows TCP/IP 配置；`tracert` 用于路径跟踪；`tasklist` 查看进程；`format` 用于格式化卷。

### 3. Linux 中执行 `ln -s a b`，其主要作用是（ ）。
A. 为 a 创建名为 b 的符号链接  
B. 为 a 创建名为 b 的硬链接  
C. 压缩 a 为 b  
D. 将 a 移动为 b

**答案：A**

**解析：** `ln -s` 中的 `-s` 表示 symbolic，创建符号链接。去掉 `-s` 才是通常意义上的硬链接创建方式。

### 4. 在典型以太网局域网中，主要依据 MAC 地址表进行二层转发的设备是（ ）。
A. 交换机  
B. 路由器  
C. 调制解调器  
D. 打印机

**答案：A**

**解析：** 二层交换机依据 MAC 地址表转发帧。路由器主要根据网络层信息进行路由转发；Modem 的核心是调制/解调或接入链路信号适配。

### 5. 下列说法正确的是（ ）。
A. Unicode 与 UTF-8 完全是同一概念  
B. UTF-8 只能表示英文字符  
C. UTF-8 是 Unicode 的一种编码方式  
D. ASCII 标准使用 16 位表示每个字符

**答案：C**

**解析：** Unicode 建立统一字符码点体系，UTF-8 是其常用编码形式之一。UTF-8 可表示广泛的 Unicode 字符；标准 ASCII 是 7 位编码体系，不是 16 位。

### 6. 一幅 640×480 像素、24 bit 色深的未压缩图像，其像素数据量约为（ ）。
A. 640×480×24 Byte  
B. 640×480×24÷8 Byte  
C. 640×480÷24 Byte  
D. 640×480×8×24 Byte

**答案：B**

**解析：** 每像素 24 bit，因此总位数为 640×480×24 bit；换算为 Byte 需除以 8。实际文件还可能有文件头等开销，但考试基础估算通常使用该公式。

### 7. 在传统 PC 接口题中，LPT 最常与哪类设备联系起来（ ）。
A. 打印机  
B. 显卡  
C. 内存  
D. CPU

**答案：A**

**解析：** LPT 是传统并行端口的常见名称，历史上典型连接打印机。现代普通 PC 已很少使用，但老教材和基础题仍可能出现。

### 8. Windows 中删除一个指向某程序的普通 `.lnk` 快捷方式，通常会（ ）。
A. 同时卸载该程序  
B. 删除程序的全部文件  
C. 只删除快捷入口，不直接删除目标程序  
D. 格式化程序所在磁盘

**答案：C**

**解析：** `.lnk` 快捷方式是指向目标的入口，删除快捷方式通常不会删除目标文件，更不等于卸载程序。

### 9. 关于 SATA 与 NVMe，下列说法正确的是（ ）。
A. NVMe 是一种专为机械软盘设计的协议  
B. SATA 与 NVMe 在任何场景下都是完全相同的概念  
C. NVMe 是面向非易失性存储并针对高速互连优化的协议  
D. SATA 只能用于光盘，不能用于硬盘

**答案：C**

**解析：** 这是对昨日内容的间隔复习。NVMe 面向非易失性存储并为 PCIe 等高速互连环境优化；SATA 常见于 HDD、SATA SSD 等，二者不能简单视为同一层次概念。

### 10. Linux 中将多个文件归档为一个 tar 文件，最典型的工具是（ ）。
A. tar  
B. ping  
C. chmod  
D. grep

**答案：A**

**解析：** `tar` 的核心能力是归档，可与 gzip、bzip2 等压缩工具配合。`grep` 做文本匹配，`chmod` 改权限，`ping` 测试网络连通性。

### 11. 下列关于 FAT32 与 NTFS 的说法，较符合 Windows 基础考试口径的是（ ）。
A. NTFS 支持权限等更丰富的文件系统功能  
B. FAT32 一定比 NTFS 功能更多  
C. 两者都只是图像文件格式  
D. NTFS 是 CPU 指令集

**答案：A**

**解析：** NTFS 提供访问控制、日志等较丰富能力，是现代 Windows 常见文件系统；FAT32 结构较简单、兼容性广，但功能特性不能说比 NTFS 更多。

### 12. 关于 BIOS/UEFI 与 POST，下列说法正确的是（ ）。
A. POST 是一种图像格式  
B. POST 是启动阶段的上电自检过程  
C. UEFI 是一种 Linux 文本命令  
D. BIOS 与 CMOS 在任何语境下都完全同义

**答案：B**

**解析：** POST 即 Power-On Self-Test，上电后进行基本硬件自检。BIOS/UEFI 属于固件体系相关概念；CMOS 在传统 PC 教材中常与保存部分固件配置参数的存储及相关设置口径联系，不能与 BIOS 简单画等号。

### 13. 关于 DDR 内存代际，下列说法正确的是（ ）。
A. DDR3、DDR4、DDR5 可不考虑平台直接任意互插  
B. 不同 DDR 代际通常在电气特性和物理设计上存在差异  
C. DDR 是一种视频容器格式  
D. DDR 只存在于机械硬盘中

**答案：B**

**解析：** 不同 DDR 代际在电气规范、引脚/防呆位置等方面通常不同，不能仅因都叫 DDR 就认为可直接互换。A 是常见干扰项。

### 14. 若希望观察数据包到某远程目标大致经过哪些中间跳，在 Windows 中常用（ ）。
A. tracert  
B. attrib  
C. ren  
D. type

**答案：A**

**解析：** `tracert` 用于路径跟踪。`attrib` 修改/查看文件属性，`ren` 重命名，`type` 显示文本文件内容。

## 三、今日速记

| 看到关键词 | 快速对应 |
|---|---|
| PCI | 传统并行、共享式局部总线 |
| PCIe | 高速串行、点对点、x1/x4/x8/x16 |
| AGP | 历史显卡专用/重点图形接口 |
| ipconfig | 看 Windows IP 配置 |
| ping | 测基本网络可达性 |
| tracert | 看路径经过的跳 |
| netstat | 看连接、监听端口等 |
| `ln -s` | 符号链接/软链接 |
| 硬链接 | 同一 inode 的多个目录项 |
| Switch | MAC、二层转发 |
| Router | 网络层、路由 |
| AP | 无线接入 |
| Modem | 调制/解调或接入信号适配 |
| Unicode | 统一字符码点体系 |
| UTF-8 | Unicode 的变长编码形式，兼容 ASCII 基本字符 |
| GBK | GB2312 的扩展 |
| 未压缩图像容量 | 宽×高×色深÷8 Byte |
| COM | 传统串口 |
| LPT | 传统并口/打印机 |
| `.lnk` | Windows 快捷方式 |
| PATH | 常见命令搜索路径环境变量 |

🧠 **考前一句话：** PCIe 记“串行点对点”，网络命令记“ipconfig 看配置、ping 测通、tracert 看路、netstat 看连接”，Linux 链接记“硬链同 inode、软链存路径”，编码记“Unicode 定码点、UTF-8 做编码”，图像裸容量记“像素数×色深÷8”。