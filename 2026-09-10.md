# 国网计算机基础与实用知识晨练｜2026-09-10

> **今日主题：** BIOS/CMOS/UEFI 与启动、自检；SRAM/DRAM/DDR；Windows 注册表与环境变量；Linux 用户与常见目录；办公文档与压缩格式；显示接口；CPU 扩展指令；互联网应用协议常识  
> **今日目标：** 近期已覆盖存储接口、历史扩展总线、外设、Windows 文件系统与命令、Linux 文件类型/管道/压缩、网络设备、字符编码和音视频格式。今天主动补齐固件启动、内存类型、注册表、Linux 用户目录、办公与压缩格式、显示接口和 CPU 指令扩展等长期知识图谱。

## 一、今日知识点

### 1. BIOS、CMOS、UEFI、POST：启动类名词一次分清

| 名词 | 考试层面定位 | 关键点 |
|---|---|---|
| BIOS | 传统 PC 固件/基本输入输出系统 | 上电后进行硬件初始化并引导系统 |
| POST | 加电自检 | 检查内存、键盘等基本硬件是否可用 |
| CMOS | 老教材常指保存 BIOS 设置的低功耗存储及相关电路 | 不是 BIOS 程序本身 |
| UEFI | 现代固件接口规范 | 现代 PC 中广泛取代传统 BIOS 启动方式 |

典型启动逻辑可简化记为：`上电 → 固件执行 → POST/硬件初始化 → 选择启动设备 → 装载操作系统引导程序 → OS 启动`。

📌 **国网考点：** BIOS 与 CMOS 最容易混。传统考试口径中，BIOS 是固件程序，CMOS 主要保存系统配置参数。

⚠️ **考试口径 vs 现代情况：** 老题常写“进入 BIOS 设置 CMOS 参数”；现代机器普遍采用 UEFI，配置存储的具体实现也不应机械理解为早期独立 CMOS RAM，但做传统选择题仍要掌握经典区别。

### 2. SRAM、DRAM、DDR：内存名词看层次

| 名称 | 基础定位 | 常见用途/特点 |
|---|---|---|
| SRAM | 静态随机存取存储器 | 速度快、成本高，常用于 Cache |
| DRAM | 动态随机存取存储器 | 需周期刷新，主存常采用 DRAM 技术 |
| SDRAM | 同步 DRAM | 与系统时钟同步工作 |
| DDR SDRAM | 双倍数据率 SDRAM | 时钟周期的多个边沿传输数据，是现代主存家族 |

DDR、DDR2、DDR3、DDR4、DDR5 属于代际演进。考试重点通常不是背具体频率，而是知道它们属于主存 DRAM 技术演进，不是 Cache 类型。

📌 **国网考点：** `Cache 常用 SRAM；主存通常使用 DRAM；DDR 属于 DRAM 家族。`

⚠️ **易错提醒：** SRAM 中的“静态”不代表断电保存数据；SRAM 和 DRAM 都属于易失性存储器。

### 3. Windows 注册表、环境变量与路径：三个常见实用概念

- **注册表（Registry）**：Windows 用于集中保存系统、硬件、用户和应用程序大量配置数据的层次化数据库。
- 常见根键名称可识别：`HKEY_LOCAL_MACHINE (HKLM)` 偏计算机级配置，`HKEY_CURRENT_USER (HKCU)` 偏当前用户配置。
- `.reg` 文件可用于导入/导出注册表项，但 `.reg` 文件本身不等于整个注册表。
- **环境变量**：为进程提供系统或用户环境信息，如 `PATH`。
- **PATH**：保存可执行程序搜索路径之一，使用户在命令行中不必每次输入程序完整路径。

```bat
set PATH
echo %PATH%
```

📌 **国网考点：** 注册表是配置数据库；环境变量是运行环境中的名称—值信息；PATH 是常见环境变量，三者不是同一概念。

### 4. Linux 用户、root、UID/GID 与常见目录

| 项目 | 含义 |
|---|---|
| root | 超级用户，拥有很高系统管理权限 |
| UID | 用户标识号 |
| GID | 组标识号 |
| `/home` | 普通用户主目录通常位于此处 |
| `/root` | root 用户的主目录 |
| `/etc` | 系统级配置文件常见位置 |
| `/var` | 日志、缓存、队列等经常变化的数据 |
| `/dev` | 设备文件 |
| `/proc` | 内核和进程信息的虚拟文件系统接口 |
| `/tmp` | 临时文件常见位置 |

常见用户管理命令：

```bash
id
passwd
useradd alice
userdel alice
su - alice
sudo command
```

`su` 用于切换用户身份；`sudo` 通常按授权以其他用户（常为 root）身份执行特定命令，两者不要直接等同。

🧠 **速记：** `/etc 配置，/var 常变，/dev 设备，/proc 进程内核，/home 普通用户家。`

### 5. DOCX、XLSX、PPTX、PDF 与 ZIP/RAR/7z/gzip：扩展名只提示格式

| 扩展名 | 常见定位 |
|---|---|
| `.docx` | Office Open XML 文字处理文档 |
| `.xlsx` | Office Open XML 电子表格 |
| `.pptx` | Office Open XML 演示文稿 |
| `.pdf` | 便携式文档格式，强调跨平台版面呈现 |
| `.zip` | 常见归档/压缩格式 |
| `.rar` | 常见归档压缩格式 |
| `.7z` | 7-Zip 常见归档格式 |
| `.gz` | gzip 压缩数据常见扩展名 |

现代 `.docx/.xlsx/.pptx` 文件底层采用基于 XML 的打包结构，通常可以理解为 ZIP 容器中组织多个 XML 等资源文件，但考试时应首先按“办公文档格式”识别。

⚠️ **易错提醒：** 文件名后缀可以被人为修改，扩展名不构成对实际文件格式的绝对证明。

### 6. VGA、DVI、HDMI、DisplayPort：显示接口抓住模拟/数字

| 接口 | 基础特点 |
|---|---|
| VGA | 传统模拟视频接口 |
| DVI | 存在数字、模拟或兼容形式，具体取决于 DVI 类型 |
| HDMI | 数字音视频接口，可同时传输数字视频和音频 |
| DisplayPort | 数字显示接口，可传输音视频等数据 |

📌 **国网考点：** VGA 最典型的标签是“模拟视频”；HDMI 不只是图像接口，还可承载数字音频。

⚠️ **易错提醒：** 不要把物理连接器外形和信号类型完全等同；尤其 DVI 存在不同变体。

### 7. MMX、SSE、AVX：CPU 扩展指令集知道用途即可

MMX、SSE、AVX 都属于 x86 体系中曾先后出现的指令集扩展家族，常用于增强并行数据、浮点或多媒体等计算能力。

- **MMX**：较早的多媒体扩展指令技术，历史题中常出现。
- **SSE**：Streaming SIMD Extensions，扩展 SIMD/浮点等处理能力。
- **AVX**：Advanced Vector Extensions，进一步增强向量计算能力。

📌 **国网考点：** 这些不是操作系统，也不是外设接口，而是处理器指令集扩展。

🧠 **速记：** `MMX → SSE → AVX：都往 SIMD/多媒体/向量计算能力方向看。`

### 8. 浏览器、电子邮件、FTP、远程登录：应用用途不要张冠李戴

| 应用/协议 | 基础用途 |
|---|---|
| HTTP/HTTPS | Web 资源访问；HTTPS 在 HTTP 通信中提供加密保护等安全能力 |
| SMTP | 电子邮件发送/传输常用协议 |
| POP3 / IMAP | 邮件接收/访问常用协议 |
| FTP | 文件传输 |
| Telnet | 传统远程终端协议，明文传输，现代安全场景通常不推荐 |
| SSH | 安全远程登录及相关安全通信能力 |

📌 **国网考点：** SMTP 偏“发邮件”，POP3/IMAP 偏“收/访问邮件”；FTP 偏文件传输；SSH 与 Telnet 都可用于远程登录，但 SSH 提供加密保护。

## 二、国网风格练习题

### 1. 在传统 PC 教材口径中，关于 BIOS 与 CMOS 的说法正确的是（ ）。
A. CMOS 就是 BIOS 固件程序本身  
B. BIOS 主要保存用户文档  
C. BIOS 是固件程序，而 CMOS 常用于保存相关配置参数  
D. CMOS 是一种显示接口

**答案：C**

**解析：** BIOS 是传统 PC 固件/基本输入输出系统；老教材中的 CMOS 常与 BIOS 配置参数保存相关。A 把程序和配置存储混为一谈，B、D 与其用途无关。

### 2. PC 上电后对基本硬件进行检查的过程通常称为（ ）。
A. POST  
B. FTP  
C. RAID  
D. SMTP

**答案：A**

**解析：** POST 即 Power-On Self-Test，加电自检。FTP 是文件传输，SMTP 是邮件传输，RAID 是磁盘阵列相关技术。

### 3. 下列关于 SRAM 与 DRAM 的说法正确的是（ ）。
A. SRAM 断电后仍可长期保存数据  
B. DRAM 不需要刷新  
C. Cache 常采用 SRAM，而主存通常采用 DRAM 体系  
D. DDR 不属于 DRAM 技术

**答案：C**

**解析：** SRAM 速度快、成本高，常用于 Cache；DRAM 需刷新，适合作为大容量主存。SRAM 仍是易失性存储器；DDR SDRAM 属于 DRAM 家族。

### 4. Windows 中主要用于集中保存大量系统和应用配置数据的是（ ）。
A. 注册表  
B. 回收站  
C. 剪贴板  
D. 打印队列

**答案：A**

**解析：** Windows 注册表是层次化配置数据库。回收站用于暂存部分被删除文件，剪贴板用于临时交换复制/剪切数据，打印队列管理待打印任务。

### 5. Windows 环境变量 PATH 的典型作用是（ ）。
A. 保存所有注册表键值  
B. 指定命令查找可执行程序时使用的一组路径  
C. 显示网络经过的路由节点  
D. 保存回收站文件

**答案：B**

**解析：** PATH 是常见环境变量，命令解释环境可利用其中列出的目录查找可执行程序。C 对应 `tracert` 一类路径跟踪用途。

### 6. Linux 中通常用于存放系统级配置文件的目录是（ ）。
A. `/etc`  
B. `/dev`  
C. `/proc`  
D. `/tmp`

**答案：A**

**解析：** `/etc` 是系统配置文件的典型目录；`/dev` 主要是设备文件，`/proc` 提供内核/进程信息接口，`/tmp` 用于临时文件。

### 7. 关于 Linux 的 `su` 与 `sudo`，下列说法较准确的是（ ）。
A. 两者都只能用于删除文件  
B. `su` 常用于切换用户，`sudo` 常按授权以其他用户身份执行命令  
C. 两者都是压缩工具  
D. `sudo` 用于显示磁盘空间

**答案：B**

**解析：** `su` 与用户身份切换相关，`sudo` 强调按授权执行命令。显示文件系统空间通常用 `df`，压缩/归档可用 tar、gzip 等。

### 8. 下列显示接口中，最典型地属于传统模拟视频接口的是（ ）。
A. VGA  
B. HDMI  
C. DisplayPort  
D. USB-C

**答案：A**

**解析：** VGA 典型传输模拟视频信号；HDMI 和 DisplayPort 属于数字显示接口。USB-C 是连接器/接口形态，可承载多种协议，不能简单等同于传统模拟视频接口。

### 9. 下列关于 HDMI 的说法正确的是（ ）。
A. 只能传输模拟音频  
B. 只能用于打印机  
C. 可传输数字视频和音频  
D. 属于 CPU 指令集

**答案：C**

**解析：** HDMI 是常见数字音视频接口，可同时承载数字视频与音频。A、B、D 都混淆了接口定位。

### 10. 下列哪一项属于处理器指令集扩展，而不是外部接口？（ ）
A. HDMI  
B. VGA  
C. AVX  
D. RJ-45

**答案：C**

**解析：** AVX 是 Advanced Vector Extensions，属于处理器向量指令扩展。HDMI、VGA 是显示相关接口，RJ-45 常见于以太网双绞线连接场景。

### 11. 电子邮件系统中，通常用于发送/传输邮件的协议是（ ）。
A. SMTP  
B. IMAP  
C. POP3  
D. ARP

**答案：A**

**解析：** SMTP 主要用于邮件发送和服务器间传输；POP3、IMAP 主要面向邮件接收/访问。ARP 属于地址解析相关协议，不是邮件协议。

### 12. 与 Telnet 相比，SSH 的突出特点是（ ）。
A. 只能传输图片  
B. 提供加密保护的远程通信能力  
C. 只能运行于 Windows  
D. 是一种文件系统

**答案：B**

**解析：** Telnet 传统上采用明文通信，SSH 提供加密的远程登录与安全通信能力，因此现代远程管理更常使用 SSH。

### 13. 【间隔复习】关于 PCI 与 PCIe，下列说法正确的是（ ）。
A. PCIe 典型采用串行点对点链路  
B. PCIe 就是 AGP 的另一个名称  
C. PCI 与 PCIe 都只能连接打印机  
D. PCIe 的 x16 表示 CPU 有 16 个核心

**答案：A**

**解析：** PCIe 使用高速串行点对点链路；`x16` 表示 Lane 数量而不是 CPU 核心数。AGP 是历史上主要面向显卡的专用接口，与 PCIe 不同。

### 14. 【间隔复习】Linux `ls -l` 输出中，首字符为 `l` 通常表示（ ）。
A. 目录  
B. 符号链接  
C. 块设备  
D. 普通文件

**答案：B**

**解析：** `l` 表示 symbolic link；目录通常为 `d`，块设备为 `b`，普通文件通常为 `-`。

### 15. 【间隔复习】一个未压缩的 100×100 像素、24 位色图像，仅按像素数据估算，其容量约为（ ）。
A. 3,000 Byte  
B. 10,000 Byte  
C. 30,000 Byte  
D. 240,000 Byte

**答案：C**

**解析：** 24 位色即每像素 24 bit = 3 Byte，所以像素数据约为 `100 × 100 × 3 = 30,000 Byte`。实际文件还可能包含文件头、元数据、压缩等额外因素。

## 三、今日速记

| 必背项 | 1～2 分钟速记 |
|---|---|
| BIOS / CMOS | BIOS 是固件程序；传统考试中 CMOS 保存配置参数 |
| UEFI | 现代固件接口，广泛替代传统 BIOS 启动方式 |
| POST | 加电自检 |
| SRAM / DRAM | Cache 常用 SRAM；主存通常用 DRAM；两者都易失 |
| DDR | DDR SDRAM 属于 DRAM 家族，不是 Cache 类型 |
| 注册表 | Windows 层次化配置数据库；`.reg` 只是常见导入/导出文件 |
| PATH | 可执行程序搜索路径相关环境变量 |
| Linux 目录 | `/etc` 配置、`/var` 常变、`/dev` 设备、`/proc` 内核进程、`/tmp` 临时 |
| 用户 | root 超级用户；UID 用户号；GID 组号；`su` 切用户，`sudo` 授权执行 |
| Office 格式 | `.docx` 文档、`.xlsx` 表格、`.pptx` 演示 |
| 压缩 | ZIP/RAR/7z 是常见归档压缩格式；gzip 常见 `.gz` |
| 显示接口 | VGA 模拟；HDMI/DP 数字；HDMI 可传音视频 |
| CPU 扩展 | MMX、SSE、AVX 属于处理器指令扩展 |
| 邮件 | SMTP 发/传；POP3、IMAP 收/访问 |
| 远程登录 | Telnet 传统明文；SSH 加密安全 |
| 间隔复习 | PCIe 串行点对点；Linux `l`=软链接；位图容量≈宽×高×色深÷8 |
