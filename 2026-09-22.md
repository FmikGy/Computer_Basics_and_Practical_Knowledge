# 国网计算机基础与实用知识晨练｜2026-09-22

> **今日主题：** POST 启动自检、MMX/SSE/AVX、Windows 注册表与常见系统扩展名、Linux 目录与挂载、显示接口、颜色模型与色深、办公文件格式、病毒/蠕虫/木马辨析  
> **今日目标：** 近期已连续覆盖 PCI/PCIe、Windows 网络命令、Linux 文件类型、字符编码、CPU 指标、存储接口、DOS 命令、Linux 压缩、DDR、IEEE 802 等。今天避开这些近期主角，重点补齐启动自检、CPU 扩展指令、Windows 注册表与系统文件、Linux 目录/挂载、显示接口、多媒体颜色表示、办公格式和基础恶意程序辨析。

## 一、今日知识点

### 1. POST、固件与启动：开机后不是直接进入操作系统

POST（Power-On Self-Test，开机自检）是计算机启动早期对关键硬件进行检查的过程。传统 PC 中常由 BIOS 固件负责启动早期工作；现代 PC 普遍采用 UEFI 固件，但“上电→固件初始化/自检→选择启动设备→加载操作系统”的基本认识仍有考试价值。

| 概念 | 核心作用 | 易混点 |
|---|---|---|
| POST | 启动早期检查 CPU、内存等关键硬件状态 | 不是操作系统中的磁盘检查程序 |
| BIOS | 传统 PC 固件体系及其接口 | 不等于 CMOS |
| UEFI | 现代固件接口规范，承担现代 PC 启动环境的重要角色 | 可视为现代 BIOS 体系的主要替代方案，但考试中 BIOS 一词仍常泛指固件设置 |
| CMOS | 传统教材中常指保存 BIOS 配置及实时时钟相关信息的低功耗存储/电路语境 | CMOS 不是 BIOS 程序本身 |
| Bootloader | 引导加载程序，负责继续加载操作系统 | 与 POST 职责不同 |

典型启动关系可速记为：

`上电 → 固件初始化/POST → 确定启动项 → 引导加载程序 → 操作系统内核 → 系统服务/用户环境`

📌 **国网考点：** “POST 是开机自检”“BIOS/UEFI 属于固件层”“CMOS 与 BIOS 不能画等号”是常见辨析。

⚠️ **考试口径与现代情况：** 老教材常表述为“BIOS 存在 ROM 中，CMOS 保存系统配置”。现代主板具体实现已经演进，但做传统基础题时应识别这一经典分工。

### 2. MMX、SSE、AVX：CPU 扩展指令集的定位

MMX、SSE、AVX 都可作为 x86 处理器扩展指令技术的代表。选择题通常不要求记复杂指令，而是识别其与多媒体、向量/并行数据处理加速之间的关系。

| 名称 | 基础定位 | 考试识别词 |
|---|---|---|
| MMX | 较早的多媒体扩展技术 | 多媒体、SIMD 历史技术 |
| SSE | Streaming SIMD Extensions | SIMD、浮点/多媒体等处理 |
| AVX | Advanced Vector Extensions | 更宽的向量运算能力、科学计算/多媒体等 |

- SIMD：Single Instruction, Multiple Data，即“一条指令对多个数据元素执行同类操作”的思想。
- 它们属于 CPU 指令集扩展，不是显卡接口、总线标准，也不是操作系统命令。
- 指令集能力只是性能因素之一，不能简单推出“支持某扩展就一定比所有不支持者更快”。

🧠 **速记：** MMX → SSE → AVX，可理解为 x86 SIMD/向量扩展演进中的代表名称；考试重“定位”，少背具体位宽和版本数字。

### 3. Windows 注册表：集中式配置数据库，不是一个普通文本配置文件

Windows 注册表（Registry）用于保存操作系统和大量应用程序的配置数据。常见考法是辨析“注册表、INI 文件、REG 文件”和几个根键名称。

常见根键：

| 根键 | 基础含义 |
|---|---|
| `HKEY_LOCAL_MACHINE`（HKLM） | 与本机系统、硬件和软件配置有关 |
| `HKEY_CURRENT_USER`（HKCU） | 当前登录用户相关配置 |
| `HKEY_CLASSES_ROOT`（HKCR） | 文件关联、COM 类等相关信息的合并视图 |
| `HKEY_USERS`（HKU） | 已加载用户配置相关信息 |
| `HKEY_CURRENT_CONFIG`（HKCC） | 当前硬件配置相关视图 |

- `regedit`：Windows 常用注册表编辑器。
- `.reg` 文件：可用于导入/导出注册表项的文本表示之一，不意味着“整个注册表就是一个 .reg 文件”。
- `.ini`：传统初始化配置文件，通常是文本格式；它与注册表不是同一概念。

📌 **国网考点：** HKLM 偏“本机”，HKCU 偏“当前用户”。

⚠️ **易错提醒：** 修改注册表可能影响系统和软件运行，考试知道用途即可，不要把“注册表”理解成普通文件夹。

### 4. EXE、DLL、SYS、LNK、MSI、BAT/CMD：扩展名见名识类

| 扩展名 | 常见定位 | 易错点 |
|---|---|---|
| `.exe` | Windows 可执行程序 | 不代表所有可执行逻辑都只能是 EXE |
| `.dll` | 动态链接库 | 通常供程序加载调用，不等同独立应用程序 |
| `.sys` | 常见系统/驱动相关文件扩展名 | 不应见到 SYS 就随意删除 |
| `.lnk` | Windows 快捷方式文件 | 快捷方式不是目标文件本体 |
| `.msi` | Windows Installer 安装包 | 与普通 EXE 安装程序不是同一封装形式 |
| `.bat` | 批处理脚本 | 由命令解释环境执行 |
| `.cmd` | Windows 命令脚本 | 与 BAT 用途相近，但并非二进制 EXE |
| `.reg` | 注册表导入/导出文本 | 双击导入可能修改注册表 |

📌 **国网考点：** 删除桌面 `.lnk` 快捷方式通常不会删除其指向的原始程序文件；DLL 的关键词是“动态链接库”。

### 5. Linux 常见目录与 mount：目录树没有 Windows 式盘符体系

Linux/UNIX 风格系统通常使用以 `/` 为根的统一目录树。不同磁盘分区、移动设备或网络文件系统可挂载到目录树中的某个挂载点。

| 目录 | 常见用途 |
|---|---|
| `/` | 根目录，整个目录树起点 |
| `/home` | 普通用户主目录常见位置 |
| `/etc` | 系统级配置文件常见位置 |
| `/var` | 经常变化的数据，如日志、缓存、队列等 |
| `/usr` | 大量用户空间程序、库和共享数据 |
| `/dev` | 设备文件 |
| `/proc` | 虚拟文件系统，提供进程和内核相关信息 |
| `/tmp` | 临时文件常见位置 |

常见命令：

```bash
df -h          # 查看已挂载文件系统的空间使用情况
du -sh DIR     # 估算目录占用空间
mount          # 查看/执行挂载
umount PATH    # 卸载文件系统（命令名是 umount）
```

- `df` 更偏“文件系统剩余/使用空间”；`du` 更偏“文件或目录占用空间”。
- `mount` 是把文件系统接入目录树；不是“复制文件到目录”。
- `/proc` 中很多内容由内核动态提供，并非普通磁盘文件。

🧠 **速记：** `/etc` 配置、`/var` 变化、`/dev` 设备、`/proc` 内核进程、`/home` 用户家。

### 6. VGA、DVI、HDMI、DisplayPort：显示接口的模拟/数字辨析

| 接口 | 基础定位 | 声音能力/特点 |
|---|---|---|
| VGA | 传统模拟视频接口 | 主要传视频，不是现代数字接口 |
| DVI | 过渡时期常见显示接口，存在数字型及兼容模拟信号的不同规格 | 考试常抓“比 VGA 更偏数字显示时代” |
| HDMI | 数字音视频接口 | 可在一根连接中传输数字视频和音频 |
| DisplayPort | 数字显示接口 | 可传输数字视频和音频，现代显示设备常见 |

⚠️ **易错提醒：** “DVI 一律只传数字信号”过于绝对，因为 DVI 存在不同规格；基础题若问纯模拟接口，典型答案是 VGA。

📌 **国网考点：** RJ-45 常用于以太网双绞线连接，不属于显示接口；HDMI 与 DisplayPort 都是现代数字显示连接的重要代表。

### 7. RGB、CMYK、色深：屏幕显示与印刷不要混

颜色模型描述颜色的表示方式。国网基础题常把 RGB、CMYK 与色深放在一起考。

| 概念 | 典型场景 | 核心认识 |
|---|---|---|
| RGB | 显示器、数字屏幕 | 红 Red、绿 Green、蓝 Blue；加色模型 |
| CMYK | 彩色印刷 | 青 Cyan、品红 Magenta、黄 Yellow、黑 Key；减色体系 |
| 色深 | 描述每个像素用于表示颜色的信息位数 | 位数越多，理论可表示颜色数量通常越多 |

若一幅未压缩位图分辨率为 `W × H`，每像素 `b` 位，则仅按像素数据粗略估算：

`容量(bit) ≈ W × H × b`

换成字节再除以 8。实际图像文件还可能包含文件头、调色板、元数据、压缩等，因此“估算值”不必等于实际文件大小。

🧠 **速记：** 屏幕 RGB，印刷 CMYK；色深看“一个像素能用多少位描述颜色”。

### 8. 病毒、蠕虫、木马：都属恶意软件，但传播与伪装特点不同

| 类型 | 典型特点 | 高频辨析 |
|---|---|---|
| 计算机病毒 | 常依附文件、程序或特定载体，通过执行/传播感染 | “寄生/感染”是传统教材关键词 |
| 蠕虫 | 强调利用网络等机制自我复制、主动传播 | 通常不必依附某个宿主程序才能复制传播 |
| 木马 | 常伪装成正常或有用程序，诱骗执行并实施未授权行为 | 重点是伪装、后门/控制等，不以自我复制为必要特征 |

基础防护思路：

- 及时安装安全补丁，修补已知漏洞；
- 使用可信来源的软件，谨慎处理未知附件和宏；
- 防病毒软件用于检测和处置已知/可识别恶意代码，但不是“绝对安全保证”；
- 防火墙主要依据规则控制网络通信，不能替代所有终端安全措施；
- 重要数据应有独立备份，备份是降低数据丢失后果的重要手段。

📌 **国网考点：** “蠕虫＝网络自传播”“木马＝伪装诱骗/未授权控制”“病毒＝传统意义上的感染宿主”是高频区分。

## 二、国网风格练习题

### 1. 计算机上电后，对内存等关键硬件进行初步检查的过程通常称为（ ）。

A. POST  
B. FTP  
C. DHCP  
D. RAID

**答案：A**

**解析：** POST 是 Power-On Self-Test，即开机自检，发生在启动早期。FTP 是文件传输协议，DHCP 用于动态网络配置，RAID 是磁盘阵列相关技术，均不是开机硬件自检。

### 2. 下列关于 BIOS 与 CMOS 的说法，符合传统 PC 教材口径的是（ ）。

A. BIOS 就是保存配置参数的 CMOS 存储器  
B. CMOS 就是操作系统内核  
C. BIOS 是固件体系，CMOS 常用于保存相关配置和时钟信息  
D. BIOS 只存在于显卡中

**答案：C**

**解析：** 传统教材强调 BIOS 固件程序与 CMOS 配置信息的分工，二者不能画等号。现代 PC 普遍采用 UEFI，但考试仍可能按经典口径辨析 BIOS/CMOS。

### 3. MMX、SSE、AVX 最适合归入下列哪一类技术？（ ）

A. CPU 扩展指令技术  
B. 磁盘文件系统  
C. 显示器接口  
D. 无线局域网标准

**答案：A**

**解析：** MMX、SSE、AVX 是 x86 处理器扩展指令技术的代表，与 SIMD/向量和多媒体等处理有关。它们不是 NTFS 一类文件系统，也不是 HDMI 一类显示接口或 IEEE 802.11 无线标准。

### 4. Windows 注册表中，与当前登录用户配置关系最直接的根键是（ ）。

A. HKEY_CURRENT_USER  
B. HKEY_LOCAL_MACHINE  
C. HKEY_CLASSES_ROOT  
D. HKEY_CURRENT_CONFIG

**答案：A**

**解析：** HKEY_CURRENT_USER（HKCU）对应当前用户配置。HKLM 更偏本机系统和软件配置；HKCR 与文件关联、COM 类等有关；HKCC 是当前硬件配置相关视图。

### 5. Windows 中扩展名 `.lnk` 通常表示（ ）。

A. 动态链接库  
B. 快捷方式  
C. 设备驱动程序  
D. 注册表数据库本体

**答案：B**

**解析：** `.lnk` 是 Windows 快捷方式文件。动态链接库通常为 `.dll`；`.sys` 常见于系统/驱动相关文件。删除快捷方式一般不等于删除其指向的目标程序。

### 6. 在 Linux 中，最常用于保存系统级配置文件的目录是（ ）。

A. `/etc`  
B. `/dev`  
C. `/proc`  
D. `/tmp`

**答案：A**

**解析：** `/etc` 是系统级配置文件的典型位置。`/dev` 主要是设备文件，`/proc` 是提供进程和内核信息的虚拟文件系统，`/tmp` 常用于临时文件。

### 7. 关于 Linux 的 `df` 与 `du`，下列说法正确的是（ ）。

A. `df` 主要查看文件系统空间使用情况，`du` 主要估算文件或目录占用空间  
B. `df` 用于删除文件，`du` 用于创建用户  
C. 二者都只用于网络诊断  
D. `du` 是卸载文件系统的命令

**答案：A**

**解析：** `df` 常用于查看已挂载文件系统的容量和使用情况，`du` 用于估算目录或文件占用空间。卸载命令是 `umount`，注意不是 `unmount`。

### 8. 下列接口中，传统上主要传输模拟视频信号的是（ ）。

A. VGA  
B. HDMI  
C. DisplayPort  
D. RJ-45

**答案：A**

**解析：** VGA 是典型模拟视频接口。HDMI、DisplayPort 是数字音视频/显示接口；RJ-45 常见于以太网双绞线连接，不是显示接口。

### 9. 下列关于 RGB 与 CMYK 的说法正确的是（ ）。

A. RGB 主要用于彩色印刷，CMYK 主要用于显示器  
B. RGB 是显示设备常见颜色模型，CMYK 常用于印刷  
C. RGB 和 CMYK 都是文件压缩算法  
D. CMYK 中 K 表示千字节

**答案：B**

**解析：** RGB 是屏幕显示常见的加色模型；CMYK 是印刷常见的减色体系。二者描述颜色表示方式，不是压缩算法。

### 10. 与“利用网络等机制自我复制并主动传播”最匹配的恶意软件类型是（ ）。

A. 蠕虫  
B. 木马  
C. 快捷方式  
D. 动态链接库

**答案：A**

**解析：** 蠕虫的典型特征是自我复制和传播，尤其常通过网络扩散。木马更强调伪装诱骗和未授权控制，不要求具备自我复制能力。

### 11. 【滚动复习】关于 PCIe 的描述，正确的是（ ）。

A. PCIe 是典型并行共享总线  
B. PCIe 的 x1、x4、x8、x16 通常表示 Lane 数量  
C. PCIe 是 Windows 文件系统  
D. PCIe 专指无线网卡协议

**答案：B**

**解析：** PCIe 采用高速串行、点对点的互连思想，x1/x4/x8/x16 表示通道数量。传统 PCI 更符合并行共享总线的经典描述。

### 12. 【滚动复习】Windows 中用于跟踪数据包到目标主机所经过路径的常用命令是（ ）。

A. `tracert`  
B. `format`  
C. `taskkill`  
D. `attrib`

**答案：A**

**解析：** Windows 的 `tracert` 用于跟踪到目标的网络路径。`format` 与格式化卷有关，`taskkill` 用于终止进程，`attrib` 用于查看或修改文件属性。

### 13. 【滚动复习】下列关于 SATA、SCSI 和 NVMe 的说法，最准确的是（ ）。

A. NVMe 是专为打印机设计的接口  
B. SATA 只用于机械硬盘，不能连接 SSD  
C. NVMe 是面向非易失性存储、常运行于 PCIe 之上的协议  
D. SCSI 与存储设备无关

**答案：C**

**解析：** NVMe 是面向非易失性存储设计的协议，现代 NVMe SSD 常通过 PCIe 通信。SATA 可连接 HDD，也可连接 SATA SSD；SCSI 长期与存储和外设连接体系密切相关。

### 14. 【滚动复习】关于 Linux 符号链接与硬链接，下列说法正确的是（ ）。

A. 符号链接本质上保存目标路径，可跨文件系统  
B. 硬链接一定保存目标文件路径字符串  
C. 删除原文件后所有硬链接必然立即失效  
D. `ln -s` 用于创建硬链接

**答案：A**

**解析：** 符号链接是独立文件，保存目标路径，因此可跨文件系统；硬链接是同一 inode 的另一个目录项，只要仍有硬链接存在，数据仍可访问。`ln -s` 创建符号链接。

## 三、今日速记

| 看到关键词 | 立即联想 |
|---|---|
| POST | 开机自检，启动早期检查关键硬件 |
| BIOS / UEFI | 固件/启动环境；UEFI 是现代主流 |
| CMOS | 传统教材：配置参数、实时时钟相关；不等于 BIOS |
| MMX / SSE / AVX | CPU 扩展指令、SIMD/向量处理代表 |
| HKLM | 本机配置 |
| HKCU | 当前用户配置 |
| `.dll` | 动态链接库 |
| `.lnk` | Windows 快捷方式 |
| `.msi` | Windows Installer 安装包 |
| `.bat` / `.cmd` | 命令脚本 |
| `/etc` | 系统配置 |
| `/home` | 普通用户主目录常见位置 |
| `/dev` | 设备文件 |
| `/proc` | 进程/内核信息的虚拟文件系统 |
| `df` | 文件系统空间 |
| `du` | 文件/目录占用空间 |
| `mount` / `umount` | 挂载 / 卸载 |
| VGA | 传统模拟视频 |
| HDMI / DisplayPort | 现代数字显示，可承载音频 |
| RGB | 屏幕、加色 |
| CMYK | 印刷、减色 |
| 色深 | 每像素颜色信息位数 |
| 病毒 | 传统关键词：感染/寄生宿主 |
| 蠕虫 | 自我复制、网络传播 |
| 木马 | 伪装诱骗、未授权控制 |

**一分钟口诀：**  
**“开机 POST，固件 BIOS/UEFI；本机 HKLM，用户 HKCU；Linux 配置看 etc，设备看 dev，空间 df、目录 du；VGA 模拟，HDMI/DP 数字；屏幕 RGB、印刷 CMYK；病毒会感染，蠕虫会自传，木马重伪装。”**
