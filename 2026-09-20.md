# 国网计算机基础与实用知识晨练｜2026-09-20

> **今日主题：** CPU 性能指标与字长、SATA/SCSI/NVMe、DOS 文件操作命令、Linux 归档压缩、网络传输介质、视频编码与容器、系统/应用/工具/中间件、CRT 与 LCD 历史口径、备份基础  
> **今日目标：** 近期已覆盖内存、文件系统、Linux 进程/文本处理、IEEE 802、BIOS/UEFI、注册表、显示接口和图像格式。今天主动转向 CPU 指标、存储接口、DOS 命令、Linux 压缩、网络介质、视频媒体、软件分类及备份常识，扩大基础杂项覆盖面。

## 一、今日知识点

### 1. CPU 主频、字长与总线宽度：都是“位数/频率”，含义不同

| 指标 | 核心含义 | 易错点 |
|---|---|---|
| 主频 | CPU 时钟工作的基本频率指标之一 | 主频高不等于整机性能必然更高 |
| 字长 | CPU 一次能够处理的二进制数据位数的典型能力指标 | 常见 32 位、64 位口径 |
| 数据总线宽度 | 一次可并行传送的数据位数 | 不应与地址总线位数混淆 |
| 地址总线宽度 | 与可形成的地址数量相关 | n 位地址线理论上可区分 2^n 个地址 |

📌 **国网考点：** CPU 性能由架构、核心数量、缓存、主频、内存体系等多因素共同影响，不能仅用主频判断。

### 2. SATA、SCSI 与 NVMe：接口/协议不要混成一类

| 名称 | 基础定位 | 常见关联 |
|---|---|---|
| SATA | 常见串行存储接口体系 | HDD、SATA SSD |
| SCSI | 面向计算机与外围设备通信的一套传统接口/命令体系 | 服务器、专业存储历史题常见 |
| NVMe | 面向非易失性存储、针对 PCIe 等高速互连优化的协议 | NVMe SSD |

- **IDE/PATA** 属于较老的并行 ATA 体系，SATA 以串行方式取代其在个人电脑中的主流位置。
- NVMe SSD 常通过 PCIe 通道工作；“M.2”主要是外形规格/连接器形态，不能简单等同于 NVMe。

⚠️ **易错提醒：** M.2 SSD 既可能使用 NVMe/PCIe，也可能存在 SATA 类型；看到 M.2 不能直接断定一定是 NVMe。

### 3. DOS/Windows 文件操作命令：copy、xcopy、move、del、ren、type

| 命令 | 常见用途 |
|---|---|
| `copy` | 复制文件 |
| `xcopy` | 扩展复制，可用于目录树等场景 |
| `move` | 移动文件或目录 |
| `del` | 删除文件 |
| `ren` | 重命名文件/目录 |
| `type` | 显示文本文件内容 |
| `dir` | 列出目录内容 |
| `cd` | 切换当前目录 |

```bat
dir
copy a.txt b.txt
ren old.txt new.txt
type new.txt
```

📌 **国网考点：** `del` 不是删除目录的典型命令；`ren` 是 rename 的缩写；`type` 常用于查看文本内容。

### 4. Linux 归档与压缩：tar 不等于 gzip

| 工具 | 核心作用 |
|---|---|
| `tar` | 把多个文件/目录归档为一个归档文件，也可配合压缩程序 |
| `gzip` / `gunzip` | gzip 格式压缩/解压 |
| `bzip2` / `bunzip2` | bzip2 压缩/解压 |
| `zip` / `unzip` | ZIP 格式压缩/解压 |

常见写法：

```bash
tar -czf backup.tar.gz mydir/
tar -xzf backup.tar.gz
```

🧠 **速记：** `tar` 重“打包”，`gzip` 重“压缩”；`.tar.gz` 往往表示先归档再 gzip 压缩。

### 5. 双绞线、同轴电缆与光纤：介质辨析

| 介质 | 高频特点 | 常见场景 |
|---|---|---|
| 双绞线 | 成本低、布线方便 | 以太网局域网常见 |
| 同轴电缆 | 有中心导体和屏蔽结构 | 早期以太网、有线电视等历史/应用场景 |
| 光纤 | 用光传输，带宽高、距离远、抗电磁干扰能力强 | 骨干、长距离、高速网络 |

- 常见以太网双绞线接口为 **RJ-45**。
- 光纤不使用 RJ-45 作为典型光接口。

📌 **国网考点：** “抗电磁干扰、远距离、高带宽”通常优先想到光纤。

### 6. 视频编码与容器：MP4/MKV 不等于具体编码算法

- **编码/Codec**：规定音视频数据如何压缩和还原，例如 H.264/AVC、H.265/HEVC 等。
- **容器格式**：把视频流、音频流、字幕、元数据等封装在一个文件中，常见 MP4、MKV 等。
- **帧率**：单位时间显示的画面帧数；与分辨率不是同一概念。
- **分辨率**：画面像素尺寸，如宽×高。

⚠️ **易错提醒：** `.mp4` 主要说明容器格式，并不能单凭扩展名确定内部一定采用哪一种视频编码。

### 7. 系统软件、应用软件、工具软件与中间件

| 类别 | 基础定位 | 例子/关键词 |
|---|---|---|
| 系统软件 | 管理和控制计算机资源，为应用提供运行基础 | 操作系统、部分驱动/系统程序 |
| 应用软件 | 面向用户具体业务或任务 | 办公、浏览器、图像处理 |
| 工具软件 | 辅助维护、管理、诊断等 | 压缩、备份、磁盘工具 |
| 中间件 | 位于操作系统/基础平台与应用之间，提供通用服务 | 通信、消息、事务、应用支撑 |

📌 **国网考点：** 编译程序等语言处理程序通常归入系统软件范畴；中间件不是硬件。

### 8. CRT 与 LCD：老教材口径和现代情况

| 项目 | CRT | LCD |
|---|---|---|
| 基本原理 | 阴极射线管电子束激发荧光屏 | 液晶调制光线形成图像 |
| 体积重量 | 通常较大、较重 | 通常更薄、更轻 |
| 历史定位 | 传统显示器典型技术 | 后来成为平板显示主流技术之一 |

- **考试口径：** CRT＝阴极射线管显示器，是经典历史题关键词。
- **现代情况：** 桌面显示设备已广泛采用 LCD 等平板显示技术，CRT 基本退出主流消费市场。

### 9. 备份：全量、增量、差异备份先抓基本关系

| 类型 | 本次备份内容 | 恢复时典型需求 |
|---|---|---|
| 全量备份 | 选定数据全部备份 | 只需最近一次有效全量备份 |
| 增量备份 | 自上一次备份（全量或增量）以来变化的数据 | 全量 + 之后各次增量 |
| 差异备份 | 自最近一次全量备份以来变化的数据 | 全量 + 最近一次差异 |

⚠️ **易错提醒：** 增量备份通常备份量较小，但恢复链较长；差异备份越接近下一次全量，备份量可能逐渐增大。

## 二、国网风格练习题

### 1. 下列关于 CPU 主频的说法，正确的是（ ）。
A. 主频越高，任何情况下整机性能一定越高  
B. 主频是衡量 CPU 的指标之一，但不能单独决定整机性能  
C. 主频表示 CPU 地址总线宽度  
D. 主频表示内存容量

**答案：B**  
**解析：** 主频是 CPU 性能相关指标之一，但实际性能还受微体系结构、缓存、核心数、内存等影响。A 把单一指标绝对化；C、D 概念错误。

### 2. 下列最符合 NVMe 定位的是（ ）。
A. 图像压缩格式  
B. 面向非易失性存储、针对高速互连优化的协议  
C. Windows 文件系统  
D. 无线局域网标准

**答案：B**  
**解析：** NVMe 是面向非易失性存储的协议，现代 NVMe SSD 常使用 PCIe。它不是文件系统、图像格式或无线标准。

### 3. 在 Windows/DOS 命令中，用于重命名文件的典型命令是（ ）。
A. ren  
B. type  
C. cls  
D. tree

**答案：A**  
**解析：** `ren` 用于重命名；`type` 显示文本文件内容；`cls` 清屏；`tree` 显示目录树。

### 4. Linux 中关于 tar 与 gzip 的说法，正确的是（ ）。
A. tar 的核心作用只是加密文件  
B. gzip 只能创建目录  
C. tar 常用于归档，gzip 常用于压缩  
D. tar 与 gzip 都是进程查看命令

**答案：C**  
**解析：** `tar` 主要负责归档，`gzip` 主要负责压缩，两者常组合生成 `.tar.gz`。A、B、D 均混淆了命令用途。

### 5. 在常见网络传输介质中，抗电磁干扰能力强且适合高速长距离传输的是（ ）。
A. 光纤  
B. 双绞线  
C. 普通电话线  
D. 并口电缆

**答案：A**  
**解析：** 光纤利用光信号传输，不易受电磁干扰，并适合高速和长距离通信。双绞线在局域网很常见，但此题关键词更明确指向光纤。

### 6. 关于 MP4 的说法，较准确的是（ ）。
A. MP4 只能封装一种固定的视频编码  
B. MP4 是常见多媒体容器格式  
C. MP4 是无损图像格式  
D. MP4 是字符编码标准

**答案：B**  
**解析：** MP4 是容器格式，可封装音视频流等内容。容器与具体 Codec 不应混淆，因此 A 错误。

### 7. 下列最适合归入应用软件的是（ ）。
A. 操作系统  
B. 设备驱动程序  
C. 办公文字处理软件  
D. 固件

**答案：C**  
**解析：** 文字处理软件直接面向用户完成具体任务，属于典型应用软件。操作系统、驱动更接近系统软件；固件运行于设备硬件层面。

### 8. 传统教材中 CRT 显示器的核心器件是（ ）。
A. 阴极射线管  
B. 激光二极管阵列  
C. 固态硬盘  
D. CMOS 电池

**答案：A**  
**解析：** CRT 即 Cathode-Ray Tube，中文常称阴极射线管。现代桌面显示器已主要采用 LCD 等平板技术，但老题仍可能考 CRT 的基本定位。

### 9. 某系统周日做全量备份，此后每天做增量备份。若周三数据损坏，典型恢复过程需要（ ）。
A. 只使用周三增量备份  
B. 周日全量备份以及之后需要的各次增量备份  
C. 只使用周二增量备份  
D. 不需要任何全量备份

**答案：B**  
**解析：** 增量备份保存相对上一次备份后的变化，恢复通常需要最近全量以及其后的增量链。差异备份才通常只需全量加最近一次差异。

### 10. 【滚动复习】下列文件系统中，Windows 环境下支持文件权限和日志等典型特性的是（ ）。
A. NTFS  
B. FAT12  
C. GIF  
D. ASCII

**答案：A**  
**解析：** NTFS 支持 ACL 权限、日志等现代文件系统特性。GIF 是图像格式，ASCII 是字符编码，均不是文件系统。

### 11. 【滚动复习】Linux 中用于动态观察进程和资源使用情况的常用命令是（ ）。
A. top  
B. mkdir  
C. chmod  
D. gzip

**答案：A**  
**解析：** `top` 动态显示进程与系统资源。`mkdir` 创建目录，`chmod` 修改权限，`gzip` 用于压缩。

### 12. 【滚动复习】下列图像格式中，通常采用有损压缩且适合照片的是（ ）。
A. JPEG  
B. PNG  
C. TXT  
D. WAV

**答案：A**  
**解析：** JPEG 典型采用有损压缩，适合连续色调照片；PNG 常见无损压缩并支持透明。

### 13. 【滚动复习】DDR 内存属于（ ）。
A. DRAM 体系  
B. SRAM Cache 专用体系且不能作主存  
C. 光存储介质  
D. 文件系统

**答案：A**  
**解析：** DDR SDRAM 属于 DRAM 家族，是现代 PC 主存的重要类型。Cache 则常使用 SRAM。

### 14. 【滚动复习】IEEE 802.3 最典型对应（ ）。
A. Ethernet  
B. WLAN  
C. WiMAX  
D. Bluetooth

**答案：A**  
**解析：** IEEE 802.3 对应以太网；802.11 对应 WLAN；802.15 与 WPAN 相关；802.16 与宽带无线城域网/WiMAX 历史定位相关。

## 三、今日速记

- **CPU 三辨：** 主频＝时钟频率指标；字长＝一次处理数据位数的典型能力指标；总线宽度＝并行传送/寻址能力相关位数。
- **存储接口：** PATA/IDE 老式并行；SATA 串行；NVMe 面向非易失性高速存储，常走 PCIe；M.2 不等于 NVMe。
- **DOS 文件命令：** `copy` 复制、`move` 移动、`del` 删除文件、`ren` 改名、`type` 看文本、`dir` 看目录。
- **Linux 压缩：** tar＝归档；gzip＝压缩；`.tar.gz`＝常见“归档 + gzip”。
- **介质：** 双绞线＝局域网常客；同轴＝历史以太网/有线电视；光纤＝高速、远距、抗电磁干扰。
- **视频：** 分辨率≠帧率；容器≠编码；MP4/MKV 是典型容器概念。
- **软件分类：** OS→系统软件；办公软件→应用软件；压缩/备份→工具；中间件→给应用提供通用支撑服务。
- **CRT：** 阴极射线管，老教材高频；现代主流已转向 LCD 等平板显示。
- **备份：** 全量＝全备；增量＝自上次备份后的变化；差异＝自上次全量后的变化。恢复：增量链较长，差异通常“全量 + 最近差异”。
