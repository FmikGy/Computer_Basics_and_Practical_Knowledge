# 国网计算机基础与实用知识晨练｜2026-09-05

> **今日主题：** PC 固件与驱动、Windows 注册表与环境变量、Linux 用户与权限、字符编码、音视频格式、互联网应用基础  
> **今日目标：** 避开近几天已重点覆盖的 CPU/内存、存储接口、Linux 目录与文本工具、多媒体容量计算等内容，继续补齐注册表、用户管理、字符编码和应用协议等基础杂项。

## 一、今日知识点

### 1. 固件、驱动程序与操作系统

| 概念 | 核心作用 |
|---|---|
| 固件 Firmware | 存于设备非易失性存储中，负责设备底层控制与初始化 |
| 驱动程序 Driver | 帮助操作系统控制具体硬件 |
| 操作系统 OS | 管理软硬件资源，为应用提供运行环境 |

📌 国网考点：UEFI/BIOS 属于固件范畴；显卡驱动、打印机驱动属于软件组件。固件不等于驱动程序。

### 2. Windows 注册表

Windows 注册表是层次化配置数据库，用于保存系统、用户、硬件和部分应用配置。

| 根键 | 考试层面定位 |
|---|---|
| HKEY_LOCAL_MACHINE（HKLM） | 本机范围配置 |
| HKEY_CURRENT_USER（HKCU） | 当前登录用户配置 |
| HKEY_CLASSES_ROOT（HKCR） | 文件关联等信息 |

常用注册表编辑器命令：`regedit`。

⚠️ 注册表不是一个普通文本配置文件，也不是所有软件的全部配置都必须存放其中。

### 3. Windows 路径、PATH 与快捷方式

- 绝对路径：明确从盘符或根位置描述对象位置。
- 相对路径：相对于当前工作目录描述位置。
- `PATH`：系统查找可执行程序的重要搜索路径之一。
- `.lnk`：Windows 常见快捷方式文件，通常指向另一个目标对象。

```bat
echo %PATH%
where ping
```

⚠️ 删除快捷方式通常不等于删除原始目标文件。

### 4. Linux 用户、组与身份切换

Linux 是典型多用户系统。用户通常由 UID 标识，组由 GID 标识；root 是传统超级用户账户。

| 命令 | 用途 |
|---|---|
| `id` | 查看 UID、GID、所属组 |
| `passwd` | 修改密码 |
| `useradd` | 创建用户 |
| `usermod` | 修改用户属性 |
| `userdel` | 删除用户 |
| `su` | 切换用户身份 |
| `sudo` | 按授权以其他身份执行指定命令 |

⚠️ `su` 侧重切换身份，`sudo` 侧重按授权执行命令，二者不是同义词。

### 5. Linux 文件与目录权限

权限数字：`r=4，w=2，x=1`。

| 权限 | 普通文件 | 目录 |
|---|---|---|
| r | 读取内容 | 列出目录项名称 |
| w | 修改内容 | 创建、删除、重命名目录项（还受其他条件影响） |
| x | 执行文件 | 进入/遍历目录 |

```bash
chmod 755 script.sh
chmod 644 notes.txt
chown alice:staff notes.txt
```

`755 = rwxr-xr-x`，`644 = rw-r--r--`。

📌 `chmod` 改权限；`chown` 改所有者；`chgrp` 改所属组。

### 6. ASCII、Unicode、UTF-8、UTF-16、GB2312、GBK

| 名称 | 考试层面理解 |
|---|---|
| ASCII | 标准 ASCII 使用 7 bit，可表示 128 个码值 |
| Unicode | 为全球字符建立统一码点体系 |
| UTF-8 | Unicode 的变长编码方式，与 ASCII 的 0～127 范围兼容 |
| UTF-16 | 以 16 位代码单元为基础的 Unicode 编码方式 |
| GB2312 | 较早的简体中文国家标准字符集/编码体系 |
| GBK | 对 GB2312 的扩展，覆盖更多字符 |

⚠️ Unicode 不等于 UTF-8；UTF-8 是 Unicode 的一种编码形式，也不是所有字符都固定占 1 字节。

### 7. 音视频：编码与容器不要混

| 类别 | 典型例子 |
|---|---|
| 音频格式/编码 | WAV、MP3、AAC |
| 视频编码 | H.264/AVC、H.265/HEVC |
| 多媒体容器 | MP4、MKV、AVI |

容器可以封装视频、音频、字幕和元数据等，因此 `.mp4` 扩展名不能完整说明内部采用了哪一种视频编码。

📌 MP3 通常采用有损音频压缩；基础题常以 WAV/PCM 作为未压缩数字音频的典型代表，但 WAV 容器并非逻辑上只能装未压缩数据。

### 8. 常见互联网应用协议

| 协议/技术 | 典型用途 |
|---|---|
| HTTP/HTTPS | Web 数据传输 |
| FTP | 文件传输 |
| SMTP | 发送、转发电子邮件 |
| POP3/IMAP | 客户端收取或访问邮件 |
| SSH | 加密远程登录与管理 |
| Telnet | 传统远程终端协议 |

🧠 速记：`Web→HTTP(S)，传文件→FTP，发邮件→SMTP，安全远程登录→SSH`。

## 二、国网风格练习题

### 1. 设备驱动程序的主要作用是（ ）。
A. 代替硬件设备  
B. 帮助操作系统控制具体硬件  
C. 只负责保存用户文档  
D. 只能存在于 BIOS 中

**答案：B**

**解析：** 驱动程序是操作系统与具体硬件之间的重要软件接口，不等于硬件或固件。

### 2. Windows 中，与当前登录用户配置关系最直接的注册表根键是（ ）。
A. HKEY_CURRENT_USER  
B. HKEY_LOCAL_MACHINE  
C. HKEY_CLASSES_ROOT  
D. HKEY_ONLY_ROOT

**答案：A**

**解析：** HKEY_CURRENT_USER（HKCU）表示当前用户相关配置；HKLM 更偏本机范围配置。

### 3. Windows 环境变量 `PATH` 的主要用途之一是（ ）。
A. 指定所有文件必须保存的位置  
B. 为查找可执行程序提供搜索路径  
C. 自动压缩文件  
D. 保存注册表副本

**答案：B**

**解析：** 命令解释器等可依据 PATH 中的目录查找用户直接输入名称的可执行程序。

### 4. Linux 中查看 UID、GID 和所属组等信息的命令是（ ）。
A. `id`  
B. `pwd`  
C. `df`  
D. `tar`

**答案：A**

**解析：** `id` 显示用户和组身份信息；`pwd` 显示当前目录；`df` 查看文件系统空间。

### 5. 对 Linux 目录而言，`x` 权限的典型含义是（ ）。
A. 自动删除目录  
B. 允许进入/遍历目录  
C. 自动压缩目录  
D. 修改文件所有者

**答案：B**

**解析：** 目录的执行权限表示搜索/遍历权限，是按路径访问内部对象的重要条件。

### 6. `rwxr-xr-x` 对应的八进制权限是（ ）。
A. 644  
B. 755  
C. 777  
D. 600

**答案：B**

**解析：** `rwx=7`，`r-x=5`，因此为 755。

### 7. 关于 Unicode 与 UTF-8，正确的是（ ）。
A. 二者完全相同  
B. UTF-8 是 Unicode 的一种编码形式  
C. UTF-8 所有字符固定占 1 字节  
D. Unicode 只能表示英文

**答案：B**

**解析：** Unicode 建立字符码点体系，UTF-8 是表示这些码点的一种变长编码方式。

### 8. 标准 ASCII 使用多少位表示 128 个码值？（ ）
A. 4 bit  
B. 7 bit  
C. 16 bit  
D. 32 bit

**答案：B**

**解析：** 标准 ASCII 为 7 位编码，`2^7=128`。

### 9. 下列最典型的多媒体容器格式是（ ）。
A. H.264  
B. H.265  
C. MP4  
D. ASCII

**答案：C**

**解析：** MP4 是容器格式；H.264/H.265 是视频编码标准；ASCII 是字符编码。

### 10. 电子邮件体系中，主要用于发送和转发邮件的协议是（ ）。
A. SMTP  
B. POP3  
C. IMAP  
D. SSH

**答案：A**

**解析：** SMTP 用于发送和服务器间转发邮件；POP3/IMAP 主要用于客户端收取或访问邮件。

### 11. 与传统 Telnet 相比，SSH 的突出特点是（ ）。
A. 只能用于打印机  
B. 提供加密远程登录与管理通道  
C. 只能传输邮件  
D. 不需要网络

**答案：B**

**解析：** SSH 提供加密和身份认证机制，传统 Telnet 的会话安全性较弱。

### 12. 【滚动复习】传统 PC 上电后进行基本硬件检测的过程称为（ ）。
A. POST  
B. RAID  
C. SMTP  
D. DHCP

**答案：A**

**解析：** POST 即 Power-On Self-Test，上电自检，是 BIOS/UEFI 启动相关典型考点。

### 13. 【滚动复习】Linux 中在目录树中按名称等条件查找文件的命令是（ ）。
A. `grep`  
B. `find`  
C. `uniq`  
D. `wc`

**答案：B**

**解析：** `find` 主要查找文件/目录；`grep` 主要按模式搜索文本内容。

### 14. 【滚动复习】PCIe 的典型特点是（ ）。
A. 点对点高速串行连接  
B. 只能连接显卡  
C. 属于软盘接口  
D. 使用传统并行共享总线作为核心方式

**答案：A**

**解析：** PCIe 采用高速串行、点对点链路，区别于传统 PCI 的并行共享总线思路。

## 三、今日速记

```text
Firmware → 设备底层控制/初始化
Driver   → OS 控制具体硬件的软件接口
OS       → 管理软硬件资源

HKLM → 本机配置
HKCU → 当前用户配置
regedit → 注册表编辑器
PATH → 可执行程序搜索路径之一
.lnk → Windows 快捷方式

Linux：
id       → UID/GID/组
passwd   → 密码
useradd  → 建用户
usermod  → 改用户
userdel  → 删用户
su       → 切换身份
sudo     → 按授权执行命令

r=4, w=2, x=1
755=rwxr-xr-x
644=rw-r--r--
目录 x → 进入/遍历

ASCII → 标准 7 bit
Unicode → 统一码点体系
UTF-8 → Unicode 变长编码
UTF-16 → 以 16 位代码单元为基础
GBK → 扩展 GB2312

MP4/MKV/AVI → 容器
H.264/H.265 → 视频编码
SMTP → 发邮件
POP3/IMAP → 收取/访问邮件
SSH → 加密远程登录
FTP → 文件传输
```