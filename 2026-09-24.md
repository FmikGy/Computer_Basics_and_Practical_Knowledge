# 国网计算机基础与实用知识晨练｜2026-09-24

> **今日主题：** RAM/ROM/SRAM/DRAM、Windows 文件系统、Linux 文本查看与检索、IEEE 802 系列定位、图像格式、编译/解释/汇编/链接/装入、USB 与 PS/2、bit/Byte 与容量单位  
> **今日目标：** 近几日已重点覆盖 HDD/SSD、POST、PCI/PCIe、Windows 网络/磁盘命令、Linux 身份/文件类型/挂载、网络设备、字符编码、音视频、芯片组等。今天补齐内存分类、Windows 文件系统、Linux 文本工具、IEEE 802 总体关系、图像格式、语言处理程序、外设接口和容量单位，并用习题间隔复习近期内容。

## 一、今日知识点

### 1. RAM、ROM、SRAM、DRAM：先分“可读写/易失”，再看实现

| 概念 | 基础定位 | 典型特点 | 常见用途/考点 |
|---|---|---|---|
| RAM | 随机存取存储器 | 通常可读写，断电后数据通常丢失 | 主存等 |
| ROM | 只读存储器大类 | 非易失，传统口径强调只读/不易改写 | 固件存储等历史教材场景 |
| SRAM | 静态 RAM | 不需像 DRAM 那样周期刷新，速度快、成本高、密度较低 | Cache 常见实现 |
| DRAM | 动态 RAM | 需刷新，密度高、成本较低 | 主存常见实现 |

- “静态/动态”不是“是否断电保存”的区别：SRAM 和 DRAM 都属于易失性 RAM。
- DDR SDRAM 属于 DRAM 技术路线，现代 PC 主内存通常使用 DDR 系列 SDRAM。
- Cache 是功能层次概念，SRAM 是实现技术；二者不能直接当同义词。

📌 **国网考点：** 常考“Cache 常采用 SRAM、主存常采用 DRAM”。

⚠️ **易错提醒：** ROM 的现代实现可存在可擦写/可编程形式；传统选择题若只问 RAM 与 ROM 的基本差异，按“RAM 易失且可读写，ROM 非易失”理解即可。

### 2. FAT32 与 NTFS：Windows 文件系统基础辨析

| 项目 | FAT32 | NTFS |
|---|---|---|
| 定位 | FAT 家族常见文件系统 | Windows NT 系列常用现代文件系统 |
| 权限/安全 | 功能相对简单 | 支持更完善的访问控制等特性 |
| 日志 | 不属于典型日志文件系统 | 具有日志机制 |
| 单文件大小 | 存在约 4 GiB 的单文件上限 | 可支持远大于 FAT32 的文件 |
| 典型场景 | 兼容性要求较高的可移动介质等 | Windows 系统盘、日常磁盘卷常见 |

- “文件系统”负责组织和管理存储介质上的文件与目录，不等于硬盘接口。
- FAT、FAT32、NTFS 是文件系统；SATA、NVMe 是存储连接/协议相关概念，不能混为一类。

📌 **国网考点：** NTFS 的权限、日志等能力通常比 FAT32 更丰富。

### 3. cat、more、less、head、tail、grep：看内容与找内容

| 命令 | 基础用途 |
|---|---|
| `cat` | 连接并输出文件内容，常用于直接查看较短文本 |
| `more` | 分页查看文本 |
| `less` | 交互式分页查看，通常比 `more` 操作更灵活 |
| `head` | 查看文件开头部分 |
| `tail` | 查看文件末尾部分 |
| `grep` | 按模式搜索文本行 |

示例：

```bash
head -n 10 app.log
tail -n 20 app.log
grep "ERROR" app.log
```

- `grep` 是内容检索工具；`find` 更典型地用于按名称、类型、时间等条件在目录树中查找文件。
- `tail -f` 常用于持续观察不断追加的日志文件，但基础考试首先记住“tail 看尾部”。

🧠 **速记：** head 看头、tail 看尾、grep 找行、find 找文件。

### 4. IEEE 802 系列：别把 802.11 当成整个 802

| 标准族 | 基础定位 | 关键词 |
|---|---|---|
| IEEE 802.1 | 局域网/城域网体系中的桥接、管理等 | VLAN、桥接等 |
| IEEE 802.2 | 历史上的 LLC（逻辑链路控制） | LLC |
| IEEE 802.3 | 以太网 | Ethernet |
| IEEE 802.5 | 令牌环网（历史标准） | Token Ring |
| IEEE 802.11 | 无线局域网 | WLAN / Wi-Fi |
| IEEE 802.15 | 无线个域网相关 | WPAN |
| IEEE 802.16 | 宽带无线城域网相关 | WiMAX |

📌 **国网考点：** 802.3 = Ethernet，802.11 = WLAN，802.15 = WPAN，802.16 常与 WiMAX 对应。

⚠️ **历史口径：** 802.5 令牌环、802.16/WiMAX 在现代个人终端中并非主流，但老题仍可能直接考标准号对应关系。

### 5. BMP、JPEG、PNG、GIF、TIFF：图像格式看“压缩、透明、动画”

| 格式 | 典型特点 | 选择题识别点 |
|---|---|---|
| BMP | 传统位图格式，常见情形下数据量较大 | 位图、简单直接 |
| JPEG/JPG | 典型有损压缩 | 照片、连续色调图像 |
| PNG | 无损压缩，支持透明 | 网页图形、透明背景 |
| GIF | 采用调色板，支持简单动画和透明 | 动图 |
| TIFF | 灵活的图像文件格式，常用于扫描、出版等 | 可支持多种压缩/色彩表示 |

- “有损”不等于“质量一定很差”，而是压缩过程可能丢弃部分信息。
- PNG 与 JPEG 的典型辨析：PNG 常用于需要无损或透明的图形，JPEG 常用于照片压缩。
- 文件扩展名只是识别线索，改名为 `.jpg` 并不会自动把其他格式真正转换为 JPEG。

### 6. 编译、解释、汇编、链接、装入：程序变成可运行形态的角色

| 程序 | 基础作用 |
|---|---|
| 编译程序 | 将高级语言源程序整体转换为目标代码/中间结果等 |
| 解释程序 | 边解释边执行源程序或中间表示，传统教材常强调不生成独立目标程序 |
| 汇编程序 | 将汇编语言翻译为机器相关的目标代码 |
| 链接程序 | 将目标模块和所需库等连接，形成可执行映像/文件 |
| 装入程序 | 将可执行程序装入内存，为运行做好准备 |

典型流程可速记为：

`源程序 → 编译/汇编 → 目标模块 → 链接 → 可执行文件 → 装入 → 运行`

⚠️ **易错提醒：** 编译器不是链接器；链接解决多个目标模块、库之间的组合与符号引用等问题。

### 7. USB 与 PS/2：通用串行接口和传统键鼠接口

- **USB（Universal Serial Bus）**：通用串行总线，可连接键盘、鼠标、存储设备、打印机等多种外设，支持即插即用，现代设备应用广泛。
- **PS/2**：传统 PC 键盘、鼠标常用接口，通常使用圆形 Mini-DIN 连接器；现代消费级设备上已明显减少。
- USB 是“总线/接口体系”，并不是只为 U 盘设计；“U 盘”只是 USB 大容量存储设备的常见形态之一。

📌 **考试口径：** 遇到老式键盘/鼠标接口常想到 PS/2；现代通用外设接口首先想到 USB。

### 8. bit、Byte、KB、MB、GB：位和字节先别差 8 倍

- `bit`（位，常写 b）是二进制信息的基本单位；`Byte`（字节，常写 B）通常由 8 bit 组成。
- 网络速率常写 Mb/s、Gb/s，存储容量常写 MB、GB；大小写不能随意忽略。
- 传统计算机考试中常按二进制倍数计算：1 KB = 1024 B，1 MB = 1024 KB，1 GB = 1024 MB。
- 现代标准中也使用 KiB、MiB、GiB 明确表示 1024 倍，而 SI 的 kB/MB/GB 可按 1000 倍；题目若明确口径，应以题干为准。

🧠 **速记：** 1 B = 8 b；“速率小 b，容量大 B”是常见识别法，但仍以单位全称和题干为准。

## 二、国网风格练习题

### 1. 在典型微型计算机中，最常用于实现 CPU Cache 的存储器是（ ）。

A. DRAM  
B. SRAM  
C. 磁盘  
D. DVD

**答案：B**

**解析：** SRAM 速度快且无需像 DRAM 那样周期刷新，常用于 Cache；DRAM 密度高、成本较低，常用于主存。两者通常都属于易失性存储器。

### 2. 下列关于 SRAM 和 DRAM 的说法，正确的是（ ）。

A. SRAM 断电后仍能长期保存数据  
B. DRAM 不需要刷新  
C. SRAM 常用于高速缓存，DRAM 常用于主存  
D. DRAM 属于 ROM

**答案：C**

**解析：** SRAM 和 DRAM 都属于 RAM，通常断电丢失数据；DRAM 需要刷新。A 把“静态”误解为“非易失”，B、D 均错误。

### 3. 与 FAT32 相比，NTFS 更典型的特点是（ ）。

A. 只能用于软盘  
B. 支持更完善的访问控制和日志等机制  
C. 单文件只能小于 4 GiB  
D. 属于硬盘接口协议

**答案：B**

**解析：** NTFS 支持权限控制、日志等较丰富特性。约 4 GiB 单文件限制是 FAT32 的典型限制之一；文件系统也不等于 SATA/NVMe 等接口或协议。

### 4. Linux 中用于按模式搜索文本行的命令是（ ）。

A. grep  
B. pwd  
C. mkdir  
D. mount

**答案：A**

**解析：** `grep` 用于文本模式匹配。`pwd` 显示当前目录，`mkdir` 创建目录，`mount` 挂载文件系统。

### 5. 若要快速查看 Linux 文本文件最后若干行，最适合的基础命令是（ ）。

A. head  
B. tail  
C. chown  
D. passwd

**答案：B**

**解析：** `tail` 查看文件尾部，`head` 查看开头。`chown` 修改所有者，`passwd` 管理密码。

### 6. IEEE 802.3 主要对应（ ）。

A. 无线局域网  
B. 以太网  
C. 令牌环网  
D. WiMAX

**答案：B**

**解析：** 802.3 对应以太网；802.11 对应 WLAN；802.5 对应历史上的 Token Ring；802.16 常与宽带无线城域网/WiMAX 联系。

### 7. 下列标准与技术对应正确的是（ ）。

A. IEEE 802.11—WLAN  
B. IEEE 802.3—WiMAX  
C. IEEE 802.15—Ethernet  
D. IEEE 802.16—Token Ring

**答案：A**

**解析：** 802.11 是无线局域网标准族。802.3 是 Ethernet，802.15 与 WPAN 相关，802.16 与宽带无线城域网/WiMAX 相关。

### 8. 对照片类连续色调图像进行有损压缩时，最典型的格式是（ ）。

A. JPEG  
B. PNG  
C. TXT  
D. WAV

**答案：A**

**解析：** JPEG 是典型有损图像压缩格式，常用于照片。PNG 典型为无损压缩并支持透明；TXT 是文本，WAV 是音频相关格式。

### 9. 下列关于 PNG 和 GIF 的说法，正确的是（ ）。

A. PNG 只能有损压缩  
B. GIF 可支持简单动画  
C. GIF 是音频格式  
D. PNG 不支持透明

**答案：B**

**解析：** GIF 的高频识别点是调色板图像、简单动画；PNG 使用无损压缩并支持透明。A、C、D 均错误。

### 10. 将多个目标模块及所需库组合形成可执行文件，主要由（ ）完成。

A. 链接程序  
B. 文本编辑器  
C. 装入程序  
D. 设备驱动程序

**答案：A**

**解析：** 链接器负责组合目标模块、库并处理相关符号引用；装入程序主要把可执行程序装入内存。二者是典型易混项。

### 11. 关于 USB 和 PS/2，下列说法正确的是（ ）。

A. USB 只能连接存储设备  
B. PS/2 是传统键盘、鼠标常见接口  
C. PS/2 是现代固态硬盘协议  
D. USB 不支持键盘

**答案：B**

**解析：** PS/2 是传统键鼠接口。USB 是通用外设接口，可连接键鼠、存储、打印机等多种设备，因此 A、D 错；PS/2 与 SSD 协议无关。

### 12. 若某网络链路速率标为 80 Mb/s，忽略协议开销，其理论上相当于每秒约（ ）MB 数据量。

A. 5  
B. 10  
C. 40  
D. 80

**答案：B**

**解析：** 1 Byte = 8 bit，因此 80 Mb/s ÷ 8 = 10 MB/s。题目考查 b 与 B 的 8 倍关系；实际有效传输速率还会受协议开销、设备等因素影响。

### 13. 【间隔复习】下列关于 SSD 与 NVMe 的说法正确的是（ ）。

A. 所有 SSD 都必须使用 NVMe  
B. NVMe 是机械硬盘的盘片类型  
C. SATA SSD 也属于 SSD  
D. SSD 一定具有机械磁头

**答案：C**

**解析：** SSD 是固态存储设备类型，既可有 SATA SSD，也常见经 PCIe 使用 NVMe 的 SSD。NVMe 是面向非易失性存储的协议，不等于 SSD 本身。

### 14. 【间隔复习】Linux 中显示 UID、GID 及组信息的常用命令是（ ）。

A. id  
B. format  
C. tracert  
D. attrib

**答案：A**

**解析：** `id` 用于显示用户身份信息。其余三个是 Windows/DOS 语境中的命令或工具名称，分别涉及格式化、路由跟踪、文件属性。

### 15. 【间隔复习】传统 PC 架构中，北桥通常更接近处理（ ）等高速连接。

A. CPU、内存及早期高速图形接口  
B. 打印机墨盒  
C. 文件压缩格式  
D. Linux 用户密码

**答案：A**

**解析：** 传统北桥负责 CPU、内存、AGP/高速图形等较高速连接，南桥偏向 I/O。现代平台大量功能已集成进 CPU，并以 PCH 等架构取代传统“北桥+南桥”的经典分工，因此老题要按历史口径识别。

## 三、今日速记

| 看到关键词 | 快速联想 |
|---|---|
| Cache 常见实现 | SRAM |
| PC 主存常见实现 | DRAM / DDR SDRAM |
| SRAM vs DRAM | SRAM 不需周期刷新；DRAM 需刷新 |
| NTFS | Windows 常用、权限/日志能力较丰富 |
| FAT32 | 兼容性广；约 4 GiB 单文件上限是高频点 |
| `head` / `tail` | 看头 / 看尾 |
| `grep` / `find` | 找文本行 / 找文件 |
| 802.3 | Ethernet |
| 802.11 | WLAN |
| 802.15 | WPAN |
| 802.16 | WiMAX/宽带无线城域网相关 |
| JPEG | 照片、有损 |
| PNG | 无损、透明 |
| GIF | 调色板、简单动画 |
| 编译/汇编 | 源代码 → 目标代码 |
| 链接 | 目标模块 + 库 → 可执行文件 |
| 装入 | 可执行程序进入内存 |
| PS/2 | 传统键鼠接口 |
| USB | 通用串行外设接口 |
| 1 Byte | 8 bit |

🧠 **最后一分钟口诀：** **“缓存 SRAM、主存 DRAM；三是以太、十一无线；JPEG 照片有损，PNG 无损透明，GIF 能动；编后要链，运行先装；小 b 看位，大 B 看字节。”**
